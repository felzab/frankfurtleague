# OPS · local stack
#
# The production images, built from source, on your machine, behind nginx. Started by
# `./scripts/local.sh`; serves on http://localhost:3000.
#
# It exists because `pnpm dev` cannot see a whole class of problem -- it exercises neither the
# standalone build, the startup environment gate, nginx, nor the security headers. This is the only
# place nginx and the security headers are exercised before a deploy; the image builds themselves are
# also proven by `./scripts/verify.sh --images` and by CI's images job.
#
#  INVARIANTS ───────────────────────────────────────────────────────────────────────────────────────
#
#   • Mirrors docker-compose.yml except where the difference is deliberate: it BUILDS rather than
#     pulls, overrides API_URL, AUTH_URL and LOG_FORMAT, publishes 3000:80 rather than 80:80 and
#     443:443, mounts nginx/local.conf and no ./certs, and sets no resource limits. A difference
#     beyond those is a difference production will not have caught.
#   • `AUTH_URL` must stay on loopback -- the constraint is written at the line that sets it.
#
#  SEE ALSO ─────────────────────────────────────────────────────────────────────────────────────────
#
#   docs/ops/spec.md -- the environments, and what each of them does not cover

x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

services:
  frontend:
    build:
      context: ./fl_frontend
      dockerfile: Dockerfile
    env_file: ./fl_frontend/.env
    environment:
      # Overrides whatever .env holds, because inside the compose network the backend is not on
      # localhost -- it is reachable by service name.
      - API_URL=http://backend:8000
      # http:// and localhost specifically: `fl_frontend/src/core/config.ts` refuses a non-loopback
      # http URL, because @auth/core derives the session cookie's Secure flag from this protocol.
      # Loopback is the one exemption.
      - AUTH_URL=http://localhost:3000
      # The point of this stack is the production behaviour, and the developer's .env legitimately
      # holds `console` for `pnpm dev` -- overridden so the stack always writes the production
      # stream (docs/logging/spec.md).
      - LOG_FORMAT=json
    restart: unless-stopped
    networks:
      - frankfurtleague-net
    logging: *default-logging
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://127.0.0.1:3000/favicon.ico",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  backend:
    build:
      context: ./fl_backend
      dockerfile: Dockerfile
    env_file: ./fl_backend/.env
    environment:
      # Same reason as the frontend's override above: the stack shows the production stream even
      # when the developer's .env selects the console format for `fastapi dev`.
      - LOG_FORMAT=json
    restart: unless-stopped
    networks:
      - frankfurtleague-net
    logging: *default-logging
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v0/system/is_live', timeout=10)",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "3000:80"
    volumes:
      - ./nginx/local.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - frankfurtleague-net
    logging: *default-logging
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy

networks:
  frankfurtleague-net:
    driver: bridge
