# Frontend

**Folder purpose:** the `fl_frontend/` Next.js application.

## Folder overview

| Read                         | For                                                                                                 |
| ---------------------------- | --------------------------------------------------------------------------------------------------- |
| [`overview.md`](overview.md) | What the app is for, and how `src/` is organised                                                    |
| [`spec.md`](spec.md)         | The contract — slices, cached reads and their tags, admin mutations, lint rules, environment, tests |

## Read next

- [`../backend/spec.md`](../backend/spec.md) — what the server-side fetches call, and what each failure returns
- [`../ops/spec.md`](../ops/spec.md) — the image this is built into, and the routing and headers above it
