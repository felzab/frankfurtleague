<!--
TEMPLATE — copy to the directory needing a README; delete this block.
Rules: ../chapters/3-corpus.md — the shape is OUT-3.
  - Hard cap 120 lines, this block included. The navigation table is mandatory.
  - At most ONE body section, holding the one thing a reader needs before opening anything in
    the table. A second body section means the content belongs in a spec sheet.
  - Inside that section the prohibitions still bind: a rule, a worked example or a term
    definition belongs in a chapter, a spec or the glossary — never here.
  - A closing "Read next" only where a page worth opening next is not already in the table.
    It is navigation rather than a body section, so it does not spend the one section above.
  - The stamp stays only where the page makes a claim of its own beyond the table (CUR-3).
-->

# \<folder\>

**Verified against:** `<sha>`, \<yyyy-mm-dd\>

**Folder purpose:** \<one line saying what this folder holds and for whom\>

## Folder overview

| Read                  | For                         |
| --------------------- | --------------------------- |
| \<file-or-subfolder\> | \<the question it answers\> |

## \<The one thing to know first\>

\<The constraint, convention or entry point a reader must have before opening anything above.
Reasoning belongs in an ADR; a contract belongs in a spec sheet. Omit this section entirely
where the table is enough.\>

## Read next

\<Delete this heading and its list where every page worth opening next is already in the table.\>

- [\<page\>](path) — \<why you would go there\>
