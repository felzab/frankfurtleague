# Backend — spec

**Scope:** `fl_backend/`

| Section                                                                        | Answers                                                                                             |
| ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- |
| [1.1 Endpoint inventory](#11-endpoint-inventory)                               | Which endpoints exist, and behind which guard                                                       |
| [1.2 `GET /spiele` parameters](#12-get-spiele-parameters)                      | Which filters and sorts the fixtures read accepts                                                   |
| [1.3 The match write path](#13-the-match-write-path)                           | What one match write does, step by step                                                             |
| [1.4 Error codes and failure responses](#14-error-codes-and-failure-responses) | What a failure body carries, and what each code refuses                                             |
| [1.5 Environment](#15-environment)                                             | Which variables the process requires, and what each defaults to                                     |
| [1.6 The test suite](#16-the-test-suite)                                       | What the two tiers cover, and which marker separates them                                           |
| [1.7 Tier rules](#17-tier-rules)                                               | Which fields the base tier is not served, which a write moves into its reach, and what decides each |
| [2. Invariants](#2-invariants)                                                 | The rules that must hold                                                                            |
| [3. Violation → remedy](#3-violation--remedy)                                  | A symptom, its cause, and what to do about it                                                       |
| [4. Known-open](#4-known-open)                                                 | The accepted gaps                                                                                   |

---

## 1. Contract

### 1.1 Endpoint inventory

All paths are prefixed `/api/v{API_VERSION}` but the [unrouted](#unrouted) root, which no router and
no guard reaches either. Guards are declared at router level and inherited by every endpoint in the
router, `system` alone excepted (I7).

#### Base-tier reads — guard `verify_access_base`

| Method | Path                                    | Handler                        | Notes                                                                                                                                                                                       |
| ------ | --------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GET    | `/spiele`                               | `spiele/router.py`             | Filters below; omitted `saison_id` means the current season. **No POST** — see I26                                                                                                          |
| GET    | `/spiele/{spiel_id}`                    | `spiele/router.py`             | Unused by the frontend, which reads the admin twin. **No DELETE** — see I26                                                                                                                 |
| GET    | `/teams`                                | `teams/router.py`              | Two response shapes, discriminated by `format`; `statistik_scope` picks the table (I1c). It declares no `include_inactive` — `READ-SQUAD-002`                                               |
| GET    | `/teams/{team_id}`                      | `teams/router.py`              | `getTeam(id)` — the team detail pages. `format: "single"`                                                                                                                                   |
| GET    | `/spieler`                              | `spieler/router.py`            | The current season where `team_id` is named and `saison_id` is not (I4). Flattened per season, and redacted (`READ-PUPIL-001`, `READ-PUPIL-002`, `READ-PUPIL-003`); the admin list uses I33 |
| GET    | `/spieler/{spieler_id}`                 | `spieler/router.py`            | Unused by the frontend. A forename and an initial where consent publishes them, as the list serves (`READ-PUPIL-001`, `READ-PUPIL-003`)                                                     |
| GET    | `/spieltage`                            | `spieltage/router.py`          | Omitted `saison_id` means the current season                                                                                                                                                |
| GET    | `/spieltage/{spieltag_id}`              | `spieltage/router.py`          | Unused by the frontend, which reads the admin twin                                                                                                                                          |
| GET    | `/saisons`                              | `saisons/router.py`            | A `future` season is withheld from this tier — see I47                                                                                                                                      |
| GET    | `/saisons/current`                      | `saisons/router.py`            | **Declared before `/{saison_id}`** and must stay there — see I37                                                                                                                            |
| GET    | `/saisons/{saison_id}`                  | `saisons/router.py`            | Unused by the frontend                                                                                                                                                                      |
| GET    | `/bewerbungen/fenster`                  | `bewerbungen/public_router.py` | The season taking applications today, or 404: its window and that it has not ended, never the season, which I47 withholds as `future`                                                       |
| GET    | `/bewerbungen/fenster/{saison_id}`      | `bewerbungen/public_router.py` | The window and whether its season ended, or that none is recorded; only unknown ids 404, the `objectid` convertor keeping it off the id route                                               |
| GET    | `/bewerbungen/schulen`                  | `bewerbungen/public_router.py` | Every club still in the league, as an id and a name — the form's picker. Not `GET /teams`, which I11 scopes to a season                                                                     |
| GET    | `/bewerbungen/kuerzel/{shorthand}`      | `bewerbungen/public_router.py` | Whether any club holds that two-letter code. One boolean, naming no club and no retirement — `READ-BEWERBUNG-001`                                                                           |
| GET    | `/bewerbungen/trikotfarben/{saison_id}` | `bewerbungen/public_router.py` | The SET of kit colours that season has assigned, naming no club — `READ-BEWERBUNG-001`. Off `saison_teams.trikot_farbe`, never a wish; 404 outside the window (I47)                         |

**Every route that serves matches runs one aggregation**, the admin reads below included, because
each side of a fixture carries an `austritt_type` joined from `saison_teams` rather than embedded
(I32). **The shape it feeds follows the tier, not the route:** the three admin reads answer
`fl_backend/app/api/spiele/schemas.py :: FLSpielJoinedAdmin`, the two base-tier reads answer
`:: FLSpielJoined` (`READ-MONEY-001`, `READ-REFEREE-001`), and the stored `:: FLSpiel` is on neither.

#### Base-tier writes — guard `verify_access_base`

Every write an unauthenticated visitor may make to this service, and the POSTs that only read. They
sit on routers of their own, because a router declares one tier (I7), and each binds
`fl_backend/app/core/security.py :: bind_public_actor` rather than `:: bind_actor`: no browser sends
the `X-FL-Actor` an admin-tier write is refused without (I41). Every write here still goes through
`fl_backend/app/core/crud.py`, so the action log stays complete by construction — and an insert
carries no `before`, so the row records that a submission arrived rather than anything in it.

| Method | Path                                    | Effect                                                                                                                                                                                              |
| ------ | --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| POST   | `/bewerbungen`                          | Stores one school's application, every consent `administrativ` (I142), and mints three confirmation links answered raw beside the id (I144); `REQ-BEWERBUNG-004..008` refuse                        |
| POST   | `/bewerbungen/einwilligung/ansicht`     | Stores nothing: a POST so the token travels in a body, answering what one link opens (`READ-BEWERBUNG-002`); refuses only `REQ-BEWERBUNG-009`                                                       |
| POST   | `/bewerbungen/einwilligung`             | One person's answer for their seat and its pair, written in one `$set` (I141); a `Widerspruch` empties the slot; the mailer's fields are `READ-BEWERBUNG-003`'s                                     |
| POST   | `/registrierungen/einladung/ansicht`    | Stores nothing: a POST so the token travels in a body (`READ-EINLADUNG-001`). A shut window is a state rather than a refusal; only `REQ-EINLADUNG-003` refuses                                      |
| POST   | `/registrierungen`                      | Stores one pupil's registration and mints its confirmation link, answered raw once; six refusals in order precede the one write (`REQ-EINLADUNG-003`, `REQ-REGISTRIERUNG-001..003`, `-008`, `-009`) |
| POST   | `/registrierungen/bestaetigung/ansicht` | Stores nothing: a POST so the token travels in a body (`READ-REGISTRIERUNG-001`); refuses only `REQ-REGISTRIERUNG-004`                                                                              |
| POST   | `/registrierungen/bestaetigung`         | A pupil's own confirmation, the birthdate and the whole consent record written in one `$set` (`I285`); no Widerspruch door, and the registration stays pending                                      |
| POST   | `/schiedsrichter/bestaetigung/ansicht`  | Stores nothing: a POST so the token travels in a body, answering what one referee's link opens (`READ-REFEREE-002`); refuses only `REQ-SCHIEDSRICHTER-002`                                          |
| POST   | `/schiedsrichter/bestaetigung`          | A referee's own answer: their birthdate and consent record in one `$set`, reaching one collection (`REQ-SCHIEDSRICHTER-002`, `-004`, `-003`, `-005`, `-008`)                                        |

**`GET /bewerbungen` stays the admin list on `router.py`; only the POST at that path is public.** What
keeps this table closed is `fl_backend/tests/api/test_admin_guard.py :: PUBLIC_WRITES` (I7).

#### Admin-tier reads — guard `verify_access_admin`

**A tier served less is served by a different endpoint** (§1.7), so a base read that withholds
something is twinned here rather than widened by a parameter. `spielorte` has no twin because it has
no base tier to widen: its whole `router.py` carries the admin guard, reads included, and so do
`schiedsrichter`'s and `bewerbungen`'s. `schiedsrichter`'s reference read is admin-guarded whole
BESIDE a base-tier confirmation router, which serves a different document rather than a narrower
shape of that read. `bewerbungen`'s base tier is instead two routers of its own serving
DIFFERENT documents — a season's application window, the clubs a form may name, and what one confirmation link
opens — rather than a narrower shape of the reads below.

| Method | Path                                                | Handler                      | Notes                                                                                                                                                                                                        |
| ------ | --------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| GET    | `/saisons/list/admin`                               | `saisons/admin_router.py`    | Every season, the `future` ones `GET /saisons` withholds included. Two static segments, which I37 keeps ahead of the id route                                                                                |
| GET    | `/spiele/list/admin`                                | `spiele/admin_router.py`     | `GET /spiele`'s filters without its season gate, in the admin fixture shape: a referee's whole name and both figures                                                                                         |
| GET    | `/spiele/action_required`                           | `spiele/admin_router.py`     | Matches needing attention and the derived bracket faults among them, scoped to `saison_id`, the current season when none is named (I4) — I28                                                                 |
| GET    | `/spiele/{spiel_id}/admin`                          | `spiele/admin_router.py`     | One match as `FLSpielJoinedAdmin`, which the editor reads to round-trip `ort.mietpreis` and `schiedsrichter.payment` (`READ-MONEY-001`). A static suffix — the public router owns `GET /{spiel_id}` here     |
| GET    | `/spieltage/list/admin`                             | `spieltage/admin_router.py`  | `GET /spieltage`'s filters, order and shape without its season gate — a season's matchdays are drawn while it is still planned                                                                               |
| GET    | `/spieltage/{spieltag_id}/admin`                    | `spieltage/admin_router.py`  | `getAdminSpieltagById(id)` — the matchday editor page. A static suffix, for `GET /spiele/{spiel_id}/admin`'s reason                                                                                          |
| GET    | `/teams/list/admin`                                 | `teams/admin_router.py`      | `GET /teams`'s filters and both shapes without its season gate, plus the `include_inactive` the base tier is not offered                                                                                     |
| GET    | `/teams/memberships`                                | `teams/admin_router.py`      | Every team with every season membership — the admin club list's and club editor's read. Each carries the kit colour and contacts I50 withholds                                                               |
| GET    | `/spieler/memberships`                              | `spieler/admin_router.py`    | Every player with every squad row they hold — the admin list's read. Not answerable by `GET /spieler` — see I33                                                                                              |
| GET    | `/spieler/nachnominierung/{saison_id}`              | `spieler/admin_router.py`    | `nachnominierung`: whether an entry into that season's squads today would be one (I334). 404 for no such season                                                                                              |
| GET    | `/spielorte`                                        | `spielorte/router.py`        | Admin-tier whole, for the rent (`READ-MONEY-001`); the structured address rides along because no public surface renders the parts (`READ-ADDRESS-001`)                                                       |
| GET    | `/spielorte/{spielort_id}`                          | `spielorte/router.py`        | Unused by the frontend, which narrows the list it already holds                                                                                                                                              |
| GET    | `/schiedsrichter`                                   | `schiedsrichter/router.py`   | Admin-tier whole: a referee is a pupil (`READ-CONTACT-001`), and the fee is money (`READ-MONEY-001`). Called bare and with `include_inactive` — I227                                                         |
| GET    | `/schiedsrichter/{schiedsrichter_id}`               | `schiedsrichter/router.py`   | `getSchiedsrichterById(id)` — the referee record page's read. Answers 404 for the ghost and for an erased id — I227                                                                                          |
| GET    | `/bewerbungen`                                      | `bewerbungen/router.py`      | Admin-tier whole — contact records, and which schools were turned down (`READ-CONTACT-001`). Newest first, narrowed by statuses and `saisonbezug`, the named season or every other                           |
| GET    | `/bewerbungen/{bewerbung_id}`                       | `bewerbungen/router.py`      | `getBewerbungById(id)` — the triage page's read, which is what one decision is taken against                                                                                                                 |
| GET    | `/registrierungen`                                  | `registrierungen/router.py`  | The pending registrations, newest first, narrowable by `saison_id`, `team_id` and `status`. Every row carries `bestaetigt`, composed server-side; no token hash reaches it — `READ-CONTACT-001`              |
| GET    | `/aktionen`                                         | `aktionen/admin_router.py`   | Every recorded write, newest first, narrowed by collection, operation, origin, trace id or document id. It spans every collection (I40, I48, I208)                                                           |
| GET    | `/aktionen/{aktion_id}`                             | `aktionen/admin_router.py`   | One log row WITH the document its write replaced — the only read serving a pre-image (I43); the list reports `stand_gesichert` instead                                                                       |
| POST   | `/kontakte/erasure/ansicht`                         | `kontakte/admin_router.py`   | Stores nothing: a POST so the address travels in a body, answering whom `POST /kontakte/erasure` would clear, by name and season (`READ-CONTACT-001`)                                                        |
| GET    | `/sperrliste`                                       | `sperrliste/admin_router.py` | One capped page, newest first, beside `anzahl_gesamt`: reason, author's address, day, last season covered. No barred address and no hash (`fl_backend/app/api/sperrliste/schemas.py :: FLSperrlisteEintrag`) |
| GET    | `/teams/{team_id}/saisons/{saison_id}/einladung`    | `teams/admin_router.py`      | The live link's state and the window's verdict — never a hash (I275). `einladung: null` where the team holds none                                                                                            |
| GET    | `/saisons/{saison_id}/einladungen/versand/vorschau` | `saisons/admin_router.py`    | Who the season's mailing would reach and why it would skip a team, over the rule the press performs. Writes nothing                                                                                          |

**Every ENTITY resource carries a `GET /{id}` whether or not something calls it** — the callerless
ones are §4's. The one that carries none is not an entity: an erasure is keyed on an address rather
than on a row (`fl_backend/app/api/kontakte/admin_router.py`). The log is read as a stream, its
single read existing for the pre-image (I43) rather than for addressability. Which read each admin
editor opens on is [`docs/frontend/spec.md`](../frontend/spec.md) §1.2's.

#### Admin-tier writes — guard `verify_access_admin`

One `admin_router.py` per slice, beside the reads for the resource it changes. Every mutation
is addressed resource-first, with the subject in the path — `POST /kontakte/erasure` excepted, whose
subject is an email address and travels in the body instead.

| Method | Path                                                        | Effect                                                                                                                                                                                |
| ------ | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PATCH  | `/spiele/{spiel_id}`                                        | Writes one match, then resolves that season's bracket. See §1.3                                                                                                                       |
|        | `/spiele/{spiel_id}?dry_run=true`                           | The same call, reporting what it would move and destroy. Writes nothing                                                                                                               |
|        | `/spiele/paarungen`                                         | Puts every match one save moved back in ONE transaction, in the body's order — see I210, I215, I222, I223                                                                             |
|        | _no `POST /spiele`, no `DELETE /spiele/{spiel_id}`_         | No request creates or removes ONE fixture; a season's set is drawn and removed whole — see I26                                                                                        |
| POST   | `/teams`                                                    | Creates a club                                                                                                                                                                        |
| PATCH  | `/teams/{team_id}`                                          | Renames a club **and fans it out** into its `saison_teams` rows and its matches, in every season that is not `past` — see I13                                                         |
| DELETE | `/teams/{team_id}`                                          | Soft delete; 409 while the club is in an `active` or `future` season                                                                                                                  |
| POST   | `/teams/{team_id}/reactivate`                               | Clears `inactive_since`                                                                                                                                                               |
| POST   | `/teams/{team_id}/saisons`                                  | Adds the club to a season, seeding the row's `name` and `shorthand` from the club (I95). `saison_id` and `gruppe` on the body; `REQ-ENTER-001..003`, `-005` refuse                    |
| PATCH  | `/teams/{team_id}/saisons/{saison_id}`                      | Group, `austritt` and the kit colour, replaced WHOLESALE with no defaults — an omitted `austritt` reinstates a club that left. No DELETE (I19, I31, I50)                              |
| PATCH  | `/teams/{team_id}/saisons/{saison_id}/kontakte`             | The contact block, provenance composed server-side (I142); `null` clears it. Its own endpoint, and refuses a save the row moved past (I19, I50, I192)                                 |
| POST   | `/teams/{team_id}/saisons/{saison_id}/replace`              | Repoints the row and its fixtures at another club, clearing the outgoing school's `austritt`, kit colour and contacts and retiring its squad rows (I19, I137)                         |
| POST   | `/teams/{team_id}/saisons/{saison_id}/einladung`            | Mints the team's registration link, revoking its live one in the same transaction. The raw value is in this answer alone; `REQ-EINLADUNG-001`, `-002` refuse                          |
| DELETE | `/teams/{team_id}/saisons/{saison_id}/einladung`            | Stamps `widerrufen_am`; the row stays, so a delivery event still lands. 404 where the team holds no live link                                                                         |
| PATCH  | `/spieler/{spieler_id}`                                     | Updates a person. Replaces the whole person wholesale, so no field has a default — see I34                                                                                            |
| DELETE | `/spieler/{spieler_id}`                                     | Soft delete                                                                                                                                                                           |
| POST   | `/spieler/{spieler_id}/reactivate`                          | Clears `inactive_since`                                                                                                                                                               |
| POST   | `/spieler/{spieler_id}/saisons`                             | Adds a squad row, deriving `ist_nachnominiert` itself (I334). 409 on a repeat (I20), and on a `rolle` another live row of that squad holds (`REQ-SQUAD-004`)                          |
| PATCH  | `/spieler/{spieler_id}/saisons/{saison_id}`                 | Team, number, position, stufe and rolle for that season, replaced wholesale — see I34 and I334. `REQ-SQUAD-004` judges the role against the payload's squad                           |
| DELETE | `/spieler/{spieler_id}/saisons/{saison_id}`                 | Soft delete of the squad row, independent of the person                                                                                                                               |
| POST   | `/spieler/{spieler_id}/saisons/{saison_id}/reactivate`      | Brings a player back into a season they already hold a row for; `REQ-SQUAD-001`, `REQ-SQUAD-003` and `REQ-SQUAD-004` refuse                                                           |
| DELETE | `/spieler/{spieler_id}/erasure`                             | **Hard-deletes the person, every squad row and their logged values in one transaction** (I42, I48). Refused while they still stand in the league (`REQ-PURGE-001`)                    |
| POST   | `/saisons`                                                  | Creates a season, always `future` — see I18                                                                                                                                           |
| PATCH  | `/saisons/{saison_id}`                                      | Dates, scoring rules and both windows, replaced wholesale with no defaults — an omitted `bewerbung` or `registrierung` shuts an open window; `status` is I18's                        |
| POST   | `/saisons/{saison_id}/activate`                             | **The only path that writes `status`** — see I18, which holds `REQ-ACTIVATE-001` through `-004`                                                                                       |
| POST   | `/saisons/{saison_id}/gruppen/swap`                         | Two clubs exchange groups and their drawn Gruppenphase fixtures, all in one transaction — see I38                                                                                     |
| POST   | `/saisons/{saison_id}/spielplan`                            | Draws the season's matchdays and fixtures and stamps `saisons.spielplan` — I46's transaction. `replace` confirms a **destructive** redraw (`REQ-SPIELPLAN-005`) — see I26, I48        |
| DELETE | `/saisons/{saison_id}/spielplan`                            | UNDRAWS the season — I46's transaction, in the replace's window. **Destructive**, and what it reopens no replace reaches ([`docs/domain.md`](../domain.md)); see I26, I48             |
| POST   | `/saisons/{saison_id}/einladungen/versand`                  | Mints and answers one link per admitted team, revoking each team's own; four ordinary states skip. `REQ-EINLADUNG-002` refuses                                                        |
| PATCH  | `/spieltage/{spieltag_id}`                                  | Re-dates a matchday; the span is the whole payload, and `position` and `saison_phase` are the draw's. Neither the match count nor the name is stored                                  |
| POST   | `/spielorte`                                                | Creates a venue; builds `maps_link` server-side                                                                                                                                       |
| PATCH  | `/spielorte/{spielort_id}`                                  | Updates a venue **and fans the change out** into every match embedding it                                                                                                             |
| DELETE | `/spielorte/{spielort_id}`                                  | Soft delete                                                                                                                                                                           |
| POST   | `/spielorte/{spielort_id}/reactivate`                       | Clears `inactive_since`                                                                                                                                                               |
| POST   | `/schiedsrichter`                                           | Creates a referee, whose address is required, and mints their confirmation link in the same transaction, answering the raw token once (`REQ-SCHIEDSRICHTER-007`)                      |
| POST   | `/schiedsrichter/{schiedsrichter_id}/bestaetigung/einladen` | Mints a fresh confirmation link, answering the raw token and the address it was minted for, once (`REQ-SCHIEDSRICHTER-001`, `-004`, `-006`, `-007`, I227)                             |
| PATCH  | `/schiedsrichter/{schiedsrichter_id}`                       | Updates a referee **and fans the name out** into every match embedding it; an UNCONFIRMED live referee's corrected address re-mints their link (`REQ-SCHIEDSRICHTER-007`, I227, I309) |
| DELETE | `/schiedsrichter/{schiedsrichter_id}`                       | Soft delete. 404 for the ghost and for an erased id — I227                                                                                                                            |
| POST   | `/schiedsrichter/{schiedsrichter_id}/reactivate`            | Clears `inactive_since` and mints an unanswered referee's link, answering the raw token once (`REQ-SCHIEDSRICHTER-007`). 404 for the ghost and an erased id — I227, I259              |
| POST   | `/bewerbungen/{bewerbung_id}/annehmen`                      | Accepts an application — I51's transaction. `gruppe` and `trikot_farbe` on the body. **IRREVERSIBLE**: a club entered in error leaves only by an `austritt` (I19, I31)                |
| POST   | `/bewerbungen/{bewerbung_id}/kontakte/{seat}/email`         | Corrects one contact seat's address and mints its fresh link; the one field an administrator may correct on a submitted application (`REQ-BEWERBUNG-014`)                             |
| POST   | `/bewerbungen/{bewerbung_id}/kontakte/{seat}`               | Seats another person where that seat's own holder stepped out, and mints their link; the deadline restarts even where it had passed (`REQ-BEWERBUNG-011`) — I310                      |
| POST   | `/bewerbungen/{bewerbung_id}/ablehnen`                      | Declines an application, recording who decided and why. `status` and `entscheidung` move and nothing else, so the submission stays as the school wrote it (`REQ-BEWERBUNG-001`)       |
| POST   | `/bewerbungen/{bewerbung_id}/einwilligung/{seat}/erneut`    | One fresh link for every seat that person holds, answered RAW to mail; the old ones open nothing and the deadline restarts (`REQ-BEWERBUNG-001`, `-011`)                              |
| POST   | `/kontakte/erasure`                                         | Clears one contact person from every `saison_teams` and `bewerbungen` row and redacts the log images — I42's transaction. The SLOT is nulled, never the block                         |
| POST   | `/schiedsrichter/{schiedsrichter_id}/anonymisieren`         | Deletes the referee and repoints their fixtures at the ghost; the echo is the ghost, never the person. Refuses the ghost. See I42, I212, I213                                         |
| POST   | `/sperrliste`                                               | Records a ban on one address; no row and no log line holds it (I268), and the answer carries the last season it covers (I273)                                                         |
|        | `/sperrliste`                                               | `REQ-SPERRLISTE-001` refuses a second ban of one address; `REQ-SPERRLISTE-002` refuses one while no season is running                                                                 |
| DELETE | `/sperrliste/{sperrliste_id}`                               | Lifts a ban, removing the row. HARD, with no soft form: a ban carries no `inactive_since`, and the row yields no address to enter again                                               |

**No BARRED address leaves the ban list, on any route.** A ban is stored as an HMAC of the address
taken under `SPERRLISTE_SCHLUESSEL`, the address itself dropped (§1.5), and the admin read projects
even that hash away — so neither a response nor a log line carries a value the barred address can be
recovered from (I268, I269). **The row is not anonymous for that:** `erstellt_von` is the
administrator's own address in plain and `grund` is free text that may name the person barred, both
of them served and both outliving an erasure (`docs/glossary.md :: Sperrliste`). **What asks the
list:** `fl_backend/app/api/sperrliste/crud.py :: address_is_gesperrt` is reached by
the ban's own write, by the public registration and by the four referee writes that mint a
confirmation link — the create, the save, the re-send and the reactivation — each passing the season the ban is judged against.

**There is no `DELETE /saisons/{saison_id}`**, and none on `/teams/{team_id}/saisons/{saison_id}` or on
`/spieltage/{spieltag_id}` either — `spieltage` carries no `inactive_since`, so a matchday has no
field to stamp and none to clear.

**Every `spieltage` response carrying a matchday injects `anzahl_spiele` before validation, writes as
much as reads** (`fl_backend/app/api/spieltage/services.py :: with_expected_matches`). The field is
required on `FLSpieltag` and sits on no document, so **the rule reaches every echo, not only the
reads** — a new write endpoint that echoes a matchday injects too, or it answers 500 from its own
response.

**The resource-first rule bends for one class of write and no other: a decision about a season that no
per-resource route can express atomically.** `/saisons/{saison_id}/gruppen/swap` writes `saison_teams`
from the season's router, because the junction endpoint above addresses one club and a swap is one
decision about two. `/saisons/{saison_id}/spielplan` writes `spieltage` and `spiele` from it, because a
season's whole draw — written, replaced or taken away — is one decision and neither resource has a
create or a delete of its own to address. A write outside that class stays on its own resource.

**Writing another collection is not itself a bend; being ADDRESSED at another resource is.**
`POST /bewerbungen/{bewerbung_id}/annehmen` writes `teams` and `saison_teams` beside the
application, and `POST /kontakte/erasure` writes `saison_teams`, `bewerbungen` and the log.
Neither is addressed anywhere but at what it decides — the one application, the one person — and
what follows from that decision travels in its transaction (I42, I51) rather than in its path.

#### `system` router — mixed guards

| Method | Path               | Guard                                               |
| ------ | ------------------ | --------------------------------------------------- |
| GET    | `/system/is_live`  | **none** — healthcheck and public uptime probe (I7) |
| GET    | `/system/is_ready` | `verify_access_system`                              |
| GET    | `/system/info`     | `verify_access_system`                              |

#### `bewerbungen` sweep router — system tier

| Method | Path                                          | Guard and what it does                                                                                                                                                               |
| ------ | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| GET    | `/bewerbungen/sweep`                          | `verify_access_system`. Every season's id, oldest first, and the day the sweep last ran (I189); here rather than `/saisons` because I47 withholds a `future` season                  |
| POST   | `/bewerbungen/sweep/{saison_id}`              | `verify_access_system`. One season's six clocks: shares of the reminders stamped and minted and of the deletion candidates listed (I322), the silent clocks erased (I150–I152, I220) |
| POST   | `/bewerbungen/sweep/{saison_id}/angekuendigt` | `verify_access_system`. Stamps the candidates whose deletion notice was delivered, so a failed erasure mails nobody twice (I156)                                                     |
| POST   | `/bewerbungen/sweep/{saison_id}/loeschen`     | `verify_access_system`. Erases the announced candidates that still qualify, redacting their log rows (I151, I156)                                                                    |
| POST   | `/bewerbungen/zustellung`                     | `verify_access_system`. Applies one delivery event to the seats its message reached, where the id matches and the stamp is later (I195)                                              |
| POST   | `/bewerbungen/zustellung/angenommen`          | `verify_access_system`. Records the provider's id for an accepted message, which every later event joins back on (I195), where no newer accept stands (I267)                         |
| POST   | `/registrierungen/sweep/{saison_id}`          | `verify_access_system`. One season's four registration clocks: a share of the reminders stamped and minted (I322), three erasures made, who must be told (I289–I293)                 |

#### `zustellung` router — system tier

| Method | Path                     | Guard and what it does                                                                                                                   |
| ------ | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| POST   | `/zustellung`            | `verify_access_system`. Applies one delivery event to the record its payload names, where the id matches and the stamp is later (I266)   |
| POST   | `/zustellung/angenommen` | `verify_access_system`. Records the provider's id for an accepted message about one record, which every later event joins back on (I266) |
| POST   | `/zustellung/abgewiesen` | `verify_access_system`. Records the send the provider refused, under no message id, where no later accept stands (I298)                  |

#### `identitaet` router — system tier

| Method | Path                  | Guard and what it does                                                                                                                                |
| ------ | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| POST   | `/identitaet/subjekt` | `verify_access_system`. Which contact seats, pupil rows and referee rows one folded mailbox matches — ids and seat facts, no person's own data (I270) |

#### Unrouted

`GET /` returns the string `"Hello World"` (`app/main.py`). Not used by anything.

### 1.2 `GET /spiele` parameters

`GET /spiele/list/admin` accepts the same set: one
`fl_backend/app/api/spiele/schemas.py :: FLSpieleFilterParams` serves both.

| Param          | Type                                                                                | Default            | Notes                                                                                                                                                         |
| -------------- | ----------------------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `saison_id`    | `str \| None`                                                                       | **current season** | Resolved in the handler, never as a field default — see I4                                                                                                    |
| `saison_phase` | `playoffs \| gruppenphase \| achtelfinale \| viertelfinale \| halbfinale \| finale` | `None`             | `playoffs` is an alias compiled to `saison_phase != "gruppenphase"`; it is not a stored value                                                                 |
| `spiel_status` | `ausstehend \| vergangen \| heute \| abgesagt \| unbekannt`                         | `None`             | Compiled to a `datum` filter, or for `abgesagt` to `sonderereignis` over `SONDEREREIGNIS_RECORDING_AN_ABSENCE`. `unbekannt` has no branch and filters nothing |
| `team_id`      | `ObjectId`                                                                          | `None`             | Matches either side (`$or` over `team1`/`team2`)                                                                                                              |
| `limit`        | `int`                                                                               | `1024`             | `ge=1, le=1024`                                                                                                                                               |
| `sort_by`      | `datum \| uhrzeit \| spiel_nr \| saison_phase`                                      | `datum`            | Tie-broken by `spiel_nr`, and by `datum` when the sort key IS `spiel_nr` (`fl_backend/app/api/spiele/services.py :: build_spiele_sort`)                       |
| `order`        | `asc \| desc`                                                                       | `asc`              |                                                                                                                                                               |

### 1.3 The match write path

`PATCH /spiele/{spiel_id}` step by step, and `PATCH /spiele/paarungen` with it: both routes hand
their payload to `fl_backend/app/api/spiele/admin_router.py :: _write_spiel_data` as a LIST of fixtures,
and a narrow entry's body is completed from the stored fixture before step 1a, so every rule below judges
a whole fixture rather than the fields the request named (I210). **The restore runs
steps 0 to 5 once per entry and all of them in one transaction** (I222), so a refusal on any entry takes
the entries already written back with it.

| Step | Behaviour                                                                                                                                                                          | What breaks if changed                                                                                                                                          |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0    | The fixture's `saison_id` is read alone, and that read answers 404 for an id no fixture holds; the season's `rules` follow, outside any transaction                                | A payload naming no fixture would reach the judgement, and a forfeit awarded from a constant could disagree with the season being played (I3a)                  |
| 1    | `judge` reads the season one fixture over the list cap, runs every refusal below, and normalises last; save and `dry_run` preview both call it                                     | A preview could promise a write the save refuses, and a truncated season would be judged with a hole in it rather than detected (I45)                           |
| 1a   | The payload's `sonderereignis` is judged FIRST, against the goals beside it and nothing else — `REQ-STATE-002` and `REQ-STATE-003`, both decided by `find_state_refusal`           | The judgement is about the event the admin chose, so a contradiction inside it surfaces as a bracket fault or a silently discarded score                        |
| 1b   | Wiring the season cannot hold is a 409 — `REQ-WIRING-001`, `REQ-WIRING-002` or `REQ-WIRING-003` (§1.4) — decided by `find_wiring_refusal` inside the transaction, before the write | Any shape I27 names would be written, and a hand-set team on a maintained side silently reverted in the same request                                            |
| 1c   | An occupant the season cannot field is a 409 with its own code, decided by `find_eligibility_refusal` against the `saison_teams` rows read in-session                              | A team that left the season, or one the season never entered, would be written onto a fixture and nothing would say so (I100)                                   |
| 1d   | A side carrying goals on a played fixture may be switched but not emptied — 409 `REQ-RESULT-001`, decided by `find_result_removal_refusal`                                         | A result would survive as a half-fixture: goals standing against a slot with nobody in it                                                                       |
| 1e   | A team fielded twice on one Spieltag is 409 `REQ-SPIELTAG-001` where the clash cannot be moved, decided by `judge_spieltag_occupancy`, which otherwise names step 3a's sides       | A team would play two fixtures of one matchday and nothing would say so (I30)                                                                                   |
| 1f   | The fixture's `datum` must fall inside its Spieltag's span — `REQ-DATE-001`, decided by `find_fixture_date_refusal` in-session. A matchday missing or without a span is left alone | A matchday would silently hold a fixture outside its own dates, and `spieltag_id` is on no payload, so nothing else could put it back                           |
| 1g   | A venue or referee NEWLY assigned, or kept on a fixture the save makes unplayed again, that is unknown or retired is `REQ-BOOKING-001` (`find_booking_refusal`)                    | A fixture would name a row nothing answers to, which step 1k cannot compose from, or reopen onto a retired row                                                  |
| 1h   | A claim the stored fixture did not already make, less than four hours from another fixture's claim, is `REQ-CLASH-001` across **every** season (`find_claims_made`, I258)          | A claim would double-book a ground or a referee; judging kept claims too would refuse every save of a fixture a reopening left clashing (I257)                  |
| 1i   | A no-show's goal counts are composed from the season's `rules.forfeit_ergebnis`, the absent side taking the loser's; `ergebnis` follows from the pair, `None` where either is      | A client could submit a result disagreeing with the goals beside it, or a forfeit disagreeing with the regulation it expresses (I3a)                            |
| 1j   | `elfmeterschiessen` kept only on a fixture outside the Gruppenphase whose two goal counts are equal, discarded otherwise — a level group draw discards it too                      | A shoot-out would stand against a fixture one side won on goals, or against a group draw that has no tie to break (I25)                                         |
| 1k   | Every composed display value comes off the rows the payload's ids name (I3), which is why normalisation runs after the refusals and not before                                     | A save would store whatever names the client held; composing first would leave no row for exactly the ids `REQ-ELIGIBILITY-002` and `REQ-BOOKING-001` turn away |
| 1l   | `judge` hands back every reference step 1g judged and every claim step 1h judged, for step 5d to anchor (`find_references_to_anchor`)                                              | A retirement, or a rival save at that row, reads fixtures and writes none of this one's, so both would commit unseen (I53)                                      |
| 2    | Payload written wholesale with `$set`, the return unread. The keys are `FLPatchSpielDataPayload`'s plus `ergebnis`, and each object under one is the COMPOSED one                  | A field absent from the payload is **overwritten**, not preserved; keys taken from the normalised fixture instead would put `saison_id` in the `$set`           |
| 3    | A miss RAISES rather than returning, and the raise takes the transaction's earlier writes back with it (I2)                                                                        | A missing match would be reported as a successful edit                                                                                                          |
| 3a   | Every side a Spieltag clash releases is emptied, **before** step 5, and named in `released_sides` (I29, I30)                                                                       | A slot the release opens would stay empty until the next save, and the preview would name a different set than the save moved                                   |
| 4    | Where a slot's `Quelle` names a group placing, `build_team_pipeline` derives the standing for the referenced groups alone; a bracket referencing none skips the read               | A group the edit just completed would read as unfinished and seed nothing; figures re-read from `spiele` would rank the preview wrong (I98)                     |
| 5    | The season's bracket is resolved and every disagreeing fixture written (I23, I24a, I25), each reported with what that write destroyed (I99)                                        | A winner would never reach the fixture whose `Quelle` names its match, and a deleted scoreline would read as an ordinary pairing change                         |
| 5a   | A resolution that would field one club twice on a Spieltag is 409 `REQ-SPIELTAG-002` before its first write, decided by `find_advancement_occupancy_refusal` (I30)                 | The resolution fills both sides itself, so step 1e never sees the pair it creates and the matchday breaks with nothing refused                                  |
| 5b   | Every fixture step 3a or step 5 names is reported in `prior_paarungen` as the slice step 1 read held it, one entry each                                                            | An undo would have to read each moved fixture back, and a value another writer moved in between would be restored from a stale read                             |
| 5c   | The fixture the request named LEADS that report, carrying the fields outside its Paarung this payload replaced (I215)                                                              | An undo of it would be built from the page's props, reverting a field another writer moved while the editor stood open                                          |
| 5d   | Every reference step 1l hands back, and every one steps 3a and 5 book again, takes an `$inc` of `bounded_writes` on its row                                                        | A retirement, the erasure or a rival save reads fixtures and writes none of this one's, so each would commit unseen (I53)                                       |

**Every anchor step 5d takes is a `patch_many_in_db`, so a save reopening booked fixtures files action-log
rows on `spielorte` and `schiedsrichter` that change nothing a reader sees, as the season anchors do (I40).**

**`dry_run=true` runs step 0, the whole of `judge`, a read-only step 3a, step 4, and a read-only step 5,
and writes nothing** (I29). It resolves the bracket over a season assembled in memory and returns the
same `FLPatchSpielDataResponse` the save does, so a preview promises what a save would do — **or the
same 409 step 5a would answer**, deliberately, so a preview cannot report a resolution the save then
refuses. No transaction: a preview that took a write lock would be paying for a question.

**Step 0 reads the `saison_id` and nothing else, and the fixture itself comes off step 1's season**
(`fl_backend/app/api/spiele/admin_router.py :: _write_spiel_data`, then
`fl_backend/app/api/spiele/services.py :: stored_in_slice`) — so the normalisation and every refusal
stand on one slice rather than on two reads that could disagree (I108). Step 5 must be scoped to one
season, because `spiel_nr` identifies a match _within_ a season, and `saison_id` is on no payload — so
the `$set` cannot have moved the fixture out from under the scope.

**More than one document, and still one transaction**, because the result and the advancement it causes
are one fact. Step 5's read takes the session, or it would see the snapshot from before step 2 and
resolve the bracket from the match as it was; step 4's takes it so the junction rows the standings
rank — each club's group and its `austritt` — are read at the same instant. **The transaction is opened
inside `_write_spiel_data` rather than by either route**, so every write it commits sits in the callback
`fl_backend/tests/core/app_source.py :: transactional_callbacks` reads and none can lose its session
unseen.

**No team document is written.** The league table is computed from the match documents by `GET /teams`
(I1), so entering a result moves the table on the next read. The frontend still invalidates the `teams`
cache tags in the same action — the data it caches changed even though no team document did.

### 1.4 Error codes and failure responses

**Every failure response body is `{error_code, trace_id}`**, a refused payload's adding `fields` —
a domain rule's code is stated at `fl_backend/app/core/domain.py :: RULES` and every other code in
[`docs/logging/error-codes.md`](../logging/error-codes.md), each entry of
`fields` is `fl_backend/app/shared/schemas/responses.py :: FLRefusedField`, the trace-id design in
[`docs/logging/spec.md`](../logging/spec.md), and every failure line and response must follow them.
The invariant the tests pin here: the code on the wire and in the log is the **exception's own**
(`fl_backend/app/core/exceptions.py :: BaseAPIException`), checked by
`fl_backend/tests/api/test_error_responses.py`. **`fl_backend/openapi.json` declares both bodies**
(I345), so a client generated from it reads the envelope rather than FastAPI's default
`HTTPValidationError`, a body this API never sends.

**Every failure an operation can answer is published at its status with the codes it carries** — a
dependency's 400, 401 and 503 (I370), a route's 404 for `DB-COMMON-001` (I369) and 409 for
`DB-COMMON-002` (I358), each rule's code (I357), `REQ-VAL-001` on the 422 of every operation
taking input and `REQ-VAL-002` on the 400 of every one taking a body — so `default` is left to the
500s, whose codes name a server fault rather than the request.

**A domain refusal answers the status its check chose by
[RFC 9110](https://www.rfc-editor.org/rfc/rfc9110#section-15.5)'s meanings**
(`fl_backend/app/core/exceptions.py :: WriteRefusal`), and its rule declares the same one
(`fl_backend/tests/core/test_domain.py :: test_every_rule_is_answered_at_the_status_it_declares`), so
the document publishes each code at the status a client meets it at:

- **409** where the refusal is about the target's current state, whoever can lift it — the caller,
  an administrator, or time — so the identical request succeeds once that state changes (ruling R573).
  The draw judges the season's stored rules and dates beneath its payload's shape, so a rule reading
  a stored value answers there under a draw twin
  (`fl_backend/app/api/saisons/services.py :: DRAW_TWINS`)
- **422** where none could, the payload contradicting itself or a fixed bound: its body is
  `FLRefusedPayloadBody`, each field the rule judged named with the rule's code as its `kind` where
  one payload shape carries the rule, and its enum sits beside `REQ-VAL-001` on the operation's 422.
  An idempotency key replayed with other details is one, as the IETF idempotency-key draft advises
- **404** where what the request names does not exist: a link's token, which is the resource's
  identifier carried in a body for secrecy, or the season entry a path names, beside the
  `DB-COMMON-001` a missing document answers
- **410** where a link is spent for good: its record was decided, or its own deadline passed and no
  write moves it, a fresh link being a new token. An application's confirmation deadline is one
  field that a re-send of any seat restarts, so every other seat's link answers again and its
  passing is a 409 (`REQ-BEWERBUNG-017`). A link's view still answers 200 with the state
  `abgelaufen`, so a page can say why before anyone presses
- **403** only where the refusal is about who the caller is — their authority, their role or their
  standing, a ban included — and never about the target's state (ruling R573): a visitor typing an
  address the ban list holds. An administrator's write naming that address is about the entry it
  writes, so it answers 409, as the ghost referee's erasure does

**A `REQ-VAL-001` names where each refusal sits, so a form marks the field at fault**
(`fl_backend/app/core/exception_handlers.py :: refused_fields_of`) — never the value, and never
pydantic's English message. Two shapes name no control, and
the form answers each with the public slice's own sentence, or the generic one where it has none
(`docs/frontend/spec.md :: I344`):

- a refusal inside a union carries pydantic's member tag in its path, which no payload key spells
- a whole-record rule carries the path of the model that holds it — empty for the body itself

**A body that could not be read answers 400 `REQ-VAL-002` with no `fields`**, whether it is not
JSON at all or not UTF-8: malformed syntax is RFC 9110's 400 rather than a refused payload, and
nothing inside it was read to be named. It has a code of its own so that no code answers at two
statuses. The router raises only 400, 404 and 405
(`fl_backend/app/core/exception_handlers.py :: ROUTING_CODES`), so a framework exception at any other
status is a server bug and answers 500 `SRV-FAIL-001`
(`fl_backend/tests/api/test_error_responses.py :: TestTheRouterAnswersInTheEnvelope`).

**A `DuplicateKeyError` maps to a 409 through a dedicated handler**: a natural-key collision on a
create is an ordinary outcome rather than a server fault.

**A request the database cannot finish inside its deadline answers `DB-FAIL-001`** (I320). **A
request that may have written answers `DB-FAIL-002` instead** (I321), one declaring it stores
nothing excepted (I327): the write may stand, and a page told it failed sends the person to repeat
it.

**The match write path raises its own 409** (`REQ-WIRING-001`) for bracket wiring the season cannot
hold — §1.3 step 1b.
Same status, different meaning: no index refused anything, the season's current state did, and the
form prevents these shapes — so a request carrying one is stale or racing another admin.

**The rest of this path's rules carry codes of their own rather than `REQ-WIRING-001`'s**: that
code's advice is "reload the page", which is right for a season that has moved and wrong for every
one of these, where the season has not moved at all.

| Code                  | Refuses                                                                                                                                                                                |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQ-WIRING-002`      | A group placing seeding a round past the one this bracket opens on (I27). Its own code because reloading repairs nothing here (§3)                                                     |
| `REQ-WIRING-003`      | A group placing naming a group this season does not run (I27). Its own code: the offer is stale, and a reload renews it (§3)                                                           |
| `REQ-ELIGIBILITY-001` | A team that LEFT the season, disqualified or withdrawn alike, on a fixture dated on or after its exit or undated. `fl_backend/app/api/spiele/schemas.py :: records_an_absence` exempts |
| `REQ-ELIGIBILITY-002` | A newly fielded team with no `saison_teams` row for the fixture's season                                                                                                               |
| `REQ-SPIELTAG-001`    | A team that would play two fixtures of one Spieltag, where the clash cannot be moved                                                                                                   |
| `REQ-SPIELTAG-002`    | A bracket resolution that would field one club twice on a Spieltag, refused before its first write                                                                                     |
| `REQ-RESULT-001`      | A side carrying goals on a played fixture, emptied rather than switched                                                                                                                |
| `REQ-STATE-002`       | Goals submitted against a `sonderereignis` that awards none — `ausgefallen` and `annulliert`                                                                                           |
| `REQ-STATE-003`       | A no-show recorded on a fixture one of whose sides is still unresolved                                                                                                                 |
| `REQ-DATE-001`        | A date outside the span of the matchday the fixture belongs to                                                                                                                         |
| `REQ-BOOKING-001`     | A venue or referee NEWLY assigned, or kept on a fixture the save makes unplayed again, that the league lacks or holds retired                                                          |
| `REQ-CLASH-001`       | A venue or referee claim the fixture did not already make, less than four hours from another fixture's claim                                                                           |

**One code per rule, never one per side.** A refusal's body is `{error_code, trace_id}` and
nothing else (L4), so the code is the only channel — and "team1 has left the season" and "team2 has
left the season" are one failure mode, which is what the code table's own rule keys on.

**A malformed id: 404 in a path, 422 in a query — a deliberate split.** The `objectid` convertor
(`fl_backend/app/core/routing.py :: by_id`) means `/spiele/not-an-id` matches no route at all and
answers the router's 404, `REQ-ROUTE-001`, in the same envelope as every other failure
(`fl_backend/app/core/exception_handlers.py :: routing_exception_handler`). Every such parameter is
published with the convertor's pattern (`fl_backend/app/main.py :: publish_path_patterns`), so a
malformed id is visibly no id the operation serves, and its 404 is the router's rather than the
operation's. A season id — a four-character string, never an ObjectId — reaches the lookup and 404s there instead. A
query parameter carries input, so malformed input fails validation like any other: `REQ-VAL-001`, 422.
No handler maps bson's `InvalidId`: `fl_backend/app/shared/schemas/custom.py :: parse_object_id` is the
one place a value becomes an ObjectId and it catches the error, so one raised anywhere else is a server
bug and answers 500 `SRV-FAIL-001`
(`fl_backend/tests/shared/test_custom.py :: test_parse_object_id_is_the_one_site_building_an_object_id_from_a_value`).

### 1.5 Environment

Declared once as a pydantic-settings model (`fl_backend/app/core/config.py :: BackendConfig`);
fields without a default are required at boot and the process refuses to start without them.

| Variable                      | Constraint                                                                                                     | Default    |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------- | ---------- |
| `API_TRUSTED_HOSTS`           | comma-separated, each a hostname or a `*` wildcard                                                             | — required |
| `API_CORS_ALLOWED_ORIGINS`    | comma-separated, each a scheme, host and port only; no `*`                                                     | — required |
| `MONGODB_URI`                 | must start `mongodb://` or `mongodb+srv://`                                                                    | — required |
| `DB_BASE_NAME`                | the characters MongoDB accepts in a database name                                                              | — required |
| `DB_SERVER_SELECTION_TIMEOUT` | int, ms, above zero and at most 60000                                                                          | `15000`    |
| `DB_MIN_CONNECTIONS`          | int, not negative and not above `DB_MAX_CONNECTIONS`                                                           | `5`        |
| `DB_MAX_CONNECTIONS`          | int, at least one                                                                                              | `100`      |
| `INTERNAL_API_KEY_*`          | `BASE` / `SYSTEM` / `ADMIN`, each a `SecretStr` of exactly 64 printable ASCII characters, none of them a space | — required |
| `SPERRLISTE_SCHLUESSEL`       | a `SecretStr` of at least 64 characters; never rotated, and the length counts characters rather than entropy   | — required |
| `LOG_LEVEL_APP`               | `DEBUG`…`CRITICAL`, case-normalised                                                                            | `INFO`     |
| `LOG_LEVEL_DB`                | same vocabulary, for pymongo                                                                                   | `WARNING`  |
| `LOG_FORMAT`                  | `json` \| `console`, case-normalised                                                                           | **`json`** |

`API_CORS_ALLOWED_ORIGINS` refuses the bare `*` that `API_TRUSTED_HOSTS` accepts, and the refusal is
deliberate: this API is reached server-side from the frontend's own origin, never from a browser at
an origin we do not already name, and `Access-Control-Allow-Origin: *` is invalid for a credentialed
request in any case. `fl_backend/tests/core/test_config.py :: TestCorsAllowedOrigins` pins it.

**The internal keys' character class is what `secrets.compare_digest` can read**
(`docs/ops/spec.md :: I11`): a key the length bound alone admits boots and then answers every
internal request 500.

**`SPERRLISTE_SCHLUESSEL` is the one variable here that can never be replaced.** Every row of
`sperrliste` holds an HMAC taken under it and no address survives to re-hash
(`fl_backend/app/api/sperrliste/services.py :: adresse_hash`), so a new value leaves every ban
standing and matching nobody, in silence — the check computes a hash no row holds and the list still
renders. The boot's length floor is therefore the last cheap moment to refuse a weak one, and it is
the digest's own width: `fl_backend/app/core/config.py :: SPERRLISTE_KEY_MIN_LENGTH`. There is no
ceiling, HMAC taking a key of any length. What losing it costs beyond this slice, and the procedure
around it, are [`../ops/runbooks.md`](../ops/runbooks.md#5-when-somebody-asks-for-their-data-or-asks-us-to-change-it)'s.

**It is a master key and no corpus is hashed under it directly.** Each purpose derives a sub-key —
one HMAC of the master under its own label — so a compromise or a mistake in one corpus cannot read
another, and a later purpose joins by choosing a label rather than by sharing a digest. The ban
list's label is `fl_backend/app/api/sperrliste/services.py :: SPERRLISTE_SCHLUESSEL_VERSION`, and
every row records it.

**`DB_MIN_CONNECTIONS` and `DB_MAX_CONNECTIONS` are judged against each other only once every field
has parsed**, that pair being a model validator rather than a field one: an operator holding a
malformed origin as well as an inverted pool is refused for the origin alone, and refused a second
time for the pool after fixing it.

**`DB_SERVER_SELECTION_TIMEOUT` holds outside a request alone** — the boot's ping and the constraint
pass. Inside one, pymongo waits out the request's remaining deadline (I320) in its place
rather than the shorter of the two the timeout specification asks for, so lowering the variable does
not make a request against an unreachable server fail sooner.

`LOG_FORMAT` defaults to the **production** format on purpose: a `.env` that omits it must not
colourise the container stream ([`docs/logging/spec.md`](../logging/spec.md)). `API_VERSION` is deliberately
not here — it is a constant of the code (`fl_backend/app/core/config.py :: API_VERSION`). A refusal
names the failing variables and never their values (`docs/ops/spec.md :: I179`), so the value to
look at is the one the log does not print.

**A name this table does not list fails the boot when the `.env` file carries it and is never read
at all from the process environment** (`fl_backend/app/core/config.py :: model_config`): only the
dotenv source hands the class an extra, and `extra="forbid"` is what stops a typo reading as an
omission. **The one it does not catch carries an empty value** — the dotenv source drops such a name
before the class judges it, so `LOG_FORMAT_=` boots on the shipped default in silence.

### 1.6 The test suite

**The backend is the source of truth and the frontend mirrors it**, so the validation rules live here
and this suite pins them. Without it a constraint could be relaxed — to make a stubborn 422 go away,
say — and nothing would notice, because a loosened rule fails no test and breaks no build.
`fl_backend/tests/README.md` navigates the tree; the contract is here.

**A constrained model is pinned three ways**: it accepts a valid payload, rejects the specific bad
value the constraint exists to stop, and **still accepts the legitimate edge cases that look like bad
values** — an empty `stadtteil`, a null `ergebnis` for an unplayed match, an integral float
`mietpreis`. What is reached only indirectly is §4.

#### The two tiers, and the marker that separates them

| Tier        | Selected by         | Needs Docker | What it costs                                    |
| ----------- | ------------------- | ------------ | ------------------------------------------------ |
| **Default** | everything unmarked | no           | The fast tier — no container and no network      |
| **`db`**    | `@pytest.mark.db`   | yes          | The containers; cold adds the `MONGO_IMAGE` pull |

**Neither tier carries a duration here, and that is deliberate**: a figure for a tier moves with the
machine as much as with the suite, and a stamped number nobody re-takes reads as current long after
it has stopped being so. **What these tiers cost inside the gate is stamped where it is re-taken
against a fixed reference**: [`docs/ops/spec.md`](../ops/spec.md) §1.6 and
`.github/gate-wall-clock.tsv`. **The gate's `db` section costs more than the tier does alone**, the
other sections running beside it, so neither figure ever stands in for the other.

`fl_backend/pyproject.toml :: addopts` deselects the marker, so a bare `pytest` runs the fast tier only.
A command-line `-m` overrides it — addopts are prepended rather than merged — so `pytest -m db` runs
exactly what the default run skips.

**A test that touches the database carries `@pytest.mark.db`.** Without it the test runs in the fast
tier, where there is no container, and passes only where a server happens to answer.
`--strict-markers` catches a misspelled marker, and `fl_backend/tests/tier.py` fails an unmarked
test as it runs, the way pytest-django blocks its database:

- **at setup**, where pytest's own fixture closure holds `:: SERVER_FIXTURES`, before a container
  starts
- **at any command it sends, in setup, call or teardown**, through the driver's documented
  `pymongo.monitoring` listener, which sees every client built in the test process once
  `fl_backend/tests/conftest.py :: pytest_configure` has registered it, however the test reached
  it: a thread the test starts is seen, a process it starts is not

**A command sent while a fixture tears down is charged to the test that fixture was built for**
(`fl_backend/tests/tier.py :: UnmarkedDatabaseUse`), because a session fixture a db test opened
finishes in the teardown of whichever test ends the session; every other command a test's phase
sends, one from a finalizer the test registers itself included, is that test's. **A run stopped
early** — `-x`, `--maxfail`, an interrupt — finishes its remaining fixtures after the last test's
teardown, outside every phase, where no command is watched; such a run has already failed.

**A client aimed where nothing answers sends no command and passes**, which is how a default-tier
test proves a refusal lands before the database (`fl_backend/tests/config.py :: UNANSWERED_URI`).

**The marker exists to keep the fast tier fast**, and a real `mongod` sits behind it because
`mongomock` cannot execute this pipeline and a check against the live database tests the data rather
than the code. Which gate scope runs which tier is [`docs/ops/spec.md`](../ops/spec.md) §1.6's.

An `*_execution.py` module runs against a real `mongod` what its structural sibling asserts about the
dict, and **neither replaces the other**: the structural test fails when a rule is **deleted**, the
executing one when a rule is present but **wrong**. A pipeline or an `update_many` that no assertion
about a dict can describe carries the executing module alone, with no sibling to add.

#### What the suite reads, and what it does not

Every field the application under test needs is passed explicitly to
`fl_backend/tests/config.py :: build_test_config`, which builds
`fl_backend/tests/config.py :: ConfigReadingNoDotenvFile` — a settings class reading no dotenv file
at all, so a machine carrying one runs the same suite CI does and a failure means the code rather
than the machine. **The process environment is still a source**, which is why a test about a default
asserts on the model's field rather than on a constructed instance.

**The server fixtures live in the root `conftest.py`**, not in `api/`, because suites under both
`api/` and `core/` want a database; each is session-scoped, so one `mongod` serves every suite that
asks for it. Which of the two a test takes — `fl_backend/tests/conftest.py :: mongo_url` or
`:: mongo_replica_set_url` — is `:: _replica_set_mongod`'s.

#### The tier distributed

**The `db` tier runs over worker processes** — `pytest -m db -n auto --dist loadfile --maxprocesses`,
the cap being `scripts/gate/verify.sh :: GATE_WIDTH_DB_PYTEST`. **A bare `pytest -m db` still runs it
serially and is the first thing to try against a failure that only appears distributed.**
**`loadfile` is a cost choice and not a correctness one**: it keeps a module-scoped corpus built once
rather than once per worker holding a slice of it, and the per-worker naming below is what would hold
a run correct under `--dist load`, which splits a file, as well.

**The two servers are the xdist controller's rather than each worker's**
(`fl_backend/tests/conftest.py :: pytest_configure_node`), and **a run selecting no db test passes
whether or not they start**, a failed start travelling to the fixtures rather than ending
collection.

**Every database the tier names is that worker's own** — `fl_backend/tests/worker.py :: worker_database`
issues the name, and `:: guard_every_database` holds the driver's own constructor to it, so a suite
hand-rolling `client[name]` cannot sidestep the rule by seeding for itself. Two base names are read
by more than one suite, which is what forced the scoping: two workers holding one name against one
server each empty the other's seeds mid-test, and the test that fails for it is somewhere else
entirely.

There are **no `__init__.py` files** — `pyproject.toml` sets `--import-mode=importlib`, which needs none
and cannot suffer same-basename collisions.

#### Conventions

- **`tests/api/` is split by concern, never one module per entity**, each module named for its
  subject and its concern — a read, a refusal, an execution, a pipeline — because an entity's
  refusals and its pipelines are proved by different fixtures, and one module per entity grows past
  what a reader can hold.
- **Fixtures are factories, not constants.** Every fixture returns a callable producing a fresh valid
  payload; a test calls it with the one field it wants to break, so each case states exactly what makes
  it invalid and no two cases can leak state through a shared mutable dict
  (`fl_backend/tests/conftest.py :: _factory`). Payloads are keyed the way MongoDB serves them — `_id`,
  not `id` — because that is the validation alias the models declare.
- **A stored document a `db` test seeds is built by `fl_backend/tests/documents.py`**, as are the
  rules block and consent record the payload fixtures take. A test passes every value it reads at the
  call, even one equal to the builder's default, so changing a value default moves no assertion; a
  null, `False` or `gruppenphase` default is instead the ordinary state a seed stands in — active, not
  withdrawn, not yet filled in — which tests rely on and depart from by passing the field. Plain
  functions rather than fixtures, because the `on_a_*` helpers and module-scoped corpora that seed
  take no fixture.
- **A test serving the app in process enters `fl_backend/tests/app_client.py :: app_client`** inside
  the coroutine that makes the request. Ruff's banned-api (TID251, `fl_backend/pyproject.toml`)
  refuses any other import of `httpx2.ASGITransport`, a construction beside the helper being a copy
  of it; a suite whose app carries routes of its own imports it under a `noqa` on that line.
- **Reject-cases are parametrised**, one `pytest.mark.parametrize` per rule, holding one value per code
  path the rule refuses on and that path's boundaries. A value reaching a branch another case already
  reaches passes and fails with that case, so it pins nothing of its own and is not added. **A value
  that is in the list because it once got through says which failure it pins**, in a `#` beside it:
  nothing else separates a regression pin from padding, and a later pass that cannot separate them cuts
  the pin.
- **Assert _which_ field failed when more than one could**, using the `assert_rejects` fixture
  (`fl_backend/tests/conftest.py :: assert_rejects`): a bare `pytest.raises(ValidationError)` passes
  whatever went wrong, so a test meant to prove one constraint stays green while an unrelated typo in
  the payload satisfies it and the constraint it names goes unenforced. Use it wherever the payload is
  hand-built rather than produced by a factory.
- **A `db` test that takes a clean database from a helper has it built once and emptied per test**,
  and a body that moves what a collection enforces — narrowing a validator, adding or dropping an
  index, creating a view or a time-series collection — says so where it seeds, `mutates_schema=True`.
  Forgetting is caught rather than remembered, by `fl_backend/tests/database.py :: a_clean_database`.
- **That database carries the shipped validators and indexes**, both from
  `fl_backend/tests/database.py :: a_clean_database` and from `:: a_clean_database_sync`, so a seed
  the product could not hold fails on the insert. `constraints=False` is the opt-out and takes a
  reason at the body: what it is for is a case whose subject IS a document
  `fl_backend/app/core/constraints.py :: COLLECTION_VALIDATORS` refuses — a row stored before the
  validator that now forbids it. A seed that merely fails is repaired, never accommodated by
  widening a validator (`.claude/rules/backend.md`).
- **The suite that manipulates a schema takes a database no other test shares**, a half-applied
  schema being a state nothing records
  (`fl_backend/tests/core/test_constraints_execution.py :: on_a_database`).
- **A `db` test reading a seeded corpus is served by a fixture that seeds once instead** —
  `fl_backend/tests/api/conftest.py :: league` and its siblings. A module whose every case reads
  takes a corpus of its own the same way, at module scope.
- **A module-scoped corpus is held to being read rather than trusted to be**
  (`fl_backend/tests/api/conftest.py :: unwritten`), and a module holding one case that WRITES hands
  that case a database of its own, `:: config_for`, rather than dropping the shared corpus back to a
  seed per test.
- **The `db` corpus is documented where it is seeded**, in `fl_backend/tests/api/conftest.py`: a
  comment at each seeded club and each fixture names the one thing it is there to make observable,
  and the tests reading it derive their expected figures in their own docstrings.
- **An invariant's `Enforced by` names the class that proves it**, and gate check `citation` resolves
  that name, so cutting a claimed class turns the gate red rather than leaving the row citing nothing.
  **That is the whole of what is mechanical**: nothing holds a claimed class to still asserting its
  row, and a class no invariant claims is cut in silence. Holding the column both ways, as
  `fl_backend/app/core/domain.py :: UNENFORCED` is held
  (`fl_backend/tests/core/test_domain.py :: test_every_unenforced_entry_is_paired_with_the_test_that_proves_it`),
  needs a population with an edge: one dedicated file gives that pair one, and a column citing the
  whole tests tree has none.

### 1.7 Tier rules

A `REQ-*` rule raised under `app/api/` refuses a write at the status its check chose, so it has a row
in `fl_backend/app/core/domain.py :: RULES` and is published at that status on its operations in
`fl_backend/openapi.json`, while the protocol codes set apart from it take theirs in
[`docs/logging/error-codes.md`](../logging/error-codes.md). A `READ-*` rule refuses nothing.
It decides which tier a field reaches, and the enforcement is the shape of a response model or the guard on
a router — neither of which returns a `WriteRefusal`. They are stated here because they belong to the
contract rather than to the failure taxonomy.

The table carries a second kind. A `WRITE-*` rule states a crossing a write path makes on purpose:
one field arriving under a second rule's tier, which neither a response model nor a guard records,
each describing a single read.

The base key IS the public/admin distinction, whatever the network topology: a base-tier
response is what an anonymous visitor is shown. The system key is a third tier, held by the
frontend process alone: the retention sweep's responses carry raw confirmation tokens and
mailboxes, which reach no other tier (`READ-BEWERBUNG-004`). Three mechanisms carry these rules, and the difference matters when adding one —
a **guard** decides who may make the read at all, a **response model** decides what that read serves,
and a **projection** decides what leaves the database, which is the only one of the three a widening
does not surface anywhere on its own — so a projection is pinned by a case naming every path it
resolves, as `I185`'s two are, or the widening ships unseen.

| Code                     | What the base tier is not served, or what a write moves into its reach                                                                                             | Enforced by                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `READ-PUPIL-001`         | A pupil's surname, which reaches it as a forename plus an initial                                                                                                  | `fl_backend/app/api/spieler/services.py :: PUBLIC_NACHNAME` for the list and `:: public_initial` for the single read                                                                                                                                                                                                                                                                          |
| `READ-PUPIL-002`         | `saison_spieler.stufe`, which stays stored, admin-visible and validator-enforced, and every other squad-row field the public table does not render                 | `fl_backend/app/api/spieler/schemas.py :: FLSpielerPublic` and `fl_backend/app/api/spieler/services.py :: build_spieler_pipeline`'s `$project`, the same allow-list one layer down                                                                                                                                                                                                            |
| `READ-PUPIL-003`         | A pupil's name entirely, except where the consent record is present, `kader_oeffentlich` and stamped; the nameless slot keeps its `nummer` and `position`          | `fl_backend/app/api/spieler/services.py :: name_is_public`, spelled as `:: PUBLIC_PERSON`'s `$cond` in `:: build_spieler_pipeline` and called through `:: public_person` for the single read; `fl_backend/tests/api/test_name_is_public_mirror.py` holds the two equal                                                                                                                        |
| `READ-PUPIL-004`         | A pupil's stored sign-in address, which reaches no base-tier read at all                                                                                           | `fl_backend/app/api/spieler/schemas.py :: FLSpielerPublic`'s allow-list and `fl_backend/app/api/spieler/services.py :: build_spieler_pipeline`'s `$project`; `fl_backend/tests/api/test_spieler_public_read.py :: WITHHELD_FIELDS`                                                                                                                                                            |
| `READ-REFEREE-002`       | Everything about a referee's confirmation link but its state, deadline, age floor, media age, wording's version and their first name                               | `fl_backend/app/api/schiedsrichter/schemas.py :: FLSchiedsrichterBestaetigungAnsichtResponse`, declared from nothing, over `fl_backend/app/api/schiedsrichter/services.py :: BESTAETIGUNG_ANSICHT_FIELDS`; `fl_backend/tests/api/test_schiedsrichter_bestaetigung_refusal.py :: TestWhatALeakedLinkLearns`, whose `test_the_view_declares_exactly_the_six_names_it_may_answer` is an equality |
| `READ-REFEREE-001`       | A referee's name past the first space, reaching it as the first word plus one initial — a particle or a double surname reduces whole                               | `fl_backend/app/api/spiele/schemas.py :: public_referee_name`, applied by `:: FLSpielSchiedsrichterFieldPublic`, which `:: FLSpielSchiedsrichterField` does not extend, so a save writes no initial back                                                                                                                                                                                      |
| `READ-CONTACT-001`       | A referee's `kontakt` and `schule` — a referee is a pupil — an application's contact records, and whom an address holds a seat for                                 | `fl_backend/app/api/schiedsrichter/router.py`, `fl_backend/app/api/bewerbungen/router.py` and `fl_backend/app/api/kontakte/admin_router.py` guarded `verify_access_admin` whole, `fl_backend/app/api/identitaet/router.py` guarded `verify_access_system`; one seat's own fields alone reach the base tier (`READ-BEWERBUNG-002`)                                                             |
| `READ-BEWERBUNG-001`     | Which club holds a taken Kürzel, whether it left the league, which wears an assigned colour, and every club or season field beyond the page's                      | `fl_backend/app/api/bewerbungen/public_router.py`, whose reads answer `fl_backend/app/api/bewerbungen/schemas.py :: FLBewerbungSchuleOption`, `:: FLBewerbungTrikotFarbenResponse`, `:: FLBewerbungKuerzelResponse` and `:: FLPostBewerbungResponse` (I47)                                                                                                                                    |
| `READ-BEWERBUNG-002`     | Everything about a confirmation link's seat but its state, school, season, every role its holder answers for, their first name and wording's version               | `fl_backend/app/api/bewerbungen/schemas.py :: FLBewerbungEinwilligungAnsichtResponse`, declared from nothing, over `fl_backend/app/api/bewerbungen/services.py :: EINWILLIGUNG_ANSICHT_FIELDS`; `fl_backend/tests/api/test_bewerbung_einwilligung_execution.py :: TestWhatALinkOpens` and `:: TestWhatAnAnonymousReadLoads`                                                                   |
| `READ-BEWERBUNG-003`     | Every contact address but the Ansprechperson seat's, served to the frontend server alone so it can address two messages, and withheld from the browser             | `fl_backend/app/api/bewerbungen/schemas.py :: FLBewerbungEinwilligungAntwortResponse`; `fl_backend/tests/api/test_bewerbung_einwilligung_execution.py :: TestWhatTheAnswerHandsTheMailer`; `fl_frontend/src/app/api/bestaetigung/kontakt/route.test.ts :: files the answer under the label this server renders, echoing what was stored`                                                      |
| `READ-REGISTRIERUNG-001` | Nothing past a link's state, team, school, season, forename, wording label, age floor and media age, and the pupil's stored birthdate, publication scope and media | `fl_backend/app/api/registrierungen/schemas.py :: FLRegistrierungBestaetigungAnsichtResponse`, answered on the base tier alone; `fl_backend/tests/api/test_registrierung_einwilligung_refusal.py :: TestTheLookupAndItsProjections`, whose field-set case is an EQUALITY                                                                                                                      |
| `READ-EINLADUNG-001`     | Nothing past the fields the invite's own read declares, none of them anybody's personal data                                                                       | `fl_backend/app/api/registrierungen/schemas.py :: FLEinladungAnsichtResponse`, declared from nothing and answered on the base tier alone; `fl_backend/tests/api/test_registrierung_submission_execution.py :: TestWhatTheInvitesOwnReadAnswers`                                                                                                                                               |
| `READ-BEWERBUNG-004`     | Nothing past a token, a first name, a role, a school, a season, a deadline, a mailbox, the application id and its notice flag                                      | `fl_backend/app/api/bewerbungen/schemas.py :: FLBewerbungSweepErinnerung` and `:: FLBewerbungSweepLoeschung`, declared from nothing and served on the system tier alone; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestTheFourteenDayClock`                                                                                                                                  |
| `WRITE-CLUB-001`         | An accepted application's school address becomes the club's, so an address behind `READ-CONTACT-001` is served under `READ-ADDRESS-002`                            | `fl_backend/app/api/bewerbungen/services.py :: compose_new_club`, applied by `fl_backend/app/api/bewerbungen/admin_router.py :: annehmen_bewerbung`                                                                                                                                                                                                                                           |
| `READ-MONEY-001`         | `default_mietpreis`, `default_payment`, and a fixture's own `ort.mietpreis` and `schiedsrichter.payment`                                                           | The two reference routers' guards, and `fl_backend/app/api/spiele/schemas.py :: FLSpielOrtFieldPublic` / `:: FLSpielSchiedsrichterFieldPublic`, which `:: FLSpielJoinedAdmin` replaces with the shapes carrying the figures                                                                                                                                                                   |
| `READ-ADDRESS-001`       | Nothing — a venue's address IS public, through `maps_link`, and is declared so rather than withheld by one route and published by another                          | `fl_backend/app/api/spielorte/admin_router.py :: _maps_link` composes the postal address into that string, and `fl_backend/app/api/spiele/schemas.py :: FLSpielOrtFieldPublic` serves it on every fixture                                                                                                                                                                                     |
| `READ-ADDRESS-002`       | Nothing — a club's address IS public: it is a school's street, and it stays served rather than narrowed the way the standings row was                              | `fl_backend/app/api/teams/schemas.py :: FLTeam` inherits `address` from `:: _TeamWritable` and serves it on both team reads; `:: FLGruppenTeam` carries none, bounding the model not the rule                                                                                                                                                                                                 |
| `READ-SQUAD-001`         | Nothing — a squad is filtered by the ROW's retirement, so a retired person keeps the rows they played                                                              | `fl_backend/app/api/spieler/services.py :: build_spieler_pipeline`, whose only `inactive_since` match sits inside the `$lookup`; the retire dialog's promise in `fl_frontend/src/features/spieler/components/modals/AdminDeleteSpielerModal.tsx :: AdminDeleteSpielerModal` rests on it                                                                                                       |
| `READ-SQUAD-002`         | Retired squad rows, which it cannot un-hide at all                                                                                                                 | The base tier declares no `include_inactive`; the rule lives on `fl_backend/app/api/spieler/schemas.py :: FLSpielerMembership`, whose every row carries its own `inactive_since`                                                                                                                                                                                                              |
| `READ-FREETEXT-001`      | Nothing — a fixture's `notiz` IS served, by decision. A name typed into it sits beyond an erasure (`I42`); only an admin takes it back                             | `fl_backend/app/api/spiele/schemas.py :: FLSpielCommon` declares the field and `:: FLSpielJoined` serves it (`I32`); `fl_backend/tests/api/test_spiele_public_read.py :: TestTheFixtureShapes` pins the field set, and `fl_frontend/src/features/spiele/components/forms/AdminEditSpielDataForm/FormNotizSection.tsx` warns the admin                                                         |
| `READ-FREETEXT-002`      | Nothing on a club's own page — an `austritt`'s `grund` IS served there, by decision. A name in it sits beyond an erasure (`I42`, `I32`)                            | `fl_backend/app/api/teams/schemas.py :: FLAustritt` on `:: FLTeam`, which `fl_backend/app/api/teams/services.py :: build_team_pipeline` allows through and `fl_backend/tests/api/test_spiele_public_read.py :: TestThePageThatPublishesTheWithdrawal` pins, and `fl_frontend/src/features/teams/components/forms/AdminTeamEditForm/FormAustrittSection.tsx` warns the admin                   |
| `READ-FREETEXT-003`      | Nothing — a club's `description` IS served on both team reads, by decision. A name typed into it sits beyond an erasure (`I42`)                                    | `fl_backend/app/api/teams/schemas.py :: FLTeam` inherits it from `:: _TeamWritable`; `fl_frontend/src/features/teams/components/ui/TeamIdentityCard.tsx` renders it and `fl_frontend/src/features/teams/components/modals/DescriptionEditModal.tsx` warns the admin                                                                                                                           |

**A withheld field is absent, never nulled.** A response whose shape follows the caller's key is one no Zod
mirror can express, so a tier served less is served by a different endpoint with its own `response_model`.

**A filter is part of the shape.** A published query parameter narrowing on a withheld field rebuilds it from
the result sets, one request per value, so the parameter leaves the base tier with the field.

**A read answering a yes-or-no question is a shape like any other.** The Kürzel check serves one boolean and
never the row behind it: "taken by the Carl-Schurz-Schule" and "taken by a club that has left the league" are
each a fact about a third party the applicant is owed no answer about, and `fl_backend/app/core/constraints.py :: uniq_shorthand`
keeps indexing a club that has left — so the two cases are indistinguishable to the caller by construction
rather than by the handler remembering to conceal one.

## 2. Invariants

| #    | Invariant                                                                                                                                                                                                                                                                       | Enforced by                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I1   | Team statistics are **derived from `spiele`**, never stored                                                                                                                                                                                                                     | `fl_backend/app/api/teams/services.py :: build_statistik_lookup_stage` and `:: build_statistik_by_team`                                                                                                                                                                                                                                                                                                                                                                                                                      |
| I1a  | A match counts towards the table **exactly when it carries an `ergebnis`**; `sonderereignis` reaches none of the figures the table is scored and sorted on                                                                                                                      | `_counting_fixtures_match`, which names `ergebnis` and the two goal counts and nothing else; `fl_backend/app/api/teams/services.py :: _counted_goals` restates it for the in-process derivation                                                                                                                                                                                                                                                                                                                              |
| I1b  | Points come from the season's `rules.win_points` / `draw_points`, in both derivations                                                                                                                                                                                           | The final `$project` inside that lookup, and `fl_backend/app/api/teams/services.py :: build_statistik_by_team`'s arithmetic beside it                                                                                                                                                                                                                                                                                                                                                                                        |
| I1c  | `statistik_scope` defaults to **`gruppenphase`**, so the table a caller gets by saying nothing is the group standing                                                                                                                                                            | The field default on `FLTeamsFilterParams`, read by that same `$match`                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| I1d  | `anzahl_gespielte_spiele` and `anzahl_abgesagte_spiele` are not a partition — a forfeit is counted by both, and only the cancellation figure reads `sonderereignis`                                                                                                             | `fl_backend/app/api/teams/services.py :: build_absage_lookup_stage`, whose `$match` is `SONDEREREIGNIS_COUNTED_AS_ABSAGE` and nothing else; `fl_backend/tests/api/test_teams_pipeline.py :: TestTheAbsageLookup` pins the membership in literals rather than against the absence set                                                                                                                                                                                                                                         |
| I1e  | The two derivations of a team's figures — the `$lookup` pipeline and the in-process pass — **agree over the same fixtures**                                                                                                                                                     | `fl_backend/tests/api/test_standings_execution.py :: TestTheTwoDerivationsAgree`, parametrised over both scopes                                                                                                                                                                                                                                                                                                                                                                                                              |
| I2   | `patch_one_in_db` returns the POST-write document and raises on a miss                                                                                                                                                                                                          | `fl_backend/tests/core/test_crud.py :: TestPatchOneInDb`, which calls the helper against a fake collection                                                                                                                                                                                                                                                                                                                                                                                                                   |
| I3   | Every display value a fixture stores is COMPOSED server-side and rides on no payload; `mietpreis` and `payment` are the exception (I6)                                                                                                                                          | `fl_backend/app/api/spiele/schemas.py :: FLPatchSpielDataPayload`, `fl_backend/app/api/spiele/services.py :: apply_payload_to_spiel` and `fl_backend/app/core/domain.py :: FIELD_POLICIES`, which declares each key `COMPOSED` beside the callable that writes it                                                                                                                                                                                                                                                            |
| I3a  | A NO-SHOW is composed, never typed: the awarded goals come from the season's `rules.forfeit_ergebnis`, and the side that stayed away takes the loser's                                                                                                                          | `fl_backend/app/api/spiele/services.py :: apply_payload_to_spiel`, which takes `rules` as a value from the router                                                                                                                                                                                                                                                                                                                                                                                                            |
| I4   | Omitting `saison_id` means the current season on every `/spiele`, `/teams` and `/spieltage` read taking one, and on a `/spieler` read naming a `team_id`                                                                                                                        | Each router's handler resolves it; `fl_backend/tests/api/test_spiele_action_required_scope.py :: TestNamingNoSeasonScopesToTheActiveOne` holds `/spiele/action_required` to it, and `fl_backend/tests/api/test_spieler_public_read.py :: TestTheSeasonASquadIsReadFor` `/spieler`                                                                                                                                                                                                                                            |
| I5   | `saison_id` is 4 characters on every model ACCEPTING one as data; a response echoing a stored id is not constrained, nor is a LIST FILTER                                                                                                                                       | `fl_backend/app/shared/schemas/bounds.py :: SAISON_ID_LENGTH` on each accepting model, swept by `fl_backend/tests/api/test_saison_id_bounds.py`, whose `:: test_a_membership_model_still_accepts_a_stored_id_a_payload_would_refuse` pins the asymmetry                                                                                                                                                                                                                                                                      |
| I6   | Money fields (`mietpreis`, `payment`) have **no** Pydantic default and stay on the write payload (I3): each records what THIS fixture agreed, never a default                                                                                                                   | `fl_backend/app/api/spiele/schemas.py :: FLSpielOrtFieldPayload`, `:: FLSpielSchiedsrichterFieldPayload`, and the stored `:: FLSpielOrtField` and `:: FLSpielSchiedsrichterField`, which declare the figure again, so the rule binds each independently                                                                                                                                                                                                                                                                      |
| I7   | Router-level guards and **one tier per router**; `system` is the one slice carrying none, and `/system/is_live` the one path the edge carries ([`docs/ops/spec.md`](../ops/spec.md) I13)                                                                                        | `dependencies=[Depends(...)]` on each `APIRouter`; `fl_backend/tests/api/test_admin_guard.py`, whose `:: UNGUARDED_BY_DESIGN` and `:: PUBLIC_WRITES` are the whole of the exception. The edge half is [`docs/ops/spec.md`](../ops/spec.md) I13's                                                                                                                                                                                                                                                                             |
| I8   | API keys compared with `secrets.compare_digest`                                                                                                                                                                                                                                 | `fl_backend/app/core/security.py :: verify_api_key`                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| I9   | The app refuses to start if MongoDB is unreachable                                                                                                                                                                                                                              | `fl_backend/app/core/db.py :: lifespan`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| I10  | The teams grouped response carries every group the season OFFERS: one holding nobody keeps its key, one it does not run has none                                                                                                                                                | `fl_backend/app/api/teams/services.py :: build_gruppen`, seeded from `:: offered_gruppen`; `fl_backend/tests/api/test_teams.py :: TestFLGruppen`                                                                                                                                                                                                                                                                                                                                                                             |
| I11  | With a `saison_id`, the `saison_teams` join is strict: a club with no row has no name to serve, not a row filtered out                                                                                                                                                          | `preserveNullAndEmptyArrays: not strict_join` in `fl_backend/app/api/teams/services.py :: build_team_pipeline`                                                                                                                                                                                                                                                                                                                                                                                                               |
| I12  | Deletion is soft wherever `inactive_since` is stamped, and that field is a **date, never a boolean**. The one hard delete is a pupil's ERASURE (`REQ-PURGE-001`)                                                                                                                | Every `delete_*` handler stamps the German current date; the field is on no payload                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| I13  | A venue, referee or club rename fans out in ONE transaction with the write and reports its count; a club's stops before a `past` season                                                                                                                                         | `fl_backend/tests/api/test_reference_fanout_execution.py`; `fl_backend/app/api/teams/admin_router.py :: patch_team`; `fl_backend/app/api/teams/schemas.py :: FLPatchTeamResponse`, which carries both counts                                                                                                                                                                                                                                                                                                                 |
| I146 | The referee anonymisation reaches every `spiele` row holding the person in ONE transaction with the row's write, answering no count where I13's renames do                                                                                                                      | `fl_backend/app/api/schiedsrichter/admin_router.py :: anonymise_schiedsrichter`; `fl_backend/tests/api/test_schiedsrichter_anonymisierung.py`, whose fan-out cases drive it against a replica set                                                                                                                                                                                                                                                                                                                            |
| I14  | `GET /teams` requires a resolved season, and `build_team_pipeline` raises without one                                                                                                                                                                                           | The guard at the top of the builder                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| I15  | Every collection carries a `$jsonSchema` validator and every uniqueness rule a unique index, both applied on **every boot**; startup fails if any cannot be                                                                                                                     | `fl_backend/app/core/constraints.py`, called from the lifespan                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| I16  | Those validators assert **types, required fields and enums only** — never ranges, patterns or lengths                                                                                                                                                                           | `test_no_validator_constrains_a_range_or_a_format`                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| I18  | **At most one season is `active`, and exactly one from the league's first activation on**; only `POST /saisons/{saison_id}/activate` writes `status`, `REQ-ACTIVATE-001` through `-004` refusing it                                                                             | `fl_backend/app/api/saisons/services.py :: find_activation_refusal` decides all four; at most one by `fl_backend/app/core/constraints.py :: uniq_saison_active`; the one writer by `fl_backend/tests/api/test_activation_writes.py`; `fl_backend/tests/api/test_activation_refusal.py`, `fl_backend/tests/api/test_activation_execution.py` and `fl_backend/tests/api/test_activation_isolation.py`                                                                                                                          |
| I19  | The junction has no DELETE, so a row is never REMOVED: a club leaves by an `austritt` or a REPLACEMENT, the only write changing `team_id`                                                                                                                                       | No DELETE is declared on `/teams/{team_id}/saisons/{saison_id}`                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| I20  | Creating **never revives** a retired row; `POST /{resource}/{id}/reactivate` does                                                                                                                                                                                               | Every create is a plain insert, so a natural-key collision is a `DuplicateKeyError` → 409                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| I17  | A Pydantic model and its validator declare the same field set                                                                                                                                                                                                                   | `fl_backend/tests/core/test_constraints.py :: test_every_mirrored_model_matches_its_validator`                                                                                                                                                                                                                                                                                                                                                                                                                               |
| I140 | A mirrored model's every field declares, in its validator, the bsonType its annotation stores as — a second guarantee beside I17's field set                                                                                                                                    | `fl_backend/tests/core/test_constraints.py :: test_every_mirrored_field_declares_the_bson_type_of_its_annotation` and `:: BSON_TYPES`                                                                                                                                                                                                                                                                                                                                                                                        |
| I141 | A seat carrying `bestaetigt_am` carries a `geburtsdatum`: the date is entered at the confirmation, and the `$jsonSchema` cannot state the pairing                                                                                                                               | `fl_backend/app/api/bewerbungen/services.py :: compose_confirmation_update`, one `$set` writing both, and `fl_backend/app/api/teams/services.py :: compose_kontakte_herkunft`, which carries the pair over an edit; `fl_backend/tests/api/test_bewerbung_einwilligung_execution.py :: TestWhatAConfirmationWrites` and `fl_backend/tests/api/test_saison_team_kontakte.py :: TestTheDateRidesWithThePerson`                                                                                                                  |
| I142 | `erfasst_von`, `bestaetigt_am` and a `geburtsdatum` are composed by the server on every write path and accepted from no payload: `person` is a confirmation's alone                                                                                                             | `fl_backend/app/api/bewerbungen/services.py :: compose_einwilligung` and `fl_backend/app/api/teams/services.py :: compose_kontakte_herkunft` compose; `fl_backend/app/api/teams/schemas.py :: FLKontaktKenntnisnahmePayload`, `:: FLKontaktpersonPayload` and `fl_backend/app/api/bewerbungen/schemas.py :: FLBewerbungEinwilligungPayload` refuse; `fl_backend/tests/api/test_saison_team_kontakte.py :: TestTheProvenanceIsTheServers` and `:: TestWhatThePayloadRefuses`                                                  |
| I143 | An application carrying `bestaetigungen` is accepted only once every seat carries its person's stamp; one without the block is not held to it                                                                                                                                   | `fl_backend/app/api/bewerbungen/services.py :: find_unconfirmed_kontakte_refusal`, inside `fl_backend/app/api/bewerbungen/admin_router.py :: annehmen_bewerbung`'s transaction; `fl_backend/tests/api/test_bewerbung_triage_refusal.py :: TestEverySeatIsConfirmedBeforeAcceptance` and `fl_backend/tests/api/test_bewerbung_triage_execution.py :: TestAcceptanceWaitsForEverySeat`                                                                                                                                         |
| I144 | A raw confirmation token exists only in the mint's response: no read model declares `token_hash` or `token_hash_zuvor`, and both admin reads exclude them                                                                                                                       | `fl_backend/app/api/bewerbungen/services.py :: hash_token` and `:: WITHOUT_TOKEN_HASHES`; `fl_backend/tests/api/test_bewerbung_einwilligung_execution.py :: TestNoHashReachesAnAdminRead` and `fl_backend/tests/api/test_bewerbung_submission_execution.py :: TestWhatTheCreateMints`                                                                                                                                                                                                                                        |
| I145 | A confirmation link is spent by the seat's stamp or its `Widerspruch`, never by nulling a hash, so a reopened link shows its own state                                                                                                                                          | `fl_backend/app/api/bewerbungen/services.py :: find_already_answered_refusal` and `:: zustand_of`; `fl_backend/tests/api/test_bewerbung_einwilligung_execution.py :: TestTheLinkIsSpentByTheStamp`                                                                                                                                                                                                                                                                                                                           |
| I150 | A retention removal names ONE season and its ids, runs `erase_many_from_db` inside a transaction, and redacts the log rows naming what it removed                                                                                                                               | `fl_backend/app/api/bewerbungen/sweep_router.py`, every removal a literal `{"saison_id", "_id": {"$in"}}` filter; `fl_backend/tests/core/test_write_shapes.py :: TestWhatARemovalFilterMayName`; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestTheFourteenDayClock`, `:: TestTheOneMonthClock`, `:: TestTheSeasonAndOneClock`                                                                                                                                                                               |
| I151 | The fourteen-day deletion mails first and erases last: the sweep lists candidates, the caller notifies and stamps, a third call erases what still qualifies                                                                                                                     | `fl_backend/app/api/bewerbungen/sweep_router.py :: sweep_saison` writes nothing for a candidate; `:: loeschen_bewerbungen` re-judges each id in-session; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestTheFourteenDayClock`                                                                                                                                                                                                                                                                                 |
| I152 | A reminder stamps `erinnert_am` and mints a fresh link before the caller mails, keeps the first hash valid, and never moves the deadline                                                                                                                                        | `fl_backend/app/api/bewerbungen/services.py :: compose_erinnerung_update` and `:: build_token_filter`; `fl_backend/tests/api/test_bewerbung_sweep_refusal.py :: TestWhatAReminderWrites`; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestTheReminderClock`                                                                                                                                                                                                                                                   |
| I153 | A declined application is erased one calendar month after the decision was taken, the day clamping to the month's end where none matches                                                                                                                                        | `fl_backend/app/api/bewerbungen/services.py :: decline_erasure_is_due` and `:: one_month_after`; `fl_backend/tests/api/test_bewerbung_sweep_refusal.py :: TestOneMonth`; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestTheOneMonthClock`                                                                                                                                                                                                                                                                    |
| I154 | An accepted application and the season's contact blocks go together, once the season after the one applied for is `past`                                                                                                                                                        | `fl_backend/app/api/bewerbungen/services.py :: acceptance_erasure_is_due` and `:: season_after_has_ended`, read off `saisons.status`; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestTheSeasonAndOneClock`                                                                                                                                                                                                                                                                                                   |
| I155 | Clearing a season's contact blocks empties every log row naming those junction rows, images of writes that touched no contact included                                                                                                                                          | `fl_backend/app/api/bewerbungen/sweep_router.py :: _redact` over `fl_backend/app/core/recording.py :: build_redaction_filter`, which keys on ids alone; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: test_the_application_goes_and_the_contact_block_empties_once_the_next_season_is_past`                                                                                                                                                                                                                     |
| I156 | An application is erased only once its deletion notice is stamped, and the stamp keeps the day the notice actually went out                                                                                                                                                     | `fl_backend/app/api/bewerbungen/services.py :: deletion_was_announced`, read by `fl_backend/app/api/bewerbungen/sweep_router.py :: loeschen_bewerbungen`; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: test_a_failed_erasure_does_not_send_the_notice_a_second_time`                                                                                                                                                                                                                                           |
| I157 | A reminder sends one mailbox one link per person: a mirrored Trainer rides the link of the seat it mirrors, and both seats are stamped                                                                                                                                          | `fl_backend/app/api/bewerbungen/services.py :: reminder_link_groups`, folded as `fl_frontend/src/features/bewerbungen/notifications.ts :: seatsByMailbox` folds the first mail; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestTheReminderClock`; `fl_frontend/src/features/bewerbungen/sweep.test.ts`                                                                                                                                                                                                       |
| I158 | `rules.erlaubte_stufen` is the net a short squad may be topped up from, never the Jahrgang a team comes from; narrowing it removes that headroom                                                                                                                                | Refused on a pupil's own registration (`REQ-REGISTRIERUNG-003`); unenforced on an administrator's squad write, where `fl_backend/tests/api/test_rules_refusal.py :: test_permits_narrowing_erlaubte_stufen_on_a_past_season` pins the narrowing as permitted                                                                                                                                                                                                                                                                 |
| I22  | A fixture side is `null` while its occupant is unknown, and `teamN_quelle` is an INDEPENDENT sibling saying where it comes from                                                                                                                                                 | `FLSpiel` and the `spiele` validator, both of which type the four fields and pair none of them                                                                                                                                                                                                                                                                                                                                                                                                                               |
| I21  | `fl_backend/openapi.json` is committed and equals what the service publishes; regenerate it with the command `fl_backend/tests/openapi_document.py :: REGENERATE` declares                                                                                                      | `fl_backend/tests/api/test_openapi_document.py :: test_the_committed_document_is_the_one_the_service_publishes`                                                                                                                                                                                                                                                                                                                                                                                                              |
| I23  | A bracket slot's occupant IS the winner of the match its `Quelle` references, recomputed on every write; a `null` `Quelle` makes the slot the admin's                                                                                                                           | `fl_backend/app/api/spiele/services.py :: resolve_bracket`, called by `patch_spiel_data` inside its transaction; both `spiel` and `gruppe` references resolve (I24)                                                                                                                                                                                                                                                                                                                                                          |
| I24  | A group is ranked by ONE chain — points, then the goal keys and the head-to-head table among whoever is level, ordered by `tiebreak_order`                                                                                                                                      | `fl_backend/app/api/teams/services.py :: build_gruppen` orders the response and `:: build_decided_standings` seeds from the same `:: _tiers`; `:: _break_tie` decides which criterion leads. Nothing re-sorts a group                                                                                                                                                                                                                                                                                                        |
| I24a | A bracket slot takes a placing no ending of an outstanding fixture could change, a call-off included; only final goals break a tie below points                                                                                                                                 | The walk over endings in `_decide_one_gruppe`, a call-off among them, within `CERTAINTY_FIXTURE_LIMIT`; `_separated_placings` decides every group above it, soundly rather than exactly                                                                                                                                                                                                                                                                                                                                      |
| I24b | A team holds a placing when it has **not left the season** and has a match that counts or could; a departed club places nowhere                                                                                                                                                 | `fl_backend/app/api/teams/services.py :: _may_hold_a_platz`, applied in `:: _tiers` and in `:: _decide_one_gruppe`; `:: _placings` advances the number only for a club that may hold one                                                                                                                                                                                                                                                                                                                                     |
| I24c | Only states no further result can fix are reported to an admin; a placing that is simply not decided yet is reported to nobody                                                                                                                                                  | `FLPatchSpielDataResponse.bracket_faults`, populated by `resolve_bracket`                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| I25  | A level knockout is decided by `elfmeterschiessen`, a scoreline of its OWN and never a stored winner; a group draw and a no-show carry none                                                                                                                                     | `FLSpielElfmeterschiessen`, which also refuses a level shoot-out; `patch_spiel_data` discards a record the goals or the phase leave no room for; `fl_backend/app/api/spiele/services.py :: _outcome_of`                                                                                                                                                                                                                                                                                                                      |
| I25a | The league table does NOT consult `elfmeterschiessen`: a shoot-out is a draw for every derived figure, so bracket and table disagree on purpose                                                                                                                                 | Its absence from `build_statistik_lookup_stage`'s `$match` and from `fl_backend/app/api/teams/services.py :: _counted_goals`, which the standings and the head-to-head mini-table both use                                                                                                                                                                                                                                                                                                                                   |
| I25b | `elfmeterschiessen`, `ergebnis` and a `nichtantreten_*` go with the occupant: a fixture whose sides change loses all three, while an event naming no side survives                                                                                                              | The `$set` in `fl_backend/app/api/spiele/crud.py :: advance_bracket_winners`, which clears all three                                                                                                                                                                                                                                                                                                                                                                                                                         |
| I26  | `/spiele` has no POST and no DELETE, so no request creates or removes ONE fixture; a season's fixtures are written and removed as a SET                                                                                                                                         | Neither verb is declared on `fl_backend/app/api/spiele/admin_router.py`; `fl_backend/app/api/saisons/services.py :: find_spielplan_refusal` decides the draw and the replace, `:: find_undraw_refusal` the undraw                                                                                                                                                                                                                                                                                                            |
| I27  | The match write path refuses with a 409 wiring the season cannot hold: `REQ-WIRING-001`, `REQ-WIRING-002` and `REQ-WIRING-003`, kept apart by their repairs (§1.4)                                                                                                              | `fl_backend/app/api/spiele/services.py :: find_wiring_refusal`, called in the transaction before the write (§1.3 step 1b); `fl_backend/tests/api/test_wiring_refusal.py` pins each rule and the legal edit beside it                                                                                                                                                                                                                                                                                                         |
| I29  | One pure function normalises a match payload, and the SAVE and the `dry_run=true` PREVIEW both apply it                                                                                                                                                                         | `fl_backend/app/api/spiele/services.py :: apply_payload_to_spiel`, which the handler restates none of; `fl_backend/tests/api/test_occupant_refusal.py :: TestApplyingThePayload`                                                                                                                                                                                                                                                                                                                                             |
| I30  | A team is fielded at most once per Spieltag; a clash MOVES a manual side and REFUSES against a `quelle`-maintained one                                                                                                                                                          | `fl_backend/app/api/spiele/services.py :: judge_spieltag_occupancy` in `judge` before the write; the group swap refuses with `REQ-SWAP-005` (I38) and the bracket resolution with `REQ-SPIELTAG-002` (§1.3 step 5a)                                                                                                                                                                                                                                                                                                          |
| I28  | Every derived bracket fault is reported and none is stored; reporting one never resolves it, and I23's containment is untouched                                                                                                                                                 | `fl_backend/app/api/spiele/services.py :: resolve_bracket`, `:: find_gruppen_not_run`, `:: find_departed_occupants`, `:: find_double_entries`, `:: find_retired_bookings`, `:: find_double_bookings` reach `fl_backend/app/api/spiele/crud.py :: find_bracket_faults`; `fl_backend/tests/api/test_bracket.py :: TestReportingAFault`, `fl_backend/tests/api/test_slice_completeness.py :: TestTheFaultSweepReportsWhatItsSweepsFound`                                                                                        |
| I32  | Every endpoint serving matches runs the SAME aggregation, joining each side's withdrawal from `saison_teams` on **that document's own `saison_id`**                                                                                                                             | `fl_backend/app/api/spiele/services.py :: build_spiele_pipeline`, the one pipeline every read serving matches runs, `fl_backend/app/api/spiele/crud.py :: find_bracket_faults` included; `fl_backend/tests/api/test_spiele_pipeline_execution.py`                                                                                                                                                                                                                                                                            |
| I31  | Leaving a season is a RECORD on `saison_teams`, its ABSENCE the null; no boolean records it, and `type` is a closed set of two                                                                                                                                                  | `fl_backend/app/api/teams/schemas.py :: FLAustritt` and the `saison_teams` validator, which types the field object-or-null and requires all three keys of a present one                                                                                                                                                                                                                                                                                                                                                      |
| I33  | The admin player list reads `GET /spieler/memberships`, PEOPLE carrying their squad rows; `GET /spieler` answers it at no filter setting, and redacts besides (`READ-PUPIL-001`, `READ-PUPIL-002`)                                                                              | `fl_backend/app/api/spieler/services.py :: build_spieler_memberships_pipeline`; `fl_backend/app/api/spieler/schemas.py :: FLSpielerPublic` is the allow-list `GET /spieler` answers with; `fl_backend/tests/api/test_spieler_memberships_read.py`                                                                                                                                                                                                                                                                            |
| I34  | The two spieler PATCH payloads replace their target WHOLESALE, so **no field on either carries a default**, as `FLPatchSaisonTeamPayload` carries none on the other junction                                                                                                    | `FLPatchSpielerPayload` and `FLPatchSaisonSpielerPayload`, whose handlers `$set` the model dump directly; the frontend mirror is checked field by field against the published document by `fl_frontend/src/core/apiContract.test.ts`                                                                                                                                                                                                                                                                                         |
| I36  | A PERSON's name is letters plus space, hyphen and apostrophe — on the WRITE payloads only, never on a read model                                                                                                                                                                | `fl_backend/app/shared/schemas/custom.py :: PERSON_NAME_PATTERN`, applied by every payload taking a person's name and by no read model; `fl_backend/tests/api/test_reference_models.py :: test_the_read_model_still_accepts_a_stored_name_the_payload_would_refuse`                                                                                                                                                                                                                                                          |
| I35  | `position`, `stufe` and `rolle` are CLOSED SETS on every model and in the `saison_spieler` validator                                                                                                                                                                            | `FLSpielerPosition`, `FLSpielerStufe` and `FLSpielerRolle`; `_POSITIONEN`, `_STUFEN` and `_SPIELER_ROLLEN` in `fl_backend/app/core/constraints.py`, held to those `Literal`s by `fl_backend/tests/core/test_constraints.py :: test_every_validator_enum_matches_its_literal`                                                                                                                                                                                                                                                 |
| I37  | A LITERAL path is declared BEFORE the parameterised one whose regex would take it, and must stay there — `GET /saisons/current` and each `/list/admin` read                                                                                                                     | `fl_backend/tests/api/test_route_order.py`, which pairs every literal path against the routes that would shadow it and asserts the `/saisons/current` pair is still among them                                                                                                                                                                                                                                                                                                                                               |
| I38  | A mid-season group change is an EXCHANGE of two clubs, moving both `saison_teams` rows AND every drawn Gruppenphase side either holds; `REQ-SWAP-001..007` refuse                                                                                                               | `fl_backend/app/api/saisons/admin_router.py :: swap_gruppen`, judging through the transaction's session; `fl_backend/app/api/teams/services.py :: has_taken_place` and `:: find_gruppe_swap_refusal`; `fl_backend/tests/api/test_gruppe_swap_execution.py`                                                                                                                                                                                                                                                                   |
| I39  | `patch_one_in_db` logs the update's own pre-image and answers the image its caller names: a wrong pre-image makes a restore revert a write it never touched                                                                                                                     | `fl_backend/app/core/crud.py :: patch_one_in_db`, whose update carries `ReturnDocument.BEFORE` whatever the caller asked for; `fl_backend/tests/core/test_crud.py :: TestPatchOneInDb`, whose `test_every_caller_names_the_image_it_wants` holds `return_document` without a default                                                                                                                                                                                                                                         |
| I40  | A write MODIFYING or CREATING a set records ONE row with the count and no pre-image, a fan-out's carrying the filter it ran too                                                                                                                                                 | `fl_backend/app/core/crud.py :: patch_many_in_db` and `:: post_many_to_db`; `fl_backend/app/core/recording.py :: _stringify_filter` stores the filter as text for a reader, never for replay                                                                                                                                                                                                                                                                                                                                 |
| I41  | `bind_actor` sits at ROUTER level beside `verify_access_admin`, refusing a missing or malformed `X-FL-Actor` with `REQ-AUTH-005` before any write; the exemption is keyed on the METHOD                                                                                         | `fl_backend/app/core/security.py :: bind_actor`, which takes the exemption from `:: SAFE_METHODS`; every `admin_router.py` declares it in the same `dependencies` list as the guard (I7)                                                                                                                                                                                                                                                                                                                                     |
| I42  | A write destroying a person's values overwrites the log images in place and stamps `redacted_at`; a null stamp means the row holds what it recorded                                                                                                                             | `fl_backend/app/core/constraints.py :: Collection.AKTIONEN`, whose validator types the field string-or-null; `fl_backend/app/core/recording.py :: record_write`, which writes it null on every row and never records the log                                                                                                                                                                                                                                                                                                 |
| I43  | A stored pre-image reaches the wire through `GET /aktionen/{aktion_id}` ALONE, served as JSON with every `ObjectId` in it rendered as text, at any depth                                                                                                                        | `fl_backend/app/api/aktionen/schemas.py :: FLAktionMitStand`, the single read's model, whose `before` validator walks the stored document; `fl_backend/tests/api/test_aktionen_read.py :: TestARecordedRowSurvivesTheResponseModel`                                                                                                                                                                                                                                                                                          |
| I44  | A refusal comparing a payload against the stored document fires on the STEP introducing or worsening a violation, never where the rule's inputs are unchanged                                                                                                                   | The comparisons against `stored` in `fl_backend/app/api/saisons/services.py :: find_rules_refusal`, `fl_backend/app/api/spieltage/services.py :: find_spieltag_order_refusal` and `fl_backend/app/api/spiele/services.py :: find_wiring_refusal`; `fl_backend/tests/api/test_rules_refusal.py`, `fl_backend/tests/api/test_spieltag_refusals.py` and `fl_backend/tests/api/test_wiring_refusal.py`                                                                                                                           |
| I45  | A read whose answer would be SILENTLY SHORT raises where it is read: one bounded by the list cap asks ONE document past it                                                                                                                                                      | `fl_backend/tests/api/test_slice_completeness.py :: TestTheBracketWriteRefusesATruncatedSeason`; each capped read asks `limit + 1` and raises on the extra document                                                                                                                                                                                                                                                                                                                                                          |
| I46  | The draw is ATOMIC across every collection it writes — matchdays, fixtures, the `spielplan` watermark, the log rows — as is each removal of one                                                                                                                                 | `fl_backend/app/api/saisons/admin_router.py :: generate_spielplan` wraps the judgement and every write in one `with_transaction` callback; `fl_backend/app/api/saisons/spielplan.py :: draw_spielplan` composes every document; `fl_backend/app/core/crud.py :: post_many_to_db` takes the session                                                                                                                                                                                                                           |
| I47  | A `future` season is withheld from the base tier, as is everything scoped to it: 404 or empty, never 403, indistinguishable from absence outside I111                                                                                                                           | `fl_backend/app/api/saisons/services.py :: WITHHELD_FROM_BASE_TIER` and `:: base_tier_status_term`; `fl_backend/app/api/saisons/visibility.py :: withheld_saison_ids` for `GET /spieler`; `fl_backend/tests/api/test_saison_contents_visibility.py :: TestTheWithheldSeasonReadsAsOneThatWasNeverCreated`                                                                                                                                                                                                                    |
| I48  | A removal records what it destroyed AND which kind: `delete_many` keeps the filter, the count and an ARRAY of pre-images; `erase_many` keeps no image                                                                                                                           | `fl_backend/app/core/crud.py :: delete_many_from_db` reads every matching document unbounded, so the logged set cannot be narrower than the deleted one; `:: erase_many_from_db` reads none                                                                                                                                                                                                                                                                                                                                  |
| I49  | **A model FastAPI parses a request body into REFUSES a key it does not declare**, and so does every model nested inside one                                                                                                                                                     | `fl_backend/tests/api/test_payload_strictness.py :: test_a_request_body_refuses_a_key_no_model_declares`, which validates every model a route declares a body of with an undeclared key                                                                                                                                                                                                                                                                                                                                      |
| I50  | A JOIN into `saison_teams` carries back an ALLOW-LIST decided INSIDE the `$lookup`: the row's three people and their details reach no base-tier read                                                                                                                            | `fl_backend/app/api/teams/services.py :: build_team_pipeline`, whose lookup pipeline ends in the five fields the outer `$project` reads; `fl_backend/tests/api/test_teams_pipeline.py :: TestTheJunctionJoinWithholdsTheRestOfTheRow` and `fl_backend/tests/api/test_teams_public_read.py :: TestTheBaseTierReadsWithholdTheJunctionsContactRecords`                                                                                                                                                                         |
| I51  | Accepting an application is ONE transaction over three writes — the club, the `saison_teams` row and the decision — every refusal answering before the first                                                                                                                    | `fl_backend/app/api/bewerbungen/admin_router.py :: annehmen_bewerbung`, whose transactional callback holds all three; `fl_backend/tests/api/test_bewerbung_triage_execution.py :: TestAPartialAcceptanceCommitsNothing` and `:: TestTheSeasonsOwnEntryRulesReachTheAcceptance`                                                                                                                                                                                                                                               |
| I52  | A request making MORE THAN ONE write makes them in ONE transaction, and an endpoint that opens one makes NO write beside it                                                                                                                                                     | `fl_backend/tests/api/test_write_transactions.py`, a sweep of the source counting every write-helper and driver-write call site reachable from a route-decorated handler outside a `with_transaction` callback                                                                                                                                                                                                                                                                                                               |
| I53  | A transaction protects a judgement over exactly the documents its callback WRITES: a conflict is raised on an overlapping WRITE set, never on a read                                                                                                                            | `fl_backend/tests/api/test_saison_patch_isolation.py`, `fl_backend/tests/api/test_activation_isolation.py`, `fl_backend/tests/api/test_capacity_isolation.py`, `fl_backend/tests/api/test_reference_isolation.py`, `fl_backend/tests/api/test_sperrliste_isolation.py` and `fl_backend/tests/api/test_schiedsrichter_anonymisierung.py`, each driving a rival write into the refusal on a replica set, and `fl_backend/tests/core/test_bounded_writes.py` pinning every anchor                                               |
| I54  | Every read whose order a person sees carries `fl_backend/app/core/crud.py :: GERMAN_COLLATION`, per read and never as a default: a collated read loses a simple-collation index                                                                                                 | `fl_backend/tests/api/test_name_ordering.py`, driving the venue, referee, club and squad lists over a corpus whose German and byte order disagree; `fl_backend/app/api/spiele/crud.py` carries the one uncollated reuse                                                                                                                                                                                                                                                                                                      |
| I95  | `GET /teams` serves the SEASON's copy of a club's `name` and `shorthand`, off the joined `saison_teams` row and never the club's own                                                                                                                                            | `fl_backend/app/api/teams/services.py :: build_team_pipeline`'s `$project`, which takes both off the joined row; the rename fan-out rewrites it (I13)                                                                                                                                                                                                                                                                                                                                                                        |
| I96  | The head-to-head table ranks a set only where every pair in it that could hold a placing has met; short of that it decides nothing                                                                                                                                              | `fl_backend/app/api/teams/services.py :: _MiniTable`'s `every_pair_met`, asked at both of its uses; `:: _may_hold_a_platz` decides who counts toward it                                                                                                                                                                                                                                                                                                                                                                      |
| I97  | The still-to-play term is SERVED beside a standings row, so the frontend applies I24b rather than a reading of its own                                                                                                                                                          | `fl_backend/app/api/teams/schemas.py :: FLGruppenTeam` carries `anzahl_ausstehende_spiele` required and with no default, read by `fl_frontend/src/features/teams/utils.ts :: mayHoldAPlatz`; `fl_backend/tests/api/test_standings.py` pins it                                                                                                                                                                                                                                                                                |
| I98  | The standings a `gruppe` slot resolves against are derived from the fixtures the resolution was handed, never re-read from `spiele`                                                                                                                                             | `fl_backend/app/api/spiele/crud.py :: _resolve_one_saison`, which derives them through `fl_backend/app/api/teams/services.py :: build_statistik_by_team`; `fl_backend/tests/api/test_spiele_write_execution.py :: TestAResultThatReordersItsGroupIsPreviewedAsItIsSaved`                                                                                                                                                                                                                                                     |
| I99  | An advancement reports what it destroyed beside the fixture it moved: the scoreline, the shoot-out and the no-show composing it                                                                                                                                                 | `fl_backend/app/api/spiele/schemas.py :: _VoidedResult`, whose fields both reports carry, filled through `fl_backend/app/api/spiele/services.py :: voided_no_show`; `fl_backend/tests/api/test_bracket.py :: TestNamingWhatWasVoided`                                                                                                                                                                                                                                                                                        |
| I100 | An ineligible occupant is refused on the fixture that holds it, never moved off it (`REQ-ELIGIBILITY-001`, `REQ-ELIGIBILITY-002`)                                                                                                                                               | `fl_backend/app/api/spiele/services.py :: find_eligibility_refusal`, called in `judge` before the write; `fl_backend/tests/api/test_occupant_refusal.py`                                                                                                                                                                                                                                                                                                                                                                     |
| I101 | The departed occupant, double Spieltag entry, retired booking and double booking are `GET /spiele/action_required`'s alone, the four faults reaching Gruppenphase fixtures                                                                                                      | `fl_backend/app/api/spiele/crud.py :: find_bracket_faults`, which adds all four beside the walk the save's `bracket_faults` come from; `fl_backend/tests/api/test_spiele_write_execution.py :: TestAFixtureTheResolutionReopensKeepsARetiredBooking`, `:: TestALiftedNoShowDoubleBooksAndTheQueueNamesBoth` and `:: TestTheQueueReadsTheHoldersTheRefusalReads`                                                                                                                                                              |
| I102 | A SERVED fixture side carries the `austritt_type` alone (`READ-FREETEXT-002`); the whole record reaches `FLSpielJoinedInternal`, which is on no endpoint, and the stored shape carries neither                                                                                  | `fl_backend/app/api/spiele/services.py :: _joined_side` projects both spellings out of ONE `$let`, and each read model declares the one it serves; `fl_backend/app/api/spiele/crud.py :: _stored_side`                                                                                                                                                                                                                                                                                                                       |
| I103 | `GET /teams?has_austritt=` selects on having left and `?austritt_type=` narrows to one route out; the two compose without either implying the other                                                                                                                             | `fl_backend/app/api/teams/services.py :: build_team_pipeline`, which translates `has_austritt` into a null test on `austritt` and `austritt_type` into its own term                                                                                                                                                                                                                                                                                                                                                          |
| I104 | No rule generalises I36's read-model exemption: whether a bound sits on the write side, the read side or both is decided at the field                                                                                                                                           | `fl_backend/app/api/saisons/schemas.py :: FLSaison` and `fl_backend/app/api/teams/schemas.py :: _TeamWritable` each bound a read; `fl_backend/tests/api/test_teams.py :: TestAContactRecordReadsBackHoweverItWasStored`                                                                                                                                                                                                                                                                                                      |
| I105 | `rolle` is the one key the `saison_spieler` validator leaves out of `required`, so a missing key and a stored null both mean no role                                                                                                                                            | `fl_backend/app/core/constraints.py`, whose `saison_spieler` `required` list omits the key                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| I106 | A wiring refusal is judged per SIDE, against the source stored there, so a fixture already wired out of rule stays editable elsewhere (I27, I44)                                                                                                                                | `fl_backend/app/api/spiele/services.py :: find_wiring_refusal`; `fl_backend/tests/api/test_wiring_refusal.py :: TestAStoredShapeDoesNotLatch`                                                                                                                                                                                                                                                                                                                                                                                |
| I107 | The action-log LIST serves `stand_gesichert` in the pre-image's place, computed presence-aware so a dropped field cannot read as a stored null (I48)                                                                                                                            | `fl_backend/app/api/aktionen/schemas.py :: FLAktion`, which declares no `before` and computes the flag; `fl_backend/tests/api/test_aktionen_read.py :: TestTheListReportsTheImageWithoutServingIt`                                                                                                                                                                                                                                                                                                                           |
| I108 | A caller-supplied season slice ASSERTS that it holds the fixture under edit, raising rather than permitting what it cannot see                                                                                                                                                  | `fl_backend/app/api/spiele/services.py :: stored_in_slice`, which every refusal over a slice takes its stored fixture from; `fl_backend/tests/api/test_slice_completeness.py :: TestARefusalNeverPermitsWhatItCannotSee`                                                                                                                                                                                                                                                                                                     |
| I109 | **RECORDED is wider than played**, and `fl_backend/app/api/saisons/services.py :: RECORDED_FACT_FIELDS` is the whole of it; a date and a kickoff time are not records                                                                                                           | `fl_backend/app/api/saisons/services.py :: holds_a_recorded_fact` over `:: RECORDED_FACT_FIELDS`; `fl_backend/tests/api/test_spielplan_refusal.py :: TestWhatCountsAsRecordedAgainstAFixture` derives a case per projected path, and `fl_backend/tests/core/test_write_shapes.py :: TestThePredicateReadsNoKeyItsProjectionMisses` holds it to the set                                                                                                                                                                       |
| I110 | The UNDRAW CLEARS the `spielplan` watermark rather than restamping it, so no season is left whose watermark and whose fixtures disagree                                                                                                                                         | `fl_backend/app/api/saisons/admin_router.py :: undraw_spielplan`, one callback over both removals and the `$unset` that clears the watermark                                                                                                                                                                                                                                                                                                                                                                                 |
| I111 | I47's ONE carve-out: those three reads serve a `future` season's running window and that it has not ended, and the by-id twin its existence (I188)                                                                                                                              | `fl_backend/app/api/bewerbungen/schemas.py :: FLBewerbungFensterResponse` and `:: FLBewerbungTrikotFarbenResponse`, each an allow-list; `fl_backend/app/api/bewerbungen/services.py :: window_is_running` and `:: season_has_ended`; `fl_backend/tests/api/test_bewerbung_public_read.py`                                                                                                                                                                                                                                    |
| I188 | The by-id window read answers that a season exists and nothing else about it, so a mistyped id keeps its 404                                                                                                                                                                    | `fl_backend/app/api/bewerbungen/schemas.py :: FLBewerbungKeinFensterResponse`, an allow-list; `fl_backend/tests/api/test_bewerbung_public_read.py :: TestTheWindowReads`                                                                                                                                                                                                                                                                                                                                                     |
| I189 | The application sweep records the day it ran on every season not carrying it, which `GET /bewerbungen/sweep` answers beside the registration pass's (I289)                                                                                                                      | `fl_backend/app/api/bewerbungen/sweep_router.py :: stamp_the_run`, a read that returns before the writer where nothing is stale; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestThePassRecordsTheDayItRan`, whose day of passes costs one log row                                                                                                                                                                                                                                                            |
| I190 | Every published operation names the key its guard demands, in the frontend's own spelling: nothing else holds a call site's tier to a route                                                                                                                                     | `fl_backend/app/main.py :: publish_key_tiers`, re-derived from the route by `fl_backend/tests/api/test_admin_guard.py :: test_the_published_tier_is_the_guard_the_route_carries`                                                                                                                                                                                                                                                                                                                                             |
| I192 | A contacts save carries the block's token and is refused whole where the row answers another                                                                                                                                                                                    | `fl_backend/app/api/teams/schemas.py :: kontakte_stand_of`, judged inside the endpoint's transaction by `fl_backend/app/api/teams/services.py :: find_kontakte_precondition_refusal`; `fl_backend/tests/api/test_saison_team_kontakte.py :: TestAnErasureLandingMidSaveIsNotUndone` and `:: TestTheTokenNamesWhatTheEditorWasServed`                                                                                                                                                                                         |
| I193 | `GET /bewerbungen` counts every status over the set its other terms leave, never over the statuses it was asked for                                                                                                                                                             | `fl_backend/app/api/bewerbungen/router.py :: get_bewerbungen`, counting against `fl_backend/app/api/bewerbungen/services.py :: build_bewerbungen_saison_term` alone; `fl_backend/tests/api/test_bewerbungen_read.py :: TestTheCountsAreTheServersRatherThanTheRowsOnHand`                                                                                                                                                                                                                                                    |
| I195 | A delivery event applies only where it names the message that seat holds and is stamped later than the state it holds                                                                                                                                                           | `fl_backend/app/api/bewerbungen/services.py :: zustellung_event_applies`, judged inside the endpoint's transaction; `fl_backend/tests/api/test_bewerbung_zustellung_refusal.py :: TestWhenAnEventApplies` and `fl_backend/tests/api/test_bewerbung_zustellung_execution.py :: TestAnEventReachesTheSeatItWasSentTo`, which drive the two conditions red separately                                                                                                                                                           |
| I196 | An application whose deletion notice the mail provider refuses is held past its deadline rather than erased unannounced, never past its season (I220)                                                                                                                           | `fl_backend/app/api/bewerbungen/services.py :: announcement_is_undeliverable`, a term of `:: deletion_is_due` that every sweep path reads; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestAnApplicationWhoseNoticeCannotArrive`                                                                                                                                                                                                                                                                              |
| I112 | Two operation members rather than one flag on one, so an absent image is never ambiguous between deliberately not kept and forgotten                                                                                                                                            | `fl_backend/app/core/constraints.py` carries both members in its `operation` enum, pinned against `fl_backend/app/core/recording.py :: Operation` by `fl_backend/tests/core/test_constraints.py :: RECORDED_LITERALS`                                                                                                                                                                                                                                                                                                        |
| I113 | A removal's pre-images are refused wherever the values ARE its subject: an erasure's image would be a fresh copy of exactly what the erasure destroyed                                                                                                                          | `fl_backend/app/core/crud.py :: erase_many_from_db`, which hands the log no `before`; `fl_backend/tests/core/test_unenforced.py` holds both removals to that one module                                                                                                                                                                                                                                                                                                                                                      |
| I114 | I49 stops exactly where a READ parses the same class: forbidding there would answer 500 for a stored document carrying a key no model declares                                                                                                                                  | `fl_backend/tests/api/test_payload_strictness.py :: test_a_model_is_strict_exactly_where_no_read_parses_it` and `:: test_no_model_a_read_parses_refuses_an_undeclared_key`                                                                                                                                                                                                                                                                                                                                                   |
| I115 | Query-parameter models are outside I49 and not by omission: `Depends()` never assembles an undeclared parameter, and `Query()` leaves it to the model's `extra` default                                                                                                         | Unenforced — every filter-params model's route wiring, `Query()` where a `list` field needs it (`fl_backend/app/api/bewerbungen/router.py :: FLBewerbungenFilters`) and `Depends()` otherwise; review                                                                                                                                                                                                                                                                                                                        |
| I116 | Acceptance holds an entry to the season's OWN entry rules, REUSED and never restated, so there is no second copy of them to drift                                                                                                                                               | `fl_backend/app/api/bewerbungen/admin_router.py :: annehmen_bewerbung` calls `fl_backend/app/api/teams/services.py :: find_entry_refusal` and `:: find_club_entry_refusal` rather than declaring rules of its own                                                                                                                                                                                                                                                                                                            |
| I117 | A `$set` of the value the snapshot already holds rewrites nothing, so it puts its own document in no write set                                                                                                                                                                  | `fl_backend/tests/api/test_activation_isolation.py :: TestARivalRolloverLandingMidReactivationIsJudgedAgain`, which drives a rival under a promotion that rewrites nothing                                                                                                                                                                                                                                                                                                                                                   |
| I118 | An endpoint whose callback judges what it does not write re-reads the movable state OUTSIDE its session immediately before answering and refuses on what moved                                                                                                                  | `fl_backend/tests/api/test_saison_patch_isolation.py :: TestAPlayerAddedMidPatchIsJudgedAgain`, paired with a control                                                                                                                                                                                                                                                                                                                                                                                                        |
| I119 | A log row is redacted in place while it lives (I42); one stamped `at_date` expires twelve months after its write, and no code deletes any                                                                                                                                       | `fl_backend/app/core/constraints.py :: TTL_INDEXES`, built and read back by `fl_backend/tests/core/test_constraints_execution.py :: test_every_declared_ttl_index_is_built_over_its_key_with_its_bound`; `fl_backend/tests/core/test_unenforced.py :: TestNoPurgeReachesARetiredRow` holds every removal to `fl_backend/app/core/crud.py`                                                                                                                                                                                    |
| I130 | `YYYY-MM-DD`, zero-padded, is the ONE stored date form: every comparison ORDERING dates is a string comparison, and parsing appears only where arithmetic needs it                                                                                                              | `fl_backend/app/shared/schemas/custom.py :: DATE_REGEX` and `fl_backend/tests/shared/test_custom.py :: test_rejects_malformed_dates`, on write payloads alone; the validators pin no pattern (I16) and nothing sweeps the comparisons                                                                                                                                                                                                                                                                                        |
| I131 | The backend's ONE cache is process-local and TTL-bounded over season documents (`fl_backend/app/api/saisons/cache.py :: SAISON_CACHE_TTL_SECONDS`), and every season write drops it WHOLE                                                                                       | `fl_backend/tests/api/test_saison_cache.py :: TestEverySeasonWriteDropsIt`, an AST sweep of every admin handler writing a season, and `:: TestTheCacheContract` for the TTL and the copies                                                                                                                                                                                                                                                                                                                                   |
| I132 | Reaching past `fl_backend/app/core/crud.py` is CLOSED to four reasons: a cursor, a sorted single-document read, a count, and absence answering rather than a 404                                                                                                                | unenforced — `fl_backend/tests/api/test_write_transactions.py` sweeps the WRITE sites alone, and nothing sweeps the reads; review judgment                                                                                                                                                                                                                                                                                                                                                                                   |
| I137 | A replacement carries over the row's identity and its fixtures alone: the `austritt`, kit colour and contacts are cleared and the outgoing squad retired                                                                                                                        | `fl_backend/app/api/teams/admin_router.py :: replace_saison_team`, one `$set` clearing the three; `fl_backend/tests/api/test_saison_team_replacement_execution.py :: TestAllFourLayersMoveTogether` and `:: TestTheOutgoingClubsSquadIsRetired`                                                                                                                                                                                                                                                                              |
| I138 | The draw writes no date or kickoff time: an undraw destroys hand-assigned ones without refusing (I109), and only the log's images hold them (I48)                                                                                                                               | `fl_backend/app/api/saisons/spielplan.py :: _spiel` writes both null, pinned by `fl_backend/tests/api/test_spielplan.py :: test_nothing_is_scheduled_and_nothing_has_happened`; `fl_backend/tests/api/test_undraw_execution.py :: TestTheRemovalKeepsEveryImage`                                                                                                                                                                                                                                                             |
| I180 | An application's contact person confirms inside the age span EVERY seat they hold allows, in whole years against the German day they answer on                                                                                                                                  | `fl_backend/app/api/bewerbungen/services.py :: mindestalter_for` over `fl_backend/app/api/bewerbungen/schemas.py :: refuse_age_outside_the_bounds`, swept by `fl_backend/tests/api/test_bewerbung_einwilligung_refusal.py :: TestWhichFloorAPersonClears` and `:: TestTheAgeAtConfirmation`, its numbers compared to the frontend's by `fl_backend/tests/shared/test_frontend_mirrors.py :: test_every_declared_pair_agrees_on_the_number`                                                                                   |
| I185 | A confirmation link's own read loads only the fields its handler resolves, both projections derived per seat rather than typed out                                                                                                                                              | `fl_backend/app/api/bewerbungen/services.py :: EINWILLIGUNG_ANSICHT_FIELDS` and `:: EINWILLIGUNG_ANTWORT_FIELDS`, derived from `:: KONTAKT_SEATS` and `:: TOKEN_HASH_FIELDS`; `fl_backend/tests/api/test_bewerbung_einwilligung_execution.py :: TestWhatAnAnonymousReadLoads` and `test_bewerbung_einwilligung_refusal.py :: TestTheAnonymousProjectionsAreDerivedPerSeat`                                                                                                                                                   |
| I199 | The first knockout round seeds one qualifier of each group per bracket block, so two of one group meet as late as the field allows                                                                                                                                              | `fl_backend/app/api/saisons/spielplan.py :: bracket_seeding`, held to a closed-form bound by `fl_backend/tests/api/test_spielplan.py :: TestTheConstructionScoresTheBestThereIs`                                                                                                                                                                                                                                                                                                                                             |
| I200 | A season-scoped list's chained sort is left UNINDEXED, `REQ-RULES-013` capping a season inside the list bound; one spanning seasons needs the index                                                                                                                             | `fl_backend/app/api/spiele/services.py :: build_spiele_sort` and `fl_backend/app/api/spieltage/services.py :: build_spieltage_sort` chain it; `fl_backend/app/api/spiele/router.py :: get_spiele` and `fl_backend/app/api/saisons/services.py :: find_rules_refusal` hold one inside `fl_backend/app/shared/schemas/bounds.py :: LIST_LIMIT_DEFAULT`                                                                                                                                                                         |
| I203 | `GET /bewerbungen` answers which collision keys two or more OPEN applications share, over the whole queue rather than over the rows it served                                                                                                                                   | `fl_backend/app/api/bewerbungen/services.py :: build_dubletten_pipeline` with `:: dubletten_schluessel_of`; `fl_backend/tests/api/test_bewerbungen_read.py :: TestTheCollisionSurvivesTheReadsCap`                                                                                                                                                                                                                                                                                                                           |
| I204 | The one season id a payload MINTS is four ASCII digits naming a playable year, its ceiling read per validation rather than at import                                                                                                                                            | `fl_backend/app/api/saisons/schemas.py :: FLPostSaisonPayload` and `:: refuse_a_saison_year_outside_the_league`, swept by `fl_backend/tests/api/test_saison_id_bounds.py`, mirrored by `fl_frontend/src/features/saisons/saisonIdMirror.test.ts`                                                                                                                                                                                                                                                                             |
| I208 | `GET /aktionen` counts every facet dimension over the rows its trace and document terms leave, each ignoring its own selection and applying the others                                                                                                                          | `fl_backend/app/api/aktionen/admin_router.py :: FACET_TALLY`, one grouped pass read by `:: _tally`; `fl_backend/tests/api/test_aktionen_tally_execution.py` drives all three dimensions against a real server                                                                                                                                                                                                                                                                                                                |
| I210 | A restore names the fields the write it undoes replaced and reads every other off the stored fixture, so an edit in between survives                                                                                                                                            | `fl_backend/app/api/spiele/schemas.py :: FLPatchSpielPaarungPayload`, whose `completed_with` reads the rest off the document; `fl_backend/tests/api/test_spiel_paarung_execution.py` drives the route and the undo's sequence against a replica set                                                                                                                                                                                                                                                                          |
| I212 | An erasure's log redaction reaches every image holding the person, an image the erasure cannot name by document id included                                                                                                                                                     | `fl_backend/app/api/schiedsrichter/services.py :: build_booked_image_filter` beside `app/core/recording.py :: build_redaction_filter`, as `app/api/kontakte/services.py :: build_orphaned_images_pipeline` and `:: images_holding` is; `fl_backend/tests/api/test_schiedsrichter_anonymisierung.py`                                                                                                                                                                                                                          |
| I213 | A referee's erasure deletes their document and repoints every fixture naming them at the ghost, played or not, keeping each fixture's own `payment`                                                                                                                             | `fl_backend/app/api/schiedsrichter/admin_router.py :: anonymise_schiedsrichter`, over `fl_backend/app/api/schiedsrichter/services.py :: build_assignment_filter` and `:: build_ghost_repoint`; `fl_backend/tests/api/test_schiedsrichter_anonymisierung.py :: test_the_referees_document_is_gone`, `:: test_every_fixture_they_officiated_is_repointed` and `:: test_the_fee_the_match_agreed_survives_the_repoint`                                                                                                          |
| I215 | An undo's report names every fixture the write changed, the requested one first, so its resolution refills the moved slots before their results are written                                                                                                                     | `fl_backend/app/api/spiele/crud.py :: report_prior_paarungen` orders it, `fl_backend/app/api/spiele/schemas.py :: FLSpielPriorOtherFields` carries what a payload replaced, and `fl_frontend/src/app/api/admin/spiele/undo/route.ts` replays it; `fl_backend/tests/api/test_bracket.py :: TestPuttingBackWhatTheResolutionDestroyed`                                                                                                                                                                                         |
| I218 | `GET /bewerbungen` counts both sides of the season relation over the set its status term leaves, never over the relation it was asked for                                                                                                                                       | `fl_backend/app/api/bewerbungen/router.py :: get_bewerbungen`, counting against `fl_backend/app/api/bewerbungen/services.py :: build_bewerbungen_status_term` alone; `fl_backend/tests/api/test_bewerbungen_read.py :: TestBothSidesOfTheSeasonRelationAreCounted`                                                                                                                                                                                                                                                           |
| I219 | Provenance and birthdate carry forward only where the address's sign-in fold and both names' `person_name_key` agree; anything else is a handover                                                                                                                               | `fl_backend/app/api/teams/services.py :: _seat_held_by` over `:: SEAT_IDENTITY_FIELDS`; `fl_backend/tests/api/test_saison_team_kontakte.py :: TestTheDateRidesWithThePerson` and `:: TestTheProvenanceIsTheServers`; `fl_backend/app/shared/folding.py :: person_name_key`                                                                                                                                                                                                                                                   |
| I220 | An application still `eingereicht` when the season it applied for is `past` is erased, whatever its seats answered or its notice became                                                                                                                                         | `fl_backend/app/api/bewerbungen/services.py :: undecided_erasure_is_due` and `:: season_has_ended`, read off `saisons.status`; `fl_backend/app/api/bewerbungen/sweep_router.py :: erase_the_undecided`, run before the reminder; `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestTheSeasonsOwnEndClock`                                                                                                                                                                                                       |
| I221 | An acceptance copies a seat's birthdate into `saison_teams` only where that seat's own `bestaetigt_am` stands; every other seat enters undated                                                                                                                                  | `fl_backend/app/api/teams/services.py :: compose_kontakte_at_entry`, inside `fl_backend/app/api/bewerbungen/admin_router.py :: annehmen_bewerbung`; `fl_backend/tests/api/test_bewerbung_triage_execution.py :: TestAnAcceptanceEntersTheSchool` and `:: TestAcceptanceWaitsForEverySeat`                                                                                                                                                                                                                                    |
| I222 | A restore replays every fixture one save moved in ONE transaction: a refusal on one leaves the season as that save left it                                                                                                                                                      | `fl_backend/app/api/spiele/admin_router.py :: _write_spiel_data` opens one transaction over the whole list; `fl_backend/tests/api/test_spiel_paarung_execution.py :: TestAChainOfRoundsGoesBackWholeOrNotAtAll` drives a refused entry                                                                                                                                                                                                                                                                                       |
| I223 | A replay writes the entries in the body's own order, `spiel_nr` ascending behind the fixture the save named, and re-sorts nothing                                                                                                                                               | `fl_backend/app/api/spiele/crud.py :: report_prior_paarungen` composes it (I215), `fl_backend/app/api/spiele/admin_router.py :: patch_spiele_paarungen` walks it as given, and `fl_backend/app/api/spiele/services.py :: find_wiring_refusal` refuses a fixture ahead of its feeder                                                                                                                                                                                                                                          |
| I224 | A referee holding a fixture still to be played is refused retirement (`REQ-RETIRE-004`); one already played never blocks it                                                                                                                                                     | `fl_backend/app/api/schiedsrichter/admin_router.py :: delete_schiedsrichter`, judging `fl_backend/app/api/schiedsrichter/services.py :: find_referee_retire_refusal` over `:: build_unplayed_assignment_filter` in-session; `fl_backend/tests/api/test_containment_refusals.py :: TestRetiringAVenueOrAReferee` and `fl_backend/tests/api/test_reference_isolation.py :: RETIRE_THE_REFEREE`'s cases                                                                                                                         |
| I227 | The ghost is off `GET /schiedsrichter` whatever the query string asks, and every by-id route answers 404 for it and for an erased id                                                                                                                                            | `fl_backend/app/api/schiedsrichter/services.py :: build_referee_filter`, which every by-id route shares, beside `:: build_real_referees_filter`; `fl_backend/tests/api/test_schiedsrichter_anonymisierung.py :: test_the_ghost_is_off_the_list_an_administrator_works_from`, `:: test_the_single_read_answers_for_neither_the_erased_referee_nor_the_ghost` and `:: test_no_write_endpoint_reaches_the_ghost`                                                                                                                |
| I256 | The erasure writes the ghost itself when none stands, so a repoint never names a row nothing holds                                                                                                                                                                              | `fl_backend/app/api/schiedsrichter/admin_router.py :: anonymise_schiedsrichter`, inside its own transaction; `fl_backend/tests/api/test_schiedsrichter_anonymisierung.py :: test_the_ghost_is_written_where_no_ghost_stood` and `:: test_a_second_erasure_reuses_the_one_ghost`                                                                                                                                                                                                                                              |
| I252 | A club that left the league stays in a `past` season's standings, list and bracket placings, and out of every other season's                                                                                                                                                    | `fl_backend/app/api/teams/services.py :: build_retirement_stages`; `fl_backend/tests/api/test_teams_public_read.py :: TestAPastSeasonKeepsTheRetiredClubsThatPlayedIt`, `:: TestARetiredClubStaysOutOfTheBaseTierReads` and `:: TestTheBracketPlacesTheClubsTheTableRanks`                                                                                                                                                                                                                                                   |
| I254 | A squad `nummer` is one to four digits on the WRITE payloads alone, never on a read model                                                                                                                                                                                       | `fl_backend/app/api/spieler/schemas.py :: SQUAD_NUMMER_PATTERN`, applied by `_SaisonSpielerPayload` and by no read model; `fl_backend/tests/api/test_reference_models.py :: TestASquadNumberOnTheWritePath`                                                                                                                                                                                                                                                                                                                  |
| I257 | A fixture still to be played on a retired row, or claiming one within four hours of another, is reported and never refused a reopening                                                                                                                                          | `fl_backend/app/api/spiele/services.py :: find_retired_bookings` and `:: find_double_bookings`; `fl_backend/app/core/domain.py :: UNENFORCED`; `fl_backend/tests/api/test_reference_isolation.py :: TestALiftedNoShowAndABookingAtItsHourAreJudgedAgainstEachOther` and `:: TestAReopeningAndARetirementLandingInsideEachOther`; `fl_backend/tests/api/test_spiele_write_execution.py :: TestAFixtureTheResolutionReopensKeepsARetiredBooking`                                                                               |
| I258 | `REQ-CLASH-001` judges only a claim the fixture did not already make, so a note or a result on a clashing fixture saves                                                                                                                                                         | `fl_backend/app/api/spiele/services.py :: find_claims_made`, read by `judge` and `:: find_references_to_anchor`; `fl_backend/tests/api/test_reference_isolation.py :: TestASaveKeepingAClashingSlotAndANewClaimBesideItAreJudgedApart` and `fl_backend/tests/api/test_spiele_write_execution.py :: TestALiftedNoShowDoubleBooksAndTheQueueNamesBoth`                                                                                                                                                                         |
| I259 | The ghost is a permanently retired row, so `REQ-BOOKING-001` refuses it every new fixture and I257 reaches it like any other                                                                                                                                                    | `fl_backend/app/core/sentinels.py :: GHOST_INACTIVE_SINCE`, written by `fl_backend/app/api/schiedsrichter/services.py :: build_ghost_schiedsrichter` and read by `fl_backend/app/api/spiele/services.py :: find_booking_refusal`; `fl_backend/tests/api/test_spiele_write_execution.py :: TestTheBookingRefusalIsReachedThroughTheRoute` and `:: TestAFixtureTheResolutionReopensKeepsARetiredBooking`, each carrying a `the-ghost` case                                                                                     |
| I262 | One consent predicate is spelled in Python and as a `$cond`, and every spelling answers one truth table                                                                                                                                                                         | `fl_backend/app/api/spieler/services.py :: name_is_public` and `:: PUBLIC_PERSON`; `fl_backend/tests/api/test_name_is_public_mirror.py :: NAME_IS_PUBLIC`, one case per spelling                                                                                                                                                                                                                                                                                                                                             |
| I265 | A pupil's `email` holds `sign_in_identifier`'s output or nothing: no payload carries it and no route writes it                                                                                                                                                                  | `fl_backend/app/shared/folding.py :: sign_in_identifier`, held to the frontend's by `fl_backend/tests/shared/email_addresses.json`; `fl_backend/app/api/spieler/schemas.py :: FLPatchSpielerPayload`, which forbids extras, pinned by `fl_backend/tests/api/test_spieler_memberships_read.py :: TestTheAddressASignedInPersonIsJoinedOn`                                                                                                                                                                                     |
| I266 | A delivery record has one home per kind, and a report naming a kind the register has no home for is refused rather than written                                                                                                                                                 | `fl_backend/app/api/zustellung/services.py :: ZIEL_PFADE`, closed against `fl_backend/app/api/zustellung/schemas.py :: FLZustellungZiel`; `fl_backend/tests/api/test_zustellung_refusal.py :: TestTheTargetRegister` and `:: TestTheSharedJudgesReadTheProjectedDocument`, which drive the register and the judge the projection feeds                                                                                                                                                                                       |
| I267 | An accepted send applies where the seat's entry stands and no newer accept is stored; a provider's stamp never blocks it                                                                                                                                                        | `fl_backend/app/api/bewerbungen/services.py :: zustellung_send_applies`, judged inside the endpoint's transaction; `fl_backend/tests/api/test_bewerbung_zustellung_refusal.py :: TestWhenAnAcceptedSendApplies` and `fl_backend/tests/api/test_zustellung_execution.py :: TestTheAcceptedSend`                                                                                                                                                                                                                               |
| I268 | A ban outlives both erasures, the person it bars and the administrator it names, until it lapses (I273)                                                                                                                                                                         | `fl_backend/app/core/domain.py :: AGGREGATES`; `fl_backend/tests/api/test_sperrliste_execution.py :: TestAnErasureLeavesTheListStanding`                                                                                                                                                                                                                                                                                                                                                                                     |
| I269 | One address reaches one ban whichever spelling arrives: a Unicode domain and its punycode key alike, decoded as stored bans were, Greek sigmas apart (§4)                                                                                                                       | `fl_backend/app/shared/folding.py :: canonical_address`; `fl_backend/tests/api/test_sperrliste_hash.py :: TestOneCanonicalFormWhateverTheRoute`, `fl_backend/tests/shared/test_email_address.py :: test_a_typed_address_and_what_its_payload_stored_key_one_ban`                                                                                                                                                                                                                                                             |
| I270 | The subject seam answers record ids and a seat's two facts — no name, address or telephone number of any person reaches its caller                                                                                                                                              | `fl_backend/app/api/identitaet/schemas.py :: FLSubjektResponse`, declared from nothing; `fl_backend/tests/api/test_identitaet_subjekt_execution.py :: test_one_mailbox_answers_all_three_kinds_at_once`                                                                                                                                                                                                                                                                                                                      |
| I275 | A raw registration link exists only in the mint's own answer: no model declares `token_hash` and the admin read projects it away                                                                                                                                                | `fl_backend/app/api/einladungen/services.py :: WITHOUT_TOKEN_HASH`; `fl_backend/tests/api/test_einladung_refusal.py :: TestNoModelInTheSliceDeclaresAHash` and `fl_backend/tests/api/test_einladung_execution.py :: TestTheStateRead`                                                                                                                                                                                                                                                                                        |
| I276 | An invitation's delivery carrier is written empty at the mint, and only the accepted send writes the record inside it                                                                                                                                                           | `fl_backend/app/api/einladungen/services.py :: compose_einladung`; `fl_backend/tests/api/test_einladung_execution.py :: TestWhatAMintStores` and `:: TestTheSeasonWidePress`                                                                                                                                                                                                                                                                                                                                                 |
| I277 | At most one live invitation per team and season, a reissue revoking and minting in one transaction                                                                                                                                                                              | `fl_backend/app/core/constraints.py :: uniq_einladung_live`, partial on a null `widerrufen_am`; `fl_backend/tests/api/test_einladung_execution.py :: TestTheRuleOfOneLiveInvitation`                                                                                                                                                                                                                                                                                                                                         |
| I278 | A registration link carries no expiry of its own: the season's registration window is judged at every use, both ends inside it                                                                                                                                                  | `fl_backend/app/api/einladungen/services.py :: registrierungsfenster_laeuft`; `fl_backend/tests/api/test_einladung_refusal.py :: TestTheWindowALinkExpiresWith`                                                                                                                                                                                                                                                                                                                                                              |
| I336 | A season that has ended takes no registration whatever dates its window stores, and the link's page reads it as shut                                                                                                                                                            | `fl_backend/app/api/registrierungen/services.py :: saison_nimmt_registrierungen_an`; `fl_backend/tests/api/test_registrierung_submission_execution.py :: TestWhatASubmissionIsRefused` and `:: TestWhatTheInvitesOwnReadAnswers`; `fl_backend/tests/api/test_einladung_execution.py :: TestTheStateRead`                                                                                                                                                                                                                     |
| I337 | A season that has ended takes no application whatever dates its window stores: `/fenster` passes over it, its window reads not running, its colours 404                                                                                                                         | `fl_backend/app/api/bewerbungen/services.py :: saison_nimmt_bewerbungen_an` and `:: SAISON_NOT_ENDED_FILTER`; `fl_backend/tests/api/test_bewerbung_public_read.py :: TestASeasonThatHasEndedTakesNoApplication`                                                                                                                                                                                                                                                                                                              |
| I279 | The season's mailing and its preview decide over one function, so the rows an administrator reads are the rows the press performs                                                                                                                                               | `fl_backend/app/api/einladungen/services.py :: plan_einladung_versand`; `fl_backend/tests/api/test_einladung_execution.py :: TestTheSeasonWidePress`                                                                                                                                                                                                                                                                                                                                                                         |
| I280 | The season's mailing passes over a team whose junction row carries an `austritt`, read on the record's presence; the per-team mint does not                                                                                                                                     | `fl_backend/app/api/einladungen/services.py :: plan_einladung_versand`; `fl_backend/tests/api/test_einladung_refusal.py :: TestWhatDecidesASkip`                                                                                                                                                                                                                                                                                                                                                                             |
| I281 | Every row of both answers says whether minting for that team revokes a link it already holds, which no other field of the row tells                                                                                                                                             | `fl_backend/app/api/einladungen/schemas.py :: _EinladungVersandZeile`; `fl_backend/tests/api/test_einladung_refusal.py :: TestWhichTeamsLoseTheLinkTheyHold`                                                                                                                                                                                                                                                                                                                                                                 |
| I282 | The season's mailing answers a row per admitted team whatever happens, one team's failed or unknown mint costing the others neither row nor link                                                                                                                                | `fl_backend/app/api/saisons/admin_router.py :: _mail_one_team`; `fl_backend/tests/api/test_einladung_execution.py :: TestTheSeasonWidePress`; `fl_backend/tests/core/test_request_deadline.py :: TestTheSendPastItsDeadline`                                                                                                                                                                                                                                                                                                 |
| I283 | A pending registration reaches neither `spieler` nor `saison_spieler`: the person and the squad row are written by the admission, in the transaction that deletes it                                                                                                            | `fl_backend/app/api/registrierungen/services.py :: compose_registrierung`, which composes those two collections' fields into one `registrierungen` document, pinned by `fl_backend/tests/api/test_registrierung_submission_execution.py :: TestWhatASubmissionStores`                                                                                                                                                                                                                                                        |
| I284 | Whether a pupil confirmed is composed by the read from the consent record's own stamp, never derived by a consumer: no unconfirmed registration is admissible                                                                                                                   | `fl_backend/app/api/registrierungen/services.py :: registrierung_ist_bestaetigt`, served as `FLRegistrierung.bestaetigt` by `fl_backend/app/api/registrierungen/router.py :: get_registrierungen`; `fl_backend/tests/api/test_registrierung_read.py :: TestWhetherThePupilConfirmed`                                                                                                                                                                                                                                         |
| I285 | A registration's `geburtsdatum` and its whole `einwilligung` record are written by one `$set` and never separately                                                                                                                                                              | `fl_backend/app/api/registrierungen/services.py :: compose_confirmation_update`, swept by `fl_backend/tests/api/test_registrierung_einwilligung_refusal.py :: TestWhatAConfirmationWrites`                                                                                                                                                                                                                                                                                                                                   |
| I286 | The confirmation view answers exactly the fields `READ-REGISTRIERUNG-001` names and no further one, an equality rather than a subset                                                                                                                                            | `fl_backend/tests/api/test_registrierung_einwilligung_refusal.py :: test_the_view_declares_exactly_the_eleven_names_it_may_answer`                                                                                                                                                                                                                                                                                                                                                                                           |
| I287 | A registration's stored birthdate and consent are shown back only where its folded address AND folded name match exactly one person                                                                                                                                             | `fl_backend/app/api/registrierungen/services.py :: persons_named` over `:: sole_person`, swept by `fl_backend/tests/api/test_registrierung_einwilligung_refusal.py :: TestWhoseAnswersThePagePresents`                                                                                                                                                                                                                                                                                                                       |
| I288 | A returning pupil's confirmation re-stamps the record under the label current that day, renewing consent under the words just read                                                                                                                                              | `fl_backend/tests/api/test_registrierung_einwilligung_execution.py :: test_a_returning_pupils_press_restamps_under_the_label_they_just_read`                                                                                                                                                                                                                                                                                                                                                                                 |
| I289 | Each retention pass stamps its own day, and one read answers both, so a stale date beside a fresh one names the pass that stopped                                                                                                                                               | `fl_backend/app/api/registrierungen/services.py :: REGISTRIERUNG_SWEEP_FELD`, spelled once for the filter and the update; `fl_backend/app/api/bewerbungen/sweep_router.py :: get_sweep_saisons`; `fl_backend/tests/api/test_registrierung_sweep_execution.py :: TestThePassRecordsTheDayItRan` and `:: test_the_season_list_answers_the_two_days_apart`                                                                                                                                                                      |
| I290 | An unconfirmed registration is erased the day after its deadline, with no notice before or after                                                                                                                                                                                | `fl_backend/app/api/registrierungen/services.py :: bestaetigung_erasure_is_due`, strictly past the stored `frist`; `fl_backend/tests/api/test_registrierung_sweep_refusal.py :: TestTheDeadlineClock`; `fl_backend/tests/api/test_registrierung_sweep_execution.py :: TestTheDeadlineClock`, which seeds both sides of the day                                                                                                                                                                                               |
| I291 | A registration's one reminder stamps `erinnert_am` and mints a fresh link before the caller mails, keeps the first hash valid, and never moves the deadline                                                                                                                     | `fl_backend/app/api/registrierungen/services.py :: compose_erinnerung_update` and `:: erinnerung_is_due`, which skips an address the provider refused; `fl_backend/tests/api/test_registrierung_sweep_refusal.py :: TestTheReminderMark`; `fl_backend/tests/api/test_registrierung_sweep_execution.py :: TestTheReminderClock`                                                                                                                                                                                               |
| I292 | A registration still `eingereicht` when its season is `past` is erased whatever the pupil answered; the confirmed alone are told afterwards                                                                                                                                     | `fl_backend/app/api/registrierungen/services.py :: undecided_erasure_is_due`; `fl_backend/app/api/registrierungen/sweep_router.py :: erase_the_undecided`, run instead of the reminder and answering `benachrichtigt`; `fl_backend/tests/api/test_registrierung_sweep_execution.py :: TestTheSeasonsOwnEnd`                                                                                                                                                                                                                  |
| I293 | A declined registration is erased one calendar month after the decision, the day clamping to the month's end                                                                                                                                                                    | `fl_backend/app/api/registrierungen/services.py :: decline_erasure_is_due` over `fl_backend/app/api/bewerbungen/services.py :: one_month_after`; `fl_backend/tests/api/test_registrierung_sweep_refusal.py :: TestTheOneMonthClock`; `fl_backend/tests/api/test_registrierung_sweep_execution.py :: TestTheOneMonthClock`                                                                                                                                                                                                    |
| I294 | The hourly pass calls the registration sweep last and on every path, so a throw there leaves the application's day standing                                                                                                                                                     | `fl_frontend/src/features/bewerbungen/sweep.ts :: sweepSaison`; `fl_frontend/src/features/bewerbungen/sweep.test.ts :: "leaves the application's day stamped when the registration half throws"` and `:: "runs for a season whose application half erased nothing at all"`                                                                                                                                                                                                                                                   |
| I295 | An erasing clock drains page after page until one comes back short or owes a message, and raises only where a full page moved nothing                                                                                                                                           | `fl_backend/app/api/registrierungen/sweep_router.py` and `fl_backend/app/core/transactions.py :: refuse_a_stalled_page`; `fl_backend/tests/api/test_registrierung_sweep_refusal.py :: TestTheClocksDrain`, `:: TestThePageIsNeverTruncated` and `fl_backend/tests/api/test_registrierung_sweep_execution.py :: TestAPageAndOneMoreIsDrainedRatherThanRefused`; `fl_backend/app/api/bewerbungen/sweep_router.py :: sweep_saison` and `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestAPageAndOneMoreIsDrained` |
| I296 | A reminder's RAW token exists in this answer and in the message minted from it, and nowhere else: the row stores only hashes                                                                                                                                                    | `fl_backend/app/api/registrierungen/sweep_router.py :: sweep_registrierungen`, swept by `fl_backend/tests/api/test_registrierung_sweep_refusal.py :: test_the_stamp_the_fresh_hash_and_the_kept_hash_and_nothing_else` and driven against a real mongod by `fl_backend/tests/api/test_registrierung_sweep_execution.py :: test_it_stamps_mints_and_answers_one_message_per_registration`                                                                                                                                     |
| I297 | One season's pass erases before it answers, so every note it hands back names a row already gone                                                                                                                                                                                | `fl_backend/tests/api/test_registrierung_sweep_execution.py :: test_it_takes_every_undecided_row_and_tells_the_pupils_who_confirmed` and `:: test_it_erases_the_day_after_the_deadline_and_not_on_it`                                                                                                                                                                                                                                                                                                                        |
| I304 | Entering a referee mints their confirmation link in the create's own transaction; no second control decides whether that person is asked                                                                                                                                        | `fl_backend/app/api/schiedsrichter/admin_router.py :: post_schiedsrichter`; `fl_backend/tests/api/test_schiedsrichter_bestaetigung_execution.py :: TestTheCreateIsTheInvitation`                                                                                                                                                                                                                                                                                                                                             |
| I305 | Every mint replaces `schiedsrichter.bestaetigung` WHOLE, so the previous link dies with the delivery state of the message it went out in                                                                                                                                        | `fl_backend/app/api/schiedsrichter/services.py :: compose_mint_update`; `fl_backend/tests/api/test_schiedsrichter_bestaetigung_execution.py :: TestTheReSend`                                                                                                                                                                                                                                                                                                                                                                |
| I306 | A referee's confirmation writes their birthdate and consent record in one `$set` on their own row and reaches no other collection                                                                                                                                               | `fl_backend/app/api/schiedsrichter/services.py :: compose_confirmation_update`; `fl_backend/tests/api/test_schiedsrichter_bestaetigung_execution.py :: TestAWithheldNameReachesOneCollection`                                                                                                                                                                                                                                                                                                                                |
| I307 | What spends a referee's link is the consent stamp and never a nulled hash, so a second press is refused `REQ-SCHIEDSRICHTER-004` rather than answering `-002`                                                                                                                   | `fl_backend/app/api/schiedsrichter/services.py :: is_confirmed`; `fl_backend/tests/api/test_schiedsrichter_bestaetigung_refusal.py :: TestAnEntryAlreadyConfirmed`                                                                                                                                                                                                                                                                                                                                                           |
| I308 | A referee's deadline is STORED on the block rather than derived, so raising the bound moves no link already in somebody's inbox                                                                                                                                                 | `fl_backend/app/api/schiedsrichter/services.py :: compose_bestaetigung`; `fl_backend/tests/api/test_schiedsrichter_bestaetigung_refusal.py :: TestTheDeadlineIsStoredRatherThanDerived`                                                                                                                                                                                                                                                                                                                                      |
| I309 | A referee is sent no confirmation link while retired or once they have answered, by any mint; a retired referee's corrected address is stored unmailed                                                                                                                          | `fl_backend/app/api/schiedsrichter/services.py :: find_retired_refusal`, `:: find_already_confirmed_refusal`, `:: compose_korrektur_update` and `:: owes_reactivation_mint`; `fl_backend/tests/api/test_schiedsrichter_bestaetigung_refusal.py :: test_a_confirmed_referees_corrected_address_is_stopped_here_and_by_no_refusal` and `fl_backend/tests/api/test_schiedsrichter_bestaetigung_execution.py :: TestTheReSend` and `:: TestTheReactivation`                                                                      |
| I310 | A seat takes another person only where its own holder answered with a Widerspruch, and a claimed pair only where both did                                                                                                                                                       | `fl_backend/app/api/bewerbungen/services.py :: find_reseat_refusal`, over every seat the write reaches; `fl_backend/tests/api/test_bewerbung_triage_refusal.py :: TestSeatingAnotherPersonWhereOneSteppedOut` and `fl_backend/tests/api/test_bewerbung_triage_execution.py :: TestSeatingAnotherPersonInAnEmptiedSeat`, which drives a real erasure before it asks                                                                                                                                                           |
| I302 | While a row written under the marker's earlier spelling survives, every read serves it from either spelling and the validator requires neither                                                                                                                                  | `fl_backend/app/api/spieler/services.py :: build_spieler_memberships_pipeline`; `fl_backend/app/api/spieler/schemas.py :: FLSpielerMembership`; `fl_backend/app/api/spieler/admin_router.py :: _as_junction`; `fl_backend/app/core/constraints.py :: COLLECTION_VALIDATORS`; `fl_backend/tests/api/test_spieler_memberships_read.py`; `fl_backend/tests/api/test_spieler_write_execution.py`; `fl_backend/tests/core/test_constraints.py`                                                                                    |
| I303 | The five sites carrying that leniency go together, `saison_spieler`'s `required` omission among them, in the change that makes the marker required once [`../ops/runbooks.md`](../ops/runbooks.md#12-deleting-this-seasons-player-records-and-resetting-the-action-log) has run | `fl_backend/tests/core/test_nachnominierung_rename.py :: LENIENT` and `:: test_the_marker_is_out_of_saison_spieler_s_required_while_the_leniency_stands`                                                                                                                                                                                                                                                                                                                                                                     |
| I273 | A ban covers the five seasons after the one it was entered under and no more, its row naming the last                                                                                                                                                                           | `fl_backend/app/api/sperrliste/services.py :: compose_gesperrt_bis_saison_id` and `fl_backend/app/api/sperrliste/crud.py :: address_is_gesperrt`, swept by `fl_backend/tests/api/test_sperrliste_lapse_execution.py :: TestTheBoundaryTheCheckReads`; `fl_backend/app/api/saisons/crud.py :: pull_massgebliche_saison_id`, pinned by `fl_backend/tests/api/test_sperrliste_lapse_execution.py :: TestTheSeasonTheBanIsCountedFrom`                                                                                           |
| I274 | A lapsed ban is ERASED with the activation that lapses it, leaving the log no image of the row                                                                                                                                                                                  | `fl_backend/app/api/saisons/admin_router.py :: activate_saison`, swept by `fl_backend/tests/api/test_sperrliste_lapse_execution.py :: TestTheSweepAtAnActivation`; a ban anchors the season the rollover demotes (`fl_backend/app/api/sperrliste/admin_router.py :: _pull_the_season_a_ban_counts_from`), raced by `fl_backend/tests/api/test_sperrliste_isolation.py`                                                                                                                                                       |
| I298 | A send the provider refused is recorded with no message id, so the reminder clocks pass over that address and no event joins it                                                                                                                                                 | `fl_backend/app/api/zustellung/router.py :: abgewiesen_zustellung` over `fl_backend/app/api/zustellung/services.py :: ABGEWIESENER_VERSAND_STAND`; `fl_backend/tests/api/test_zustellung_refusal.py :: TestTheRefusedSend` and `fl_backend/tests/api/test_zustellung_execution.py :: TestTheRefusedSend`                                                                                                                                                                                                                     |
| I329 | Every storing address payload holds the ban key's own address rule; the erasure's lookup holds none, so any stored seat stays erasable                                                                                                                                          | `fl_backend/app/shared/schemas/kontakt.py :: CustomEmail`, `fl_backend/app/api/kontakte/schemas.py :: FLKontaktErasurePayload`; `fl_backend/tests/api/test_address_rule_refusal.py`, `fl_backend/tests/api/test_kontakt_erasure_execution.py :: test_a_seat_stored_under_an_address_no_payload_takes_now_is_erased`, `fl_frontend/src/features/kontakte/schemas.test.ts`                                                                                                                                                     |
| I330 | A recorded probe keys as recorded, or is refused where recorded null; the record is only appended to, so a moved ban key goes red                                                                                                                                               | `fl_backend/app/shared/folding.py :: canonical_address`; `fl_backend/tests/shared/test_email_address.py :: test_every_ban_key_is_the_one_recorded` over `fl_backend/tests/shared/ban_keys.json`                                                                                                                                                                                                                                                                                                                              |
| I331 | A contact erasure reaches exactly the seats and log images whose stored address shares the asker's sign-in fold                                                                                                                                                                 | `fl_backend/app/api/kontakte/services.py :: same_address`, `:: find_matching_slots`, `:: rows_naming`, `:: images_holding`; `fl_backend/tests/api/test_kontakt_erasure_execution.py`                                                                                                                                                                                                                                                                                                                                         |
| I332 | An address payload stores an ASCII local part and a punycode domain, converting a typed Unicode domain, and refuses a local part above ASCII                                                                                                                                    | `fl_backend/app/shared/folding.py :: league_address`, mirrored by `fl_frontend/src/core/emailAddress.ts :: isDeliverableAddress`; both held to `fl_backend/tests/shared/email_addresses.json`                                                                                                                                                                                                                                                                                                                                |
| I333 | A stored address compares on the sign-in fold, so an address asked for with its domain in Unicode joins the punycode spelling every payload stores                                                                                                                              | `fl_backend/app/shared/folding.py :: sign_in_identifier`, `fl_frontend/src/core/emailAddress.ts :: asSignInIdentifier`; `fl_backend/tests/shared/test_email_address.py`, `fl_backend/tests/api/test_identitaet_subjekt_execution.py`                                                                                                                                                                                                                                                                                         |
| I340 | No payload creates or saves a referee without an email address: it is the one route by which the person learns an administrator entered them                                                                                                                                    | `fl_backend/app/shared/schemas/kontakt.py :: FLKontaktPayload`; `fl_frontend/src/shared/schemas.ts :: FLKontaktPayloadSchema`; `fl_backend/tests/shared/test_kontakt.py :: TestTheAddressIsRequired`; `fl_frontend/src/features/schiedsrichter/einladen.test.ts`                                                                                                                                                                                                                                                             |
| I338 | A consent record's `medien` is `true` only where its person had reached `MEDIEN_MIN_AGE_YEARS` on the day the record was written                                                                                                                                                | `fl_backend/app/api/registrierungen/services.py :: find_medien_refusal` and `fl_backend/app/api/schiedsrichter/services.py :: find_medien_refusal`, swept by `fl_backend/tests/api/test_registrierung_einwilligung_refusal.py :: TestTheMediaAge` and `fl_backend/tests/api/test_schiedsrichter_bestaetigung_refusal.py :: TestTheMediaAge`, wired at the endpoint by each `*_execution.py :: TestTheMediaAge`                                                                                                               |
| I334 | A squad row's `ist_nachnominiert` is derived at its create by the test the invite's read answers; no payload carries it and no write moves it                                                                                                                                   | `fl_backend/app/api/spieltage/crud.py :: nachnominierung_laeuft_in`; `fl_backend/tests/api/test_spieler_write_execution.py :: TestTheCreateDerivesTheLateEntryMarker`; `fl_backend/tests/api/test_spieler_write_execution.py :: test_an_edit_leaves_the_marker_as_the_create_stored_it`; `fl_backend/tests/api/test_spieltage_nachnominierung.py :: TestThePeriodIsReadOffTheFirstPhasesMatchdayOne`; `fl_backend/tests/api/test_spieler_memberships_read.py :: test_no_payload_declares_the_marker`                         |
| I335 | The Nachnominierung period opens on the first day of the first phase's matchday 1, never a fixture's date, and not while that matchday is undated                                                                                                                               | `fl_backend/app/api/spieltage/services.py :: nachnominierung_laeuft`; `fl_backend/app/api/spieltage/services.py :: build_erster_spieltag_filter`; `fl_backend/tests/api/test_spieltage_nachnominierung.py :: TestTheNachnominierungPeriodOpensOnMatchdayOnesFirstDay`                                                                                                                                                                                                                                                        |
| I320 | Every request's database work, its transactions included, ends within `fl_backend/app/core/middlewares.py :: REQUEST_DEADLINE_S`, positive and the only deadline set                                                                                                            | `fl_backend/app/core/middlewares.py :: TraceContextMiddleware`; `fl_backend/tests/core/test_request_deadline.py :: TestTheRequestDeadlineIsTheOnlyOne`, whose db-tier `:: TestATransactionPastTheRequestDeadlineCommitsNothing` holds the provisional `pymongo.timeout` across an upgrade; counterparts `docs/frontend/spec.md :: I362` and `:: I366`                                                                                                                                                                        |
| I321 | A commit the driver labels of unknown outcome, or a write request the deadline cuts, answers `DB-FAIL-002`, erring toward unknown where nothing was sent                                                                                                                        | `fl_backend/app/core/exception_handlers.py :: db_exception_handler` and `:: stores_nothing`; `fl_backend/tests/core/test_request_deadline.py :: TestACommitOfUnknownOutcomeIsNotCalledFailed`, `:: TestAWriteTheDeadlineCutIsNotCalledFailed` and `:: TestADeadlineCuttingARequestThatStoresNothingIsAFailure`                                                                                                                                                                                                               |
| I322 | Each capped sweep loop takes a fixed share per call, leaving the rest due: a whole page past the deadline would write nothing, every call                                                                                                                                       | `fl_backend/app/api/registrierungen/sweep_router.py :: REMINDERS_PER_PASS`, `fl_backend/app/api/bewerbungen/sweep_router.py :: REMINDERS_PER_PASS` and `:: BLOCKS_CLEARED_PER_PASS`, `fl_backend/app/api/bewerbungen/schemas.py :: DELETIONS_LISTED_PER_PASS`; `fl_backend/tests/api/test_registrierung_sweep_execution.py :: TestTheReminderClockTakesAShareEachCall`, `fl_backend/tests/api/test_bewerbung_sweep_execution.py :: TestEachCappedClockMakesProgressAcrossPasses`                                             |
| I323 | A sweep call runs the transaction whose rows it hands back last, so no later step's error answers after a commit owing a message                                                                                                                                                | `fl_backend/app/api/registrierungen/sweep_router.py :: sweep_registrierungen` and `fl_backend/app/api/bewerbungen/sweep_router.py :: sweep_saison`; `fl_backend/tests/api/test_registrierung_sweep_execution.py :: TestAnswerAndMessagesCommitTogether`                                                                                                                                                                                                                                                                      |
| I324 | A season write drops the cache however it ends, a raised write included: one whose answer was lost may have landed                                                                                                                                                              | `fl_backend/app/api/saisons/cache.py :: dropping_the_saison_cache`; `fl_backend/tests/api/test_saison_cache.py :: TestEverySeasonWriteDropsIt` and `:: TestTheDropRunsHoweverTheWriteEnds`                                                                                                                                                                                                                                                                                                                                   |
| I345 | Every operation publishes `FLFailureBody` for its failures, and each taking input `FLRefusedPayloadBody` for its 422, never FastAPI's own                                                                                                                                       | `fl_backend/app/main.py :: with_failure_bodies` and `:: with_refusals`; `fl_backend/tests/api/test_error_responses.py :: TestThePublishedFailureBodies`; `fl_frontend/src/core/apiContract.test.ts` against the `fl_frontend/src/core/schemas.ts` mirrors                                                                                                                                                                                                                                                                    |
| I346 | A public submission whose `Idempotency-Key` is already stored answers with that row and stores none; the same key over other details is refused                                                                                                                                 | `fl_backend/app/api/bewerbungen/public_router.py :: post_bewerbung`, `fl_backend/app/api/registrierungen/public_router.py :: post_registrierung` and `uniq_bewerbung_idempotenz_schluessel` beside `uniq_registrierung_idempotenz_schluessel` in `fl_backend/app/core/constraints.py :: UNIQUE_INDEXES`; `TestTheSubmissionKey` in `fl_backend/tests/api/test_bewerbung_submission_execution.py` and `fl_backend/tests/api/test_registrierung_submission_execution.py`                                                       |
| I347 | A replay mints fresh links only while nothing is answered or reminded and no mail is on record but a refused or suppressed one                                                                                                                                                  | `fl_backend/app/api/bewerbungen/services.py :: build_wiederholung_filter` and `fl_backend/app/api/registrierungen/services.py :: build_wiederholung_filter`, over `fl_backend/app/api/bewerbungen/services.py :: ZUSTELLUNG_NICHT_ANGEKOMMEN`; the `TestTheSubmissionKey` classes I346 cites, one case per such state                                                                                                                                                                                                        |
| I349 | A key lives as long as its row: a retention sweep ends either kind, and admission ends a registration's, deleting its row (I283)                                                                                                                                                | `fl_backend/app/core/constraints.py :: _IDEMPOTENZ_PROPERTIES`, the key's only home, which no read serves; `fl_backend/tests/api/test_registrierung_submission_execution.py :: test_the_key_ends_with_its_row`                                                                                                                                                                                                                                                                                                               |
| I350 | The `Idempotency-Key` value is a bare UUID v4, Stripe's form and not the IETF draft's quoted structured-field string, which answers 422                                                                                                                                         | The `idempotency_key` header parameter of `fl_backend/app/api/bewerbungen/public_router.py :: post_bewerbung` and `fl_backend/app/api/registrierungen/public_router.py :: post_registrierung`; `test_a_key_that_is_not_version_4_is_a_422_storing_nothing` in `fl_backend/tests/api/test_bewerbung_submission_execution.py` and in `fl_backend/tests/api/test_registrierung_submission_execution.py :: TestTheKeyOverTheWire`                                                                                                |
| I327 | An operation storing nothing whatever its method publishes `x-fl-stores-nothing`: `true`, or the boolean query flag under which it stores nothing                                                                                                                               | `fl_backend/app/main.py :: publish_stores_nothing`, from `fl_backend/app/core/exception_handlers.py :: stores_nothing` and `:: stores_nothing_when`; `fl_backend/tests/core/test_request_deadline.py :: TestTheDeclarationIsPublished`                                                                                                                                                                                                                                                                                       |
| I328 | A mailing's team row whose commit the driver labels of unknown outcome says `erzeugung_ungewiss`                                                                                                                                                                                | `fl_backend/app/api/saisons/admin_router.py :: _mail_one_team`; `fl_backend/tests/core/test_request_deadline.py :: TestTheSendPastItsDeadline`                                                                                                                                                                                                                                                                                                                                                                               |
| I357 | A status is published where a rule or the route's declaration names it, its enum holding exactly the codes those name                                                                                                                                                           | `fl_backend/app/main.py :: publish_refusals`; `fl_backend/tests/api/test_error_responses.py :: TestThePublishedFailureBodies` and `:: TestTheDeclared409`; each rule's status held to its check's by `fl_backend/tests/core/test_domain.py :: test_every_rule_is_answered_at_the_status_it_declares`                                                                                                                                                                                                                         |
| I358 | A route's 409 declares `DB-COMMON-002` exactly where it reaches an insert or update on a collection `UNIQUE_INDEXES` covers or naming its `_id`                                                                                                                                 | `fl_backend/tests/core/test_duplicate_key_publication.py`, tracing each route to the writes it makes; a batch's refusal by `fl_backend/tests/core/test_crud.py :: TestPostManyToDb`                                                                                                                                                                                                                                                                                                                                          |
| I369 | A route's 404 declares `DB-COMMON-001` exactly where its handler can raise `DocumentNotFoundException` without absorbing it                                                                                                                                                     | `fl_backend/tests/core/test_not_found_publication.py`, tracing each route to the raises it reaches                                                                                                                                                                                                                                                                                                                                                                                                                           |
| I370 | Every operation publishes, at its status, each code a dependency it runs answers before the handler: a key's 401, an actor's 400, the database's 503                                                                                                                            | `fl_backend/app/main.py :: DEPENDENCY_REFUSALS`, held to what a request meets by `fl_backend/tests/api/test_dependency_refusals.py`                                                                                                                                                                                                                                                                                                                                                                                          |

## 3. Violation → remedy

| Symptom                                                                                                     | Cause                                                                                                                                                                                       | Remedy                                                                                                                                                                                                                                                                               |
| ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| League table does not change after a result edit                                                            | A stale frontend cache, not the backend                                                                                                                                                     | The table is recomputed per request (I1); check that the Spiel action still invalidates `teams`                                                                                                                                                                                      |
| A forfeited fixture counts in the table                                                                     | Working as intended — a no-show carries the `ergebnis` composed for it, and that is the whole rule (I1a, I3a)                                                                               | Nothing. Record `ausgefallen` or `annulliert` instead if it should not count                                                                                                                                                                                                         |
| A semi-final lost its result after a QF edit                                                                | Working as intended — its occupant changed (I23)                                                                                                                                            | Nothing. The goals were scored by a team the fixture does not hold; re-enter the new one                                                                                                                                                                                             |
| A winner did not reach the next fixture                                                                     | Its `Quelle` is `null`, or names a `spiel_nr` the season does not have                                                                                                                      | Set the source on that side in the admin form; the toast names every fixture the save moved (I23)                                                                                                                                                                                    |
| A knockout slot stays empty and nothing moves it                                                            | One of the derived bracket faults (I28) — the action-required list names which, and why                                                                                                     | Correct the source it names, or clear the `Quelle` and enter a side by hand                                                                                                                                                                                                          |
| A group-seeded slot is still empty                                                                          | Some result in the group could still change it, or the group is past `CERTAINTY_FIXTURE_LIMIT` and the sound test declined it (I24a)                                                        | Nothing. It fills itself once the group can go no other way and the layer deciding it can prove that                                                                                                                                                                                 |
| A group-seeded slot was reported, not filled                                                                | The `platz` exceeds what the group can produce, or the chain cannot separate it (I24c)                                                                                                      | Fix the `platz`, or take the slot over by choosing "Manuell gesetzt" and entering a side                                                                                                                                                                                             |
| A fault's toast was missed                                                                                  | Nothing stores it, by design (I28)                                                                                                                                                          | Open the action-required list, which re-derives every fault on every request                                                                                                                                                                                                         |
| A seeded slot changed after a forfeit was awarded                                                           | Working as intended — a fixture that awarded nothing was not outstanding (I24a)                                                                                                             | Nothing. The forfeit's save re-resolved the bracket against the standing it changed                                                                                                                                                                                                  |
| A team cannot be entered on a bracket side                                                                  | The side has a `Quelle`, and the write path refuses a hand-set team beside one (I27)                                                                                                        | Choose "Manuell gesetzt" on that side, and the slot is yours                                                                                                                                                                                                                         |
| A save comes back 409 with `REQ-WIRING-001`                                                                 | The form was built against a season that has moved — a second tab or another admin                                                                                                          | Reload the page and repeat the edit against the current bracket (I27)                                                                                                                                                                                                                |
| A save comes back 409 with `REQ-WIRING-002`                                                                 | The save pointed a side at a group placing, and this season's own fixtures open its bracket on an earlier round (I27)                                                                       | Reloading changes nothing — the season cannot have moved (§1.4). Point that side at an earlier match, or choose "Manuell gesetzt" and enter a team                                                                                                                                   |
| A save comes back 409 with `REQ-WIRING-003`                                                                 | The save pointed a side at a placing in a group this season does not run (I27)                                                                                                              | Reload the page — the picker offers the season's own groups. Then pick one the season runs, or choose "Manuell gesetzt" and enter a team                                                                                                                                             |
| A save comes back 409 with `REQ-ELIGIBILITY-001`                                                            | The team has left this season, and this save changed an input the rule reads (I100)                                                                                                         | Pick another team, or record the walkover — any phase, a knockout needing both sides resolved (`REQ-STATE-003`). Calling off is open on a Gruppenphase fixture                                                                                                                       |
| A cleared `sonderereignis` comes back 409 with `REQ-ELIGIBILITY-001`                                        | The club standing on the fixture has left the season, and the event was its exemption (I100)                                                                                                | Leave the event recorded, or take the departed club off the fixture first                                                                                                                                                                                                            |
| A save comes back 409 with `REQ-ELIGIBILITY-002`                                                            | The team holds no `saison_teams` row for this season, so it is not in the competition                                                                                                       | Add the junction row, or pick a team that is in the season (I11)                                                                                                                                                                                                                     |
| A save comes back 409 with `REQ-SPIELTAG-001`                                                               | The team already plays this Spieltag on a side the resolution maintains (I30)                                                                                                               | Clear that fixture's `Quelle` on the occupied side, or pick another team                                                                                                                                                                                                             |
| A save comes back 409 with `REQ-SPIELTAG-002`                                                               | The change would advance a club into two fixtures of one Spieltag, and the resolution fills both sides itself (I30)                                                                         | Give one of the two sides a different `Quelle`, or take a hand-set team out of either fixture                                                                                                                                                                                        |
| A save comes back 422 with `REQ-STATE-002`                                                                  | The fixture was marked `ausgefallen` or `annulliert` while goals still stood in the form                                                                                                    | Clear both goal counts first, then save the state                                                                                                                                                                                                                                    |
| A save comes back 422 with `REQ-STATE-003`                                                                  | A no-show was chosen on a fixture one of whose sides is still empty                                                                                                                         | Fill both sides, or wait for the bracket to resolve them                                                                                                                                                                                                                             |
| A save comes back 409 with `REQ-BOOKING-001`                                                                | The venue or referee chosen, or kept on a fixture this save made unplayed again, is in no record or is retired                                                                              | Pick one the list offers, reactivate a retired one, or leave the fixture called off or scored. Any other edit keeping the reference still saves                                                                                                                                      |
| A fixture was emptied by a save that never named it                                                         | Working as intended — a team was fielded elsewhere on its Spieltag (I30)                                                                                                                    | Nothing. The response's `released_sides` names every one, and the toast offers 15s to undo (I29)                                                                                                                                                                                     |
| A warning named fixtures that lost nothing                                                                  | It cannot: the warning is a dry run of the write path, not a guess (I29)                                                                                                                    | Check that the preview actually answered — no answer renders no banner, which is not "no risk"                                                                                                                                                                                       |
| A level knockout advanced nobody                                                                            | No shoot-out is recorded against it, so it has no winner (I25)                                                                                                                              | Enter the shoot-out on that match; the slot fills on that save                                                                                                                                                                                                                       |
| A shoot-out did not change the league table                                                                 | Working as intended — it is a draw for every derived figure (I25a)                                                                                                                          | Nothing. Only the bracket takes a winner from a shoot-out                                                                                                                                                                                                                            |
| A shoot-out vanished after an unrelated edit                                                                | Its fixture's occupant changed, so the whole result went with it (I25b)                                                                                                                     | Re-enter the result and the shoot-out for the side that is in the fixture now                                                                                                                                                                                                        |
| A fixture still to be played is reported on a retired venue or referee                                      | A result correction, or a side emptied for a Spieltag clash (§1.3 step 3a), voided its result and kept the booking (I257)                                                                   | Book another, or reactivate the row. Entering the replay's result there clears it too                                                                                                                                                                                                |
| A fixture is reported double-booked after a result correction                                               | The correction lifted a no-show whose freed slot another fixture has since taken (I257)                                                                                                     | Move one of the two. A note or a result on either still saves; moving either to another hour inside the buffer is refused `REQ-CLASH-001`                                                                                                                                            |
| A fixture still to be played is reported on an erased referee                                               | A correction, or a Spieltag clash's emptied side (§1.3 step 3a), reopened it while it stood on the ghost, a retired row (I259)                                                              | Book whoever officiates it. The ghost is never reactivated, so a fresh booking is the only way out                                                                                                                                                                                   |
| `GET /spiele/action_required` fails for a season whose own fixtures are well-formed                         | A fixture of another season, booked on one of its venues or referees within a day, stores a malformed `uhrzeit` (I257)                                                                      | Correct that fixture's stored time at the database                                                                                                                                                                                                                                   |
| A team's page and the Saisontabelle disagree                                                                | Working as intended — two scopes (I1c)                                                                                                                                                      | Nothing. The page counts every phase, the table counts the Gruppenphase                                                                                                                                                                                                              |
| Every team's table reads zero                                                                               | A season resolved to one with no matches                                                                                                                                                    | Check `saison_id`; an unknown season 404s rather than returning an empty list                                                                                                                                                                                                        |
| A planned season is missing from every public page                                                          | Working as intended — it is `future`, and the base tier is served neither the season nor anything scoped to it (I47)                                                                        | Nothing. The admin surfaces read the `/list/admin` twins, and activating the season publishes it (I18)                                                                                                                                                                               |
| Venue rent becomes 0 after an unrelated edit                                                                | A Pydantic default was added to `mietpreis`                                                                                                                                                 | Remove it (I6)                                                                                                                                                                                                                                                                       |
| A team vanishes from `/teams`                                                                               | No `saison_teams` row for that season                                                                                                                                                       | Create the junction row (I11)                                                                                                                                                                                                                                                        |
| `/dashboard/saisontabelle` fails to load                                                                    | A key missing for a group the season offers                                                                                                                                                 | I10 has regressed: every group the season offers keeps its key                                                                                                                                                                                                                       |
| A create comes back 409                                                                                     | A unique index still holds the key                                                                                                                                                          | The retired row keeps its slot on purpose (I20). Reactivate it, or choose another key                                                                                                                                                                                                |
| A retire comes back 409 with `REQ-RETIRE-001`                                                               | The club is entered in a running or planned season                                                                                                                                          | Wait for the season to end, or leave the club active — a season is left only by an `austritt`                                                                                                                                                                                        |
| A season patch comes back 409 with `REQ-RULES-012`                                                          | The patch moved `rules.tiebreak_order` on a season holding a knockout fixture that has taken place — the window `REQ-SWAP-002` reads (I38)                                                  | Leave the tie-break as stored and the rest of the patch goes through, judged on the move alone (I44). The order itself does not reopen                                                                                                                                               |
| A confirmed replace comes back 409 with `REQ-SPIELPLAN-005`                                                 | The season is outside the replace's window — not `future`, or holding a fixture with one of I109's records against it                                                                       | Reload — the panel gates the press on those same two figures. Nothing returns a season to `future` (I18), so the status half never reopens                                                                                                                                           |
| An undraw comes back 409 with `REQ-SPIELPLAN-006`                                                           | The same window, which the undraw shares with the replace (I109)                                                                                                                            | Reload, exactly as for `REQ-SPIELPLAN-005` — the same figures gate both presses                                                                                                                                                                                                      |
| A rollover comes back 409 with `REQ-ACTIVATE-001`                                                           | The outgoing season still holds fixtures with no result and no event that awards none (I18)                                                                                                 | Enter those results, or record the event that called each one off, then roll over                                                                                                                                                                                                    |
| A rollover comes back 409 with `REQ-ACTIVATE-002`                                                           | The target season is already `past`, and promoting it would reopen the points, the groups and the table derived from them (I18)                                                             | Nothing. A season closed by mistake is repaired at the database                                                                                                                                                                                                                      |
| A rollover comes back 409 with `REQ-ACTIVATE-003`                                                           | The target season holds no fixtures, and activation writes `status` one way (I18)                                                                                                           | Draw its Spielplan, then roll over                                                                                                                                                                                                                                                   |
| A rollover comes back 409 with `REQ-ACTIVATE-004`                                                           | One or more of the target season's matchdays carry no `beginn`, so the league would go live without a calendar (I18)                                                                        | Date every matchday, then roll over                                                                                                                                                                                                                                                  |
| An entry comes back 409 with `REQ-ENTER-001`                                                                | The season is running or past — a team enters a season only while it is `future`                                                                                                            | Enter the team into the next planned season instead                                                                                                                                                                                                                                  |
| An entry or move comes back 409 with `REQ-ENTER-002/003`                                                    | The group is outside `rules.number_of_groups`, or already holds `rules.teams_per_group` rows                                                                                                | Pick a group the season runs that still has space; a departed team's place stays taken                                                                                                                                                                                               |
| An entry comes back 409 with `REQ-ENTER-005`                                                                | The club has left the LEAGUE, so no season can take it                                                                                                                                      | Reactivate it through `POST /teams/{team_id}/reactivate`, then enter it                                                                                                                                                                                                              |
| A submission comes back 409 with `REQ-BEWERBUNG-004`                                                        | The season's window shut or the season ended after the page loaded, or it named a season with none — the form is a snapshot                                                                 | Reload the page: it re-reads the window and says the deadline has passed rather than offering a form the write refuses                                                                                                                                                               |
| A submission comes back 409 with `REQ-BEWERBUNG-008`                                                        | Another school took the Kürzel between the availability check and the submit; the check narrows that collision and cannot close it (`fl_backend/app/core/constraints.py :: uniq_shorthand`) | Propose a different code. The write is where the answer is authoritative, and acceptance holds it a second time                                                                                                                                                                      |
| Acceptance comes back 409 `REQ-BEWERBUNG-013`                                                               | A contact person has not confirmed their own seat, or answered it with a `Widerspruch` (I143)                                                                                               | Wait, or re-send that seat's link from the application's page; a seat holding a `Widerspruch` cannot be completed as the application stands                                                                                                                                          |
| A club's Gruppe picker is locked mid-season                                                                 | Working as intended — a single move leaves its fixtures against the group it left (I38)                                                                                                     | Swap it with a club of the target group, on `/admin/saisons/[saison_id]`                                                                                                                                                                                                             |
| A swap comes back 409 with `REQ-SWAP-001`                                                                   | The pair is not two clubs of that season standing in different groups (I38)                                                                                                                 | Reload the season editor; the pickers only offer pairs the endpoint accepts                                                                                                                                                                                                          |
| A swap comes back 422 with `REQ-SWAP-007`                                                                   | Both ids name one club                                                                                                                                                                      | Pick two different clubs; no season makes one club a pair                                                                                                                                                                                                                            |
| A swap comes back 409 with `REQ-SWAP-002`                                                                   | A knockout fixture of that season was played, abandoned, forfeited, given a goal count or a stored shoot-out (I38)                                                                          | Nothing. The bracket was seeded from these groups, and no later edit reopens the window                                                                                                                                                                                              |
| A swap comes back 409 with `REQ-SWAP-003`                                                                   | The season is `past` (I38)                                                                                                                                                                  | Nothing. A finished season's table is derived from its groups, so they are frozen with its rules                                                                                                                                                                                     |
| A swap comes back 409 with `REQ-SWAP-004`                                                                   | One of the two clubs has a Gruppenphase fixture played, abandoned, forfeited, scored or holding a stored shoot-out (I38)                                                                    | Nothing. A group is a round robin, and a club that has played inside one belongs to it                                                                                                                                                                                               |
| A swap comes back 409 with `REQ-SWAP-005`                                                                   | The exchange would BREAK a Spieltag, leaving a club in two of its matches (I38, I30)                                                                                                        | Clear the bracket side's manual pick on one of the two fixtures; nothing moves a fixture to another Spieltag, `spieltag_id` being on no payload                                                                                                                                      |
| A swap comes back 409 with `REQ-SWAP-006`                                                                   | The exchange would field a departed club on a group fixture dated on or after its exit, or undated, recording no absence for it (I38)                                                       | Lift the `austritt` on that club's junction row, swap, then record it again (I31)                                                                                                                                                                                                    |
| A swap moved a club's fixtures to the other club                                                            | Working as intended — each club inherits the other's opponents, dates and venues (I38)                                                                                                      | Nothing. Swapping the same pair again restores every one of them                                                                                                                                                                                                                     |
| A replacement comes back 409 with `REQ-REPLACE-001`                                                         | The season is `past`, and its fixtures and the table derived from them are the record of who played                                                                                         | Nothing. A junction row changes hands only in a running or planned season (I19), and the panel explains rather than offering the press                                                                                                                                               |
| A replacement comes back 409 with `REQ-REPLACE-002`                                                         | A fixture the outgoing club holds in this season has taken place, and a replacement would credit it to the incoming club                                                                    | Record an `austritt` on that club's junction row instead: the club leaves and keeps its fixtures, and that patch judges nothing (I31)                                                                                                                                                |
| A replacement comes back 409 with `REQ-REPLACE-003`                                                         | The incoming club already holds a row in this season                                                                                                                                        | Pick a club holding no row here; a club that withdrew still holds one (I31)                                                                                                                                                                                                          |
| A replacement comes back 422 with `REQ-REPLACE-004`                                                         | The incoming club is the club being replaced                                                                                                                                                | Pick another club to take the row over                                                                                                                                                                                                                                               |
| A replacement leaves the season with no way to reach the team                                               | Working as intended — the kit colour and the contact records described the OUTGOING school, so the replacement cleared them (I19, I137)                                                     | PATCH `/teams/{team_id}/saisons/{saison_id}/kontakte` with the incoming school's own three people, which is also when their consent is recorded                                                                                                                                      |
| A public team read serves no kit colour and no contacts                                                     | Working as intended — the junction join allow-lists what it carries back, and those fields are on no base-tier response (I50)                                                               | Read them from `GET /teams/memberships`, the admin surface that owns them                                                                                                                                                                                                            |
| A squad row's reactivate comes back 409 with `REQ-SQUAD-001`                                                | Its club does not stand in the season — the state a REPLACEMENT leaves behind (I19)                                                                                                         | Reassign the row through `PATCH /spieler/{spieler_id}/saisons/{saison_id}` to a club the season holds, then reactivate. Putting the old club back is not the route (`REQ-ENTER-001`)                                                                                                 |
| A squad write comes back 409 with `REQ-SQUAD-004`                                                           | Another live row of the squad the write lands in already holds the `rolle` — on a transfer that is the club being joined (§1.1)                                                             | Take the role off whoever holds it, or retire that row, and save again. A reactivate carries the role its row was retired with                                                                                                                                                       |
| A retired venue is missing from an admin picker                                                             | The default read filters it out                                                                                                                                                             | Pass `include_inactive=true` — a switch, not a value to match on                                                                                                                                                                                                                     |
| A German name list is in the wrong order — „Ö“ after „Z“, every lower-case initial after every capital      | The read was issued without `GERMAN_COLLATION` (I54)                                                                                                                                        | Attach it to the `aggregate()` or `find()` at the call site, never to the `$sort` stage; a paginated read cannot be repaired on the client                                                                                                                                           |
| A team that left the season still appears in the table                                                      | Working as intended — it keeps its row and only loses its placing (I24b)                                                                                                                    | Nothing. The placings walk past it and the team below takes the place                                                                                                                                                                                                                |
| A PATCH to a junction row comes back 422                                                                    | The payload defaults none of its three writable fields and the form omitted one, or the body carried `kontakte`, which `extra="forbid"` refuses here (I31, I49)                             | Send all three of `gruppe`, `austritt` and `trikot_farbe`, `null` where nothing is to be recorded, and the contact block to its own endpoint                                                                                                                                         |
| An erasure comes back 409 with `REQ-PURGE-001`                                                              | The player is still in the league, retirement being the erasure's precondition rather than its trigger (I12)                                                                                | Retire them first through `DELETE /spieler/{spieler_id}`, which stamps `inactive_since` and judges nothing, then erase                                                                                                                                                               |
| A person's name is refused with 422                                                                         | The write payloads allow letters, space, hyphen and apostrophe alone (I36), so an initial or a title carries a refused `.`                                                                  | Spell the name without it                                                                                                                                                                                                                                                            |
| 401 with `REQ-AUTH-002`                                                                                     | Wrong or missing `base` key                                                                                                                                                                 | Check `INTERNAL_API_KEY_BASE` matches on both sides                                                                                                                                                                                                                                  |
| A junction contact edit comes back 422 naming `erfasst_von`, `bestaetigt_am` or `geburtsdatum`              | The editor sent the provenance or the birthdate, which no payload accepts (I141, I142)                                                                                                      | Send the four contact fields and the consent's `umfang`, `text_version` and `datum`; the server composes the rest per seat                                                                                                                                                           |
| A contact seat holds a birthdate beside no confirmation stamp                                               | The date rides with the person's address and is theirs to enter at their confirmation (I141)                                                                                                | [`../ops/runbooks.md`](../ops/runbooks.md#11-a-contact-seats-birthdate-that-no-confirmation-stamped) finds them on both collections and clears them at the database; no save can, `fl_backend/app/api/teams/services.py :: _geburtsdatum_held_by` carrying every stored date forward |
| An admin write comes back 400 with `REQ-AUTH-005`                                                           | The request reached the backend with no well-formed `X-FL-Actor`, so the write could be attributed to nobody (I41)                                                                          | Sign in again — the header carries the session's address, and nginx discards any a client sends                                                                                                                                                                                      |
| 503 with `Retry-After: 30`                                                                                  | Database unavailable                                                                                                                                                                        | `DB-CONN-001` — check MongoDB                                                                                                                                                                                                                                                        |
| A contacts save comes back 409 with `REQ-KONTAKT-001`                                                       | The row's contact block moved between the editor's read and the save, an erasure being the usual cause (I192)                                                                               | Reload the contacts page and enter the change again; the reloaded block is what the next save is judged against                                                                                                                                                                      |
| A second ban of an address the list already holds is accepted                                               | `SPERRLISTE_SCHLUESSEL` was replaced, so every stored hash stands under a value nothing computes now (I268)                                                                                 | Restore the previous value, or clear the list and enter the bans again from whatever record names the addresses: nothing recovers them from the rows                                                                                                                                 |
| A referee who answered their link still publishes as „anonym“, or a withheld one still publishes their name | The confirmation's caller did not drop the `spiele` tag (I306)                                                                                                                              | Call `revalidateTag("spiele", { expire: 0 })` in the route handler (`docs/frontend/spec.md :: I14`); without the option the recommended profile serves the stale entry once more                                                                                                     |

## 4. Known-open

| Item                                                                     | State                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Not every handler body has a direct test                                 | Narrowed — the transactional write paths, the reference fan-outs and both CRUD helpers are exercised directly. Uncovered: the remaining handler bodies, and the aggregation-inside-a-transaction path                                                                              |
| The database user needs `collMod`                                        | Accepted — `collMod` is a `dbAdmin` action, so a user can build every index, attach no validators, and the app then refuses to start (I15)                                                                                                                                         |
| Cross-document rules are reported, never applied                         | Accepted — `report_relations` counts what two rules no validator expresses are broken by (I30, I28); re-run `python -m app.core.constraints --check` after a hand edit                                                                                                             |
| Two rules refusals cannot be reached                                     | Accepted — `REQ-RULES-004` and `REQ-RULES-006` are unreachable, `REQ-RULES-011` answering first (I44). They stay declared against the freeze being lifted; no test asserts the unreachability                                                                                      |
| OpenAPI carries no service-level prose                                   | Open — every endpoint has a `summary` and a docstring; the app declares no `title` or `description`. The Swagger UI is routed nowhere ([`docs/ops/spec.md`](../ops/spec.md) I134)                                                                                                  |
| A single read exists whether or not something calls it                   | Accepted — a callerless `GET /{id}` stays, an id being answerable at its resource whatever handed it over (§1.1)                                                                                                                                                                   |
| Nothing purges a retired row                                             | Accepted — a retired row is never purged on its age; the decision stands at `fl_backend/app/core/domain.py :: UNENFORCED`, the one removal a pupil's erasure (I12)                                                                                                                 |
| A write and its log row can commit apart                                 | Open — a write and its log row commit apart where no transaction is open; a failed insert answers 500 for a committed write (I52)                                                                                                                                                  |
| A German list is ordered at the cost of one index                        | Accepted — `build_team_pipeline`'s `$lookup` on a string `saison_id` plans `COLLSCAN` rather than `IXSCAN uniq_saison_id_team_id` under `GERMAN_COLLATION` (I54). Bounded: one junction row per club per season                                                                    |
| The retirement match scans `saisons` under the German collation          | Accepted — `build_retirement_stages` reads the season's `status` through one scan of `saisons` per read rather than `_id_`, measured 2026-09-14 (I54, I252)                                                                                                                        |
| The season patch judges more than its transaction covers                 | Narrowed — `fl_backend/app/api/saisons/admin_router.py :: patch_saison` re-judges outside its session (I118) and a rival's `bounded_writes` conflicts on the season; a match write takes none and slips                                                                            |
| The draw, the undraw and the group swap have no case driving their reads | Narrowed — those three sit in `fl_backend/app/api/saisons/admin_router.py`; `fl_backend/tests/core/test_write_shapes.py` holds every in-transaction read's `session=` and `fl_backend/tests/core/test_bounded_writes.py` a helper's own, neither proving a read joins the snapshot |
| Nothing sweeps `has_taken_place`'s four inline field sets                | Open — no constant to sweep, so a field the predicate learns to weigh reads `None` in all four and four refusals go quiet together                                                                                                                                                 |
| The junction's own ids are declared and unmirrored                       | Accepted — `saison_teams` has no row model, so its `_id` and `team_id` reach the bsonType sweep by no route (I17, I140)                                                                                                                                                            |
| No endpoint writes a `spieler` row                                       | Open — no route creates a pupil, and the admission that will is the next programme's; the rows standing are those already there (I12)                                                                                                                                              |
| A ban past the list's read cap is unreachable                            | Open — `GET /sperrliste` serves `LIST_LIMIT_DEFAULT` rows and takes no paging control, so past that count `anzahl_gesamt` alone reports a ban nothing reaches                                                                                                                      |

- **An address only the API refuses is marked with the generic field sentence**
  (`fl_frontend/src/shared/utils/actionError.ts :: FELD_ABGELEHNT`), never its reason: a reserved or
  special-use domain (`.test`, `.local`) and a domain IDNA 2008 disallows pass the form's rule and
  reach the box as a `kind` the page does not word. No real address falls in either class.
- **The two runtimes convert a Unicode domain on their own Unicode tables**, so a label holding a
  code point newer than one runtime's release converts on one side alone; no registry issues such a
  label yet, and `fl_backend/tests/shared/email_addresses.json` holds the two to one answer on the
  domains that occur (I332).
- **A ban stored on a Greek domain may not match the address it bars.** Those keys were taken with
  NFKC and a full lower-casing ahead of the domain's conversion, which turn a lunate sigma, a
  mathematical final sigma, and a capital sigma ending the domain or standing before a hyphen into
  `ς` where `fl_backend/app/shared/folding.py :: canonical_address` keys `σ`. A Greek domain on this league's
  ban list is not realistic, and matching both keys would put back the Unicode fold the address rule
  keeps out of every key (I269, I332).
- **A transaction whose deadline passes between two of its operations stays open on the server.**
  pymongo sends no `abortTransaction` once the deadline has passed, and afterwards reports the
  session out of its transaction, so no call through its API can abort it; the server holds it,
  and the write locks it took, until the next transaction on that pooled server session supersedes it
  or `transactionLifetimeLimitSeconds` reaps it. An operation in flight at the deadline carries
  `maxTimeMS`, and the server aborts on it instead. `killSessions` would end it and is refused on the
  Atlas Flex tier this league runs on.
- **A request opening several transactions can answer a failure after one of them committed.** Its
  answer then says nothing of that commit. The sweeps run the transaction whose rows they hand back
  last (I323), and the season's mailing answers each team's row whatever the next team does
  (I282); every other such request leaves the committed part standing, answering `DB-FAIL-002` where
  the deadline cut it (I321) and `DB-FAIL-001` for any other database failure.
  An unanswered last commit is the one erasing case that ordering cannot reach: the registration
  sweep's season-end page erases its pupils and hands back whom it owes a message in one
  transaction, so where that commit lands unanswered the rows are gone and nothing names whom to
  tell. An outbox written inside the erasing transaction would close it, at the cost of a collection,
  its validator, a retention clock and an erasure reach for addresses it holds until the caller has
  mailed them.
- **One bracket save is one transaction whatever it moves.** `PATCH /spiele/{spiel_id}` and
  `PATCH /spiele/paarungen` (up to `fl_backend/app/shared/schemas/bounds.py :: LIST_LIMIT_DEFAULT`
  entries) must land whole, so they take no share per call as the sweeps do (I322): a save
  the request's deadline cuts off (I320) answers `DB-FAIL-002` (I321), and stands only where its
  commit did. A save moves the fixtures one result feeds through the bracket, a handful at the
  league's size.
