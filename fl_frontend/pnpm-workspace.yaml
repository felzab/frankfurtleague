allowBuilds:
  sharp: true
  unrs-resolver: true
overrides:
  # sharp and postcss reach the tree through next at versions carrying advisories
  # (`docs/_auditing/reports/2026-07-frontend.md`); these pin next's resolution, not
  # the patched direct dep. Only sharp is runtime-reachable: libvips CVEs on /_next/image.
  sharp: "^0.35.3"
  postcss: "^8.5.24"
