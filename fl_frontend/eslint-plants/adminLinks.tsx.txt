// Linted as src/features/zzplant/adminLinks.tsx.
import Link from "next/link";

import { ShellNotFound } from "@/shared/components/ui/ShellNotFound";
import { withSaisonId } from "@/shared/utils/saisonHref";

declare const base: string;
declare const saisonHref: (route: string) => string;
declare const saisonId: string;

type Landing = "/bereich/admin" | "/";

export const WRAPPED = withSaisonId("/bereich/admin/teams", saisonId);
export const BY_THE_HOOK = saisonHref("/bereich/admin/spiele");
export const NAMES_THE_PARAMETER = "/bereich/admin/teams?saison_id=2627";
export const HAND_BUILT = `${base}/bereich/admin/spiele`; // expect: admin-link
export const TEMPLATED = `/bereich/admin/teams/${saisonId}`; // expect: admin-link
export const SINGLE_QUOTED = '/bereich/admin/spielorte'; // expect: admin-link
export const SECOND_ARGUMENT = withSaisonId(saisonId as never, "/bereich/admin/x" as never); // expect: admin-link
export const LANDING: Landing = "/";
export const WRAPPED_TEMPLATE = withSaisonId(`/bereich/admin/teams/${saisonId}`, saisonId);
export const TEMPLATE_NAMES_THE_PARAMETER = `/bereich/admin/teams?saison_id=${saisonId}`;
export const AFTER_THE_FRAGMENT = "/bereich/admin/teams#liste?saison_id=2627"; // expect: admin-link
export const TEMPLATE_SECOND_ARGUMENT = withSaisonId(saisonId as never, `/bereich/admin/${saisonId}` as never); // expect: admin-link
// An excuse names this ban alone, so every other ban still reaches its line.
// eslint-disable-next-line local/admin-link -- a plant excusing its admin link
export const EXCUSED = ["/bereich/admin/teams", "flex gap-5"]; // expect: gap

export function AdminLinks() {
  return (
    <div>
      <Link href="/bereich/admin/teams">Teams</Link>{/* expect: admin-link */}
      <ShellNotFound href="/bereich/admin/teams" />
      <ShellNotFound href={`/bereich/admin/${saisonId}`} />
    </div>
  );
}
