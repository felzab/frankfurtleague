// Linted as src/features/spiele/components/forms/AdminEditSpielDataForm/PickOrCreateAutocomplete.tsx.
// The venue and referee picker, whose emptying drops its whole section, and every other ban still reaches it.
declare const Autocomplete: (props: object) => null;

export const SECTION_PICK = <Autocomplete isRequired={false} name="ort.spielort_id" className="flex gap-5" />; // expect: gap
