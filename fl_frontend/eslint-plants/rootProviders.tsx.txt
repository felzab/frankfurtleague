// Linted as src/core/providers/RootProviders.tsx.
// The root providers may mount react-aria's locale provider, and every other import ban still reaches them.
import { I18nProvider } from "@heroui/react/rac";
import { Button } from "@heroui/react"; // expect: vendor-root
import { Form } from "@heroui/react/form"; // expect: heroui-form
import { AppRouterContext } from "next/dist/shared/lib/app-router-context.shared-runtime.js"; // expect: next-private
import { DateField } from "@heroui/react/date-field"; // expect: segmented-import
import { NumberField } from "@heroui/react/number-field"; // expect: number-field
import { HintPopover } from "@/shared/components/ui/InfoHint"; // expect: hint-internals expect: layer-core
import { captureStdout } from "@/core/stdoutCapture"; // expect: test-only
import { createRequire as loadVia } from "node:module"; // expect: node-module
import { SITE_URL } from "@/core/brand"; // expect: site-origin
import { TextField as HeroTextField } from "@heroui/react/textfield"; // expect: marked-field

export const USED = [I18nProvider, Button, Form, AppRouterContext, DateField, NumberField, HintPopover, captureStdout, loadVia, SITE_URL];
export const TEXT_FIELD_USED = HeroTextField;
