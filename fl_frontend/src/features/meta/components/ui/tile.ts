/**
 * The brand box a meta page's glyph sits in. A tile is not a block, so a page carries as many as it
 * has cards without spending its one `bg-brand-solid` block (`docs/frontend/spec.md` §1.17).
 */
export const META_TILE = "bg-brand-solid text-brand-solid-foreground flex size-10 shrink-0 items-center justify-center rounded-xl shadow-sm";
